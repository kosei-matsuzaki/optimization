# 現状（俯瞰）

**この文書は毎回上書きされる。** 追記しない。履歴は [research_loop.md](research_loop.md) のログ、詳細は [acceptance_topology.md](acceptance_topology.md)。

- 更新: 2026-09-22（俯瞰、**その157 まで反映**）。前回は 09-21（その153 まで）。**この 4 サイクルで前回「首位の弱点」と書いた赤字（公表値との内訳の食い違い）の容疑を 3 本とも潰したが、<u>3 本とも無罪だった</u>** ＝ **測り方では説明がつかない。** そして**同じ 4 サイクルが、自分の側の数値も 1 本削った。**
- `research-loop` は `origin/main` より **151 commit / 429 ファイル / +18937 −3560 行**先。最古の未マージは 09-11 12:30 UTC ＝ **11.4 日 ＝ マージ規則（1 日 1 回）を 10.4 日超過。**
- **方針欄の最新は 2026-09-11 で (1)(2)(3)(4) は履行済み ＝ 未処理の指示は無い。**
- **今回も実験はしていない。`core/` は 1 行も触っていない。MC-ESO の既定は 1 つも変えていない。**

---

## ユーザーの判断を仰ぐこと

**(0)【操作、いちばん急ぐ。前回から 13 commit 増えた】`research-loop` を `main` にマージしてください。** 151 commit / 11.4 日。
**status.md と report.html は毎日全面書き換えされる 2 本**なので、**溜めるほど人間側のマージが conflict に化けます。** 俯瞰は `main` に commit しません。

**(1)【10 日目 → 11 日目・繰り越し。そして<u>推奨を変えます</u>】ゴール文の読み方 ＋ 論文の背骨をどちらに置くか。**
**読み (A)「出自を問わない」／読み (B)「この研究が作った手法で勝つ」の対立は不変で、(B) なら 157 サイクルで勝ちはゼロです。**
**変わったのは (A) の勝ちの足元です。** 前回「キュー 1 の結果で (A) が生きるか死ぬかが決まる」と書きましたが、**その検査は 3 本走って 3 本とも空振りでした** ——
**報告点数（その154: MPR は報告点数に対して完全に平ら）／ 配線（その155: 公表文書が名指しする唯一のつまみ c を 100 倍振って 0.049）／ instance（その157: 4 本平均でも −0.1535、instance が説明するのは 15.7%）。**
**＝ 「公表 MPR 0.844 に対してこちらが 0.69」という赤字は、測り直しでは閉じません。**
**推奨**: **論文の背骨を「公表値との比較」から「harness 内の直接対決 ＋ ベンチマークの可比較性」へ移す。**
**理由は 1 行** —— **harness 内の主張（新 suite D=10 で niching 4 手法 5 配線が記憶なし多スタートに負ける）はこの赤字の影響を受けないが、
harness をまたぐ主張は、自分で測った食い違い 0.09〜0.20 の 1/3 以下の幅を主張しているため。**
**この移動は (A)(B) どちらの読みでも同じ**（公表値との比較は (A) の勝ちの置き場であって、(B) の勝ちは元からゼロ）。
**一行「背骨を移してよい」で十分です。** キュー 1・2 はどちらの答えでも撃つので、待たずに進めます。

**(2)【11 日目・繰り越し。内容・推奨とも不変】その93 のクラス上限が D=20 で外れていました。**
**その151 の実測 MPR 0.3831 が、その93 の D=20 上限 4 通り（0.3149 / 0.3273 / 0.3350 / 0.3729）を<u>すべて超えました</u>**（D=10 では同じ screen が 97.2% で当たっていた）。
**同じ型の screen が、2026-09-08 のユーザー決定・案 (C) の根拠です。**
**俯瞰の推奨は変わらず「案 (C) は変えない」**（根拠が推定から実測に置き換わっている ——
新 suite D=10 で MC-ESO 0.1385 対 記憶なし多スタート 0.6284、16/16）。
**判断が要るのは 1 点だけ** —— **「クラス上限」という語を論文と記録から降ろしてよいか。** 降ろす場合の作業はキュー 4 です。
**止める場合だけ書いてください。**

---

## いま論文に書ける主張

**「この研究が作った手法が既存手法より多く見つけた」は 157 サイクル目も書けない。** 今回は**自分の側の数値が 1 本弱くなり、外部アンカーが 1 本閉じた。**

**【堅い。harness 内で閉じているので、下記「弱い環」1 の影響を受けない】**
1. **新 suite D=10 で niching 手法が記憶なし多スタートに負けるのは 4 手法 5 配線で成立**（MC-ESO 0.1385 / `NCDE` 0.1171 / `r3pso` 0.1468 が 16/16、公表帯 `r3pso-p400` 0.3341 も 16/0/0。例外は直した NMMSO の 12/16）。**この研究でいちばん堅い 1 行。**
2. **素の basin-hopping も 16/16 で負ける**（その133）＝ 強いのは「摂動つき再起動」一般ではなく記憶を持たない多スタートそのもの。
3. **勝っている側の 2 つのつまみは両方とも振り終わり、符号はどちらでも動かない**（`descent_budget` 5 水準、交差点は正規予算の 1/325〜1/227 で既定はその 8.1 倍上、上半分は 16 問中 9 問が完全同値の台地。`sigma_ratio` 3 水準は 4 倍振って有意差なし）。**ただしこの点検はすべて D=10 の上。**（その145〜その148）
4. **【方法論・実測 3 件】外部実装は実装の既定ではなく論文の実験節と 1 行ずつ照合する**（NMMSO +0.2596 / `r3pso` +0.1873 / `NCDE` は主水準で帰無）。**欠陥は実在して大きいのに判定を 1 問も動かさない。**（その143）
5. **【方法論・今回いちばん強くなった】この競技の公式 Score は harness をまたいで比較できない。** **同一手法（RR-CMA-ES）を同一の公表設定で測って、MPR は 0.09〜0.19 <u>低く</u>、mean-F1 は 0.12〜0.20 <u>高く</u>出る。Score の ±0.05 一致はその相殺である。**
   **この食い違いは 4 instance × 3 次元で符号も桁もそろい**（その157: Score +0.0308 / mean-F1 +0.2151 が 4 本とも同形）、**報告点数・被覆係数 c・再起動本数・instance のどれでも説明できない**（その154・その155・その157）。**＝ 判定規則を 1 つ足す: 公表値との照合は Score 単独で判定せず、MPR と mean-F1 を並べ、両者が同じ向きにそろって初めて「再現した」と書く。**

**【範囲つき。今回 D=5 の数値が弱くなった】**
6. **`Restart-Lander`（記憶なし多スタート）が競技次点 RR-CMA-ES を上回るのは、D ≦ 10 で符号が正、<u>α=0.05 を跨ぐのは D=10 の seed 0 の 1 本だけ</u>、D=20 では差が消える。**
   **D=5 は 4 instance 平均 +0.0305（SD 0.0197、+0.0550 / +0.0315 / +0.0289 / +0.0068。<u>どの instance でも非有意</u>）／ D=10 +0.0907（seed 0、p=0.029541。3 seed 平均 +0.0694 / p=0.0739、2 instance 平均 +0.0654 / p=0.0833）／ D=20 −0.0071（9/2/5、p=0.6698）。**
   **前回ここに書いた「D=5 +0.0550」は 4 instance で最も有利な 1 本だった**（その157）。**D=20 の正しい 1 行は「2 手法の差は消える」**（RR が勝つ、ではない）。

**【脆い。今回さらに弱くなった】**
7. **`Restart-Lander` が公表最良を上回る（D=5 の 4 instance 平均 +0.0613 / SD 0.0065、D=10 +0.0204、D=20 +0.0059）は、<u>内訳が一致しない比較の上の差</u>である。**（下記「弱い環」1）**instance では壊れない**（その157）が、壊れないことは食い違いを説明しない。
8. **公表最良を上回る（D=10）は報告半径にも依存する**（`r ≥ 0.001·span` の 6 点のみ。その130）。

## ゴールとの距離

**ゴール**: 多峰度の高い問題で、既存手法より多くの大域最適を見つける。**読み (B) では勝ちゼロ。読み (A) の勝ちは harness をまたぐ比較の上にあり、その比較の分解能が足りないことが今回確定した。**

**(i) 3 次元の並び**（新 suite 16 問、seed 0、正規予算 `50000·D`、報告規則 `eps_loose+dedup` r=0.05·span。D=5 は 4 instance 平均、D=10/20 は PIN01）

| D | 正規予算 | `Restart-Lander` | RR-CMA-ES | 対差 | p | 公表最良 | RL − 公表 |
|---|---|---|---|---|---|---|---|
| 5 | 250,000 | **0.7758**（SD 0.0065） | 0.7453（SD 0.0170） | +0.0305 | 全 instance 非有意 | 0.7145 | **+0.0613** |
| 10 | 500,000 | **0.6284** | 0.5377 | +0.0907 | 0.029541 | 0.6080 | +0.0204 |
| 20 | 1,000,000 | 0.4504 | **0.4575** | −0.0071 | 0.6698 | 0.4445 | +0.0059 |

**(ii) D=10 のその他の手法**: `NCDE` 0.1171 ／ MC-ESO 0.1385 ／ `r3pso` 0.1468 ／ `RL-1540` 0.3284 ／ `r3pso-p400` 0.3341 ／ 素の basin-hopping 0.3404 ／ **NMMSO（公表設定に修正後）0.4144。**
**(iii) 公表 MPR との赤字（RR-CMA-ES、同一手法どうし）**: **D=5 は PIN01 −0.1821、4 instance 平均 −0.1535（0.6905、SD 0.0205）。c=2 の効き +0.0487 を上界として足しても −0.1048。**（その153・その155・その157）
**(iv) D=2 は未測定。(v) 2D/3D（N04-N10）—— headroom 無し。(vi) F14-F20 —— 閉じた。**

## 確定した事実

- **n を増やす道は 3 本とも塞がった。** **seed（その123・その124）／ 次元（r(5,10)=+0.6594・r(10,20)=+0.6238）／ instance（D=5 の 4 本で SD 0.0205 ＝ seed ばらつき 0.0238 の 0.86 倍、問題ごとの相関は 6 対すべて +0.62 以上、最大 +0.9325）。** **独立な標本は 16 問で打ち止め。これは恒久的な上限。**（その149・その152・その153・その157）
- **測り方の容疑は全部閉じた。** 報告点数（MPR は完全に平ら）／ 被覆係数 c（100 倍で 0.049、しかも既定が 3 水準の<u>最小</u> ＝ 用量反応の形をしていない）／ 再起動本数（**26 本ではなく 125 本。その155 が archive サイズを本数と読み違えていた事実誤認を訂正**）／ instance。**残るのは検出規則 1 本（キュー 2）だけで、その先には「公表値を出した提出配線が別物」しか無く、その設定は手元のどの文書にも無い。**
- **instance は駆動を 1 ミリも動かさない。** 再起動の計器は PIN02/03/04 で 125.8 / 125.2 / 125.2 本・1 本あたり 2,031 / 2,045 / 2,037 評価 ＝ PIN01 実測と幅 1% 以内。**動くのは「1 本が当たるか」だけ**で、**その151（次元）・その155（c）が指した場所と同じ。**
- **足りないのは降下の本数でも長さでもなく、1 本あたりの当たり率。** D=5→20 で降下本数は 95.2 → 117.7 と<u>増えて</u>いるのに 1 本あたり評価は 5249 → 8497、広さ 0.584 倍・深さ 0.761 倍。（その151・その153・その157）
- **記憶なし多スタートのほうが instance に鈍い**（RL の instance 間 SD は RR の 1/2.6）。
- **`best_f` バイト一致のゲートは報告集合を覆っていない**（その156 の検算が拾った。F16-Weierstrass で `pr_1e-4` が base 0.65 対 腕 0.55）。**採用判断 6 件は全件不採用で閉じているので実害は無いが、ゲートの但し書きとして記録が要る。**
- **記録が黙って帰無を作れる経路が約 30 サイクル生きていた**（その150 §3）。**実害のあるもの 4 本のうち直したのは 1 本、<u>残り 3 本は未処理のまま</u>**（うち `e113/analyze.py` は頭の主張の機序側）。
- **GECCO 2025 の競技結果は 5 度目の検索でも見つからない** ＝ **公表値の出典 1 本という限界は 12 日間動いていない。**

## 否定された仮説（再提案しない）

| 仮説 | 結果 |
|---|---|
| PR は evals/K や K で決まる ／ 掘り残しの回収 ／ F14-F20 に勝てるセル ／ 報告点数・報告規則・cap・trim が差を説明する ／ 最適の配置の構造が差を決めている ／ 着地点近傍への再投入は効く ／ 公表設定に直せば外部 niching 手法は null に届く ／ MC-ESO の広さの不足は hunt 長による ／ 12500 はつまみの当たり ／ 次元軸・instance 軸を増やせば n が増える ／ その93 のクラス上限は一族の到達上限である | 棄却（その17〜その153。全文は [acceptance_topology.md](acceptance_topology.md)） |
| **公表 MPR との赤字は報告点数で説明できる** | **【今回】棄却 —— MPR は報告点数に対して完全に平ら**（その154） |
| **公表 MPR との赤字は RR-CMA-ES の配線（被覆係数 c）で説明できる** | **【今回】棄却 —— 100 倍振って 0.049、用量反応の形ですらない**（その155） |
| **公表 MPR との赤字は problem instance の当たり外れで説明できる** | **【今回】棄却 —— 4 instance 平均でも −0.1535、説明できるのは 15.7%**（その157） |

## いちばん弱い環

1. **【首位・今回「測って消せる」望みが消えた】公表値との比較は、同じ量を比べていない。そして 3 本の検査が全部無罪だった。**
   **赤字 0.09〜0.19 は報告点数でも c でも再起動本数でも instance でも説明できない。** 残る技術的検査は検出規則 1 本（キュー 2）だけで、**それも外れたら、残るのは測定ではなく編集上の決定**（公表値と比べる行を落とす）。**＝ この弱点は「直る」側ではなく「範囲を書いて畳む」側になった。**
2. **【153→157 サイクル連続】勝ちが「この研究の手法」からは 1 つも出ていない。この 21 サイクル、腕を 1 本も作っていない。**
   占有 11 件に加え、**自分の測定 2 件（その113「撒き方」16/16 で否定、その118「配置の構造」ρ=−0.091）が残りを潰している。**
   **ただし今回、20 サイクルぶりに<u>機序に向いた測れる問いが 1 本出た</u>** —— 下記「次に効く一手」の 1 番。
3. **harness 内の直接対決に有意性が無く、増やす道も 3 本とも塞がった。** α=0.05 を跨ぐのは D=10 の seed 0 の 1 本だけ（D=5 は 4 instance とも非有意、D=20 は p=0.6698）。**16 問は suite の全部で、seed・次元・instance はすべて疑似反復。＝ これは恒久的な限界で、今後どのサイクルでも直らない。**
4. **記録そのものが「有意でなかった」を黙って作れる。** その150 §3 の 4 本のうち **3 本が未処理**、うち `e113/analyze.py` は **頭の主張の機序側（4.5 倍差の内訳）**。**測定の論文を書く以上、体裁ではなく中身の問題。キュー 3。**
5. **外部比較の橋** —— **公表値の表が資料 1 本のまま 12 日間動いていない。** 本文取得は CI の `fetch_refs` が唯一の経路。
6. **`CLAUDE.md` が pin する SR@1e-10 93.5% / evals 798 がこの環境で 3 回とも再現しない**（92.08% / 677.7）。ゲートは `best_f` バイト一致だが、**そのゲートは報告集合を覆っていない**（その156）。**pin の数値は書き換えない。**
7. **片付けは規則違反ゼロ、余裕 56** —— `analysis/` は **344 ファイル / 2.11 MB**（平置きは `README.md` 1 本のみ、300 行超の非 gz CSV ゼロ、1 MB 超の塊ゼロ、`check_cost_rows.py` / `check_doc_links.py` とも通過）。
   **未処理の記述ずれは 2 件** —— **`web/app_lib/results.py:234` の「PR」列が `pr_1e-4`（<u>8 度目の再確認、未修正</u>）**、**`scripts/` 直下 22 本のうち 11 本が呼び手なし。**

## 次に効く一手

**キューは 4 → 5 件。順序を変えた。ゴールは変えていない。** **変更点は 2 つ:**
**(1) 新設した「降下 1 本あたりの当たり率の分解」を<u>1 番に置いた</u>。**（実行役が書いた検出規則の問いは 2 番へ。**並べ替えたのは俯瞰であって実行役ではない。**）
**理由**: **その157 自身が「この赤字は測り直しでは閉じない見込み」と書いている ＝ 検出規則の問いは<u>どちらに転んでも書く文が決まるだけ</u>で、ゴールは動かない。**
**対して 1 番は、21 サイクルで初めての機序に向いた問いで、しかも追加評価ゼロである。**
**(2) 前回「キューに入れなかった」と書いた D=2 / D=20 の seed 増 / `NCDE` の母集団は、今回も入れない**（理由は前回と同じ ＝ n を増やす道が 3 本とも疑似反復だと確定したので、どれも書く文を 1 語も変えない）。

- **1 番「なぜ 1 本あたりの当たり率が落ちるのか ——『届かない』のか『同じ所に何度も届く』のか」**（新設、追加評価ゼロ）——
  **保存してある降下ダンプ（D=5 は `e153`/`e157`、D=10 は `e115`、D=20 は `e151`）は 1 降下ごとに `land_opt` と `dist` を持っており、
  「大域最適に届いたか」と「その最適は既出か」を数え分けられる。** **俯瞰が 1 問だけ叩いた確認値**（D=20 / M01: 122 降下のうち 115 が届き、そのうち **105 が既出** ＝ 相異なるのは 10）**は、この問いが空でないことだけを示す。**
  **重要なのはこれが分岐すること** —— **既出が支配するなら律速は記憶であり、RR-CMA-ES が D=20 で追いつく機序の説明になり、<u>21 サイクルぶりに腕の置き場が出る</u>。
  届かないほうが支配するなら、再起動一族は高次元で原理的に勝てず、論文は測り方の側に確定する。** **どちらでも次が決まり、コストはゼロ。**
- **2 番「検出規則の上界を引く」**（実行役が その157 の結末として書いた問い。**位置だけ 1 つ下げた。内容は変えていない**）—— **弱い環 1 の最後の技術的検査、追加評価ゼロ。**
- **3 番「黙って帰無を作る経路の棚卸し」**（不変）—— **その150 §3 の残り 3 本を潰し、`e113` の結論を生き残った入力から引き直す。追加評価ゼロ。**
- **4 番「その93 の上限推定は<u>どこで</u>外れたのか」**（不変）—— **「クラス上限」を論文に書けるかがこれで決まる**（判断 (2)）。
- **5 番「記述とコードのずれ ＋ 棚卸し」**（不変。`web/` の PR 列は 8 度目の未修正）。

**率直に言うと、この 4 サイクルは前回いちばん重いと書いた弱点に 3 本の検査を当てて、3 本とも無罪を返した。**
**それは失敗ではないが、良い報せでもない** —— **「測れば直るかもしれない」が「測っても直らない」に変わったということだ。**
**157 サイクル目のこの研究の位置は、1 行で書ける** ——
**harness 内には堅い結果が 1 つある（niching 4 手法 5 配線が記憶なし多スタートに 16/16 で負ける）。harness をまたぐ主張は分解能が足りない。そして自分の手法による勝ちはゼロのままだ。**
**だから (1) の推奨は「背骨を harness 内 ＋ 可比較性に移す」であり、キュー 1 は「それでも機序の側に道が残っているか」を追加評価ゼロで確かめる、21 サイクルぶりの一手である。**
