# 現状（俯瞰）

**この文書は毎回上書きされる。** 追記しない。履歴は [research_loop.md](research_loop.md) のログ、
詳細は [acceptance_topology.md](acceptance_topology.md) にある。

- 更新: 2026-09-06（俯瞰ルーチン、**その73 まで反映**。前回は その61 までで 12 回分が入っていなかった）
- `research-loop` は `origin/main` より **102 commit / 349 ファイル / +17823 −395 行**先。
  main の先端は 09-03 08:01 UTC、research-loop は 09-06 19:07 UTC ＝ **約 83 時間 ＝ 3.5 日差。
  マージ規則（1 日 1 回）を大きく超えている。** 直近 24 時間で +3839 −970 行。
- **方針欄（ユーザーが書く欄）は空。** 以下は全部こちらの判断で、優先されるべき指示は入っていない。

---

## ユーザーの判断を仰ぐこと

**(1) ゴールの対象関数集合（判断 (2)。7 回目の再掲だが、今回だけは状況が変わった）。**

**その73 で初めて「既存手法に勝てる可能性のある関数」が実測で出た**（N18-CF3-10D、正規予算 4e5・12 seed）。
**MC-ESO は被覆 6/6**（PR@1e-1 = 1.000、12/0/0、p=0.0005）**なのに深さは 1/6**（PR@1e-3 = 0.194、
10/12 seed で 1 解）。**NMMSO は被覆 4/6（0.597）だが入った 4 のうち 3-4 を掘り切る（0.583）。公表最良は
PR@1e-5 で 0.667 = 4/6**（その71）。**＝ 6 盆地全部に入るのに 1 つしか掘らない。律速が正反対。**
落ちているのは「点が無い」ことではなく「点が深くない」こと（6 解とも 1e-1 で報告集合に載っている）で、
**その深さ側こそ本テーマが無償で閉じた軸**（c=1.0 / `_sig10` / `_fl08`）＝ **判断待ちの 6 件が初めて
「同点に近づく道具」ではなく「勝つ道具」の候補になった。推奨は変わらず (A)**（F14-F20 の 7 関数へ広げる）。
ただし 2 点を添える: **(i) 1 関数・12 seed の観察で、深さ調律が変換されるかは未測定**（キュー 1。その69 では
同じ c=1.0 が N09 で**符号が逆**だった前例がある）。**(ii) (A) はゴールの言い回しを変える** ——
F14-F20 は **K = 6-8 で D が大きい**問題で律速は「難しい 2 峰」（その70・その71）＝ **現ゴール文の
「K の大きい関数」も本テーマの主張「被覆の偏り」もそのままでは載らない。ゴール変更なので俯瞰では書き換えない。**

**(2) 採用判断 6 件（c=1.0 / `_sig10` / `_fl08` / `soltrim_rho` / `commit_place` / 合成腕）が 16 サイクル滞留。**
証拠側は全件閉じており実行役に測るものは無い。**分岐は前回と同じ 1 点**: ゲートを **`best_f` バイト一致**で
運用するか **pin（SR@1e-10 / `evals_succ_mean`）**で運用するか。手順は
[research_loop.md 末尾の (a)-(f)](research_loop.md#未解決の問い上から着手する)。
**前回書いた「4 件全部採用してもゴールには届かない」という但し書きは、(1) により無効になった。**

---

## いま論文に書ける主張

**「既存手法より多く見つけた」は今日もまだ書けない。1 つも勝っていない。**
変わったのは、**書ける可能性のある場所が 1 か所に特定された**こと（上記 F18）。

**前回 status.md が「論文になる条件」として掲げた「天井は best-of-n で再起動点を選ぶ手法クラスの性質だ」は
その64 で棄却済みである。撤回する**（幅比例は NMMSO を含む 4 手法すべてに出て、best-of-n の選抜は
体積比例 null からの外れ 16.6 pt のうち 1.9 しか作っていない）。証拠が揃っている主張は 2 つだけ:

1. **再起動型 niching の PR は 深さ × 報告 × 被覆 に分解でき、各軸は独立に閉じられる**
   （その51・その55 / その60・その63 / その57・その62）。PR は天井と報告比の**積**（N09 で予測 0.3541 対
   実測 0.3549）。**弱点: 主要部が MC-ESO 1 手法の内部観察で、唯一 4 手法に広げた測定は否定的結果だった。**
2. **その21 の二分法（`reported ≈ min(distinct, deep)`）が手法間で予測を出す**（その73 が初例。同じ F18-10D で
   MC-ESO は深さ律速、NMMSO は被覆律速）＝ **どちらが binding かは手法と問題の組で決まり、測れる。**
   これが 1 を「単一手法の内部観察」から「診断法」へ上げる唯一の道。

**文献側の注意（今回 3 語彙で確認）**: 「多様性 対 収束のトレードオフ」自体は niching の教科書事項で、
[MCMC 側にも mode coverage 診断がある](https://arxiv.org/pdf/2512.17977)。**査読者が最初に突くのはここ。**
直接の占有は見つからなかったが、**主張できるのは概念でなく計器の側**（hunt 単位の帰属・体積比例 null・
報告 cap の設計則）**に限る。この差は論文の書き方で守るしかない。**

---

## ゴールとの距離

**ゴール**: 多峰度の高い問題で、既存手法より多くの大域最適を見つける。
**(i) 現在の対象集合（2D/3D、その50、正規予算 15 seed、PR@1e-5）—— headroom が無い。**

| PR@1e-5 | N04 | N05 | N06 | N07 | N08 | N09 | N10 |
|---|---|---|---|---|---|---|---|
| NMMSO | 1.000 | 1.000 | **1.000** | **1.000** | **0.984** | **0.989** | 1.000 |
| MC-ESO（既定・実測） | 1.000 | 1.000 | 0.730 | 0.304 | 0.013 | 0.170 | 1.000 |
| MC-ESO（候補 6 件・**見込み**） | 1.000 | 1.000 | 0.985 | ~0.30 | 0.863 | 0.355 | 1.000 |

**「見込み」行は測定ではなく単腕の足し算。6 件同時の構成は一度も回していない**（引用時は必ずこう書く）。
**この集合では全件採っても勝ちはゼロ。**
**(ii) 案 (A) の対象集合（その73、正規予算 12 seed、PR@1e-5）—— こちらには余地がある。**

| PR@1e-5 | N14-CF3-3D | N16-CF3-5D | N18-CF3-10D |
|---|---|---|---|
| MC-ESO（既定・**無調律**） | 0.361 | **0.653** | 0.167 |
| NMMSO（同環境） | 0.667 | 0.667 | 0.500 |
| 公表最良（6 手法・50 run） | 0.793 | **0.677** | **0.667** |

**N16 は無調律で公表最良の 0.024 下**（12 seed 中 11 seed が 4/6）。N14 は 1e-4 → 1e-5 の段、
N18 は 1e-1 → 1e-3 の崖 ＝ **どちらもその51・その55 が N08 で閉じた署名と同じ。**
**F14-F20 の残り 4 関数（CF4 系 F15/F17/F19/F20）は未測定。**

---

## 確定した事実

- **深さの軸は閉じた**（c=1.0 で N08 の PR@1e-5 0.013 → 0.754、σ 解像度で 0.863、代金ゼロ）。
  **報告の軸も閉じた**（N09 で `reported == visited` が 15/15 seed）。
  **被覆の天井は現在の道具では出し切った**（N09 0.355 が 3 サイクル動かず、降下路線も閉じた）。
- **「残差はすべて被覆側」は一般則ではなく現在の関数集合の性質だった**（その73。**CF3-10D では 100% 深さ側**）。
- **F11-F13 の headroom はゼロ。案 (A) の実体は F14-F20 の 7 関数**で、律速は「難しい 2 峰」。
  **予算不足ではない**（10^9 評価でも解けないと公表されている、その71）。
- **CEC2013 F11-F20 は既存依存の `ioh` で回せる**（データファイル不要、照合 5 通り通過、その70）。
- **BBOB-24 dim2 の gate は「枯渇後だけを触る介入」に構造的に盲目**（例外は `soltrim_rho` の履歴一致）。

## 否定された仮説（再提案しない）

| 仮説 | 結果 |
|---|---|
| PR は evals/K や K で決まる | 棄却（その17） |
| 報告点数・報告規則が原因 | 棄却（その28）＋ 先行研究に占有 |
| 反発半径の適応 / 位相分けで被覆が直る | 棄却（その22・その29） |
| 掘り残した盆地に戻れば回収できる | 棄却（その30）。真の在庫は N06 0.3 / N07 0.0 |
| commit を無条件の梃子として足せる | 棄却（その42）。被覆律速専用 |
| 1/10 予算の順位は正規予算の代理になる | 棄却（その50） |
| 内部 archive の容量を上げれば報告損失が減る | 棄却（その60）。外側の cap を自分で踏む |
| **被覆の天井は best-of-n クラスの性質** | **棄却（その64）。4 手法すべてに幅比例が出る** |
| **残差は全関数で被覆側** | **棄却（その73）。CF3-10D では 100% 深さ側** |

**今回は新手法を提案しない**（占有記録への追加もゼロ）。**その73 が既存の道具で届く道を開けた以上、
いま新しい機序を足すのは希釈にしかならない**というのが判断。

---

## いちばん弱い環

1. **F18 の発見は 1 関数・12 seed。** CF4 系 4 関数は未測定で、「10D で被覆だけは取れる」が CF3 固有か
   手法の性質かが分からない。**ここが崩れると推奨 (A) ごと崩れる**（キュー 2）。
2. **超えるべき目標値そのものが未確認。** 公表最良 0.667 の出所は HillVallEA GECCO'18 の 6 手法表 1 本だけで、
   **HillVallEA19（[arXiv 1907.10988](https://arxiv.org/pdf/1907.10988)）と GECCO'19 競技結果は未取得** ——
   より高い値があれば「初めて勝てる」は成立しない（キュー 3）。
3. **採択できた改善が 16 サイクル連続でゼロ**（実行役は診断腕でしか前進できない）。**候補 6 件を同時に
   入れた構成も一度も無い**（上表の「見込み」は足し算で、交互作用は未測定）。
4. **`CLAUDE.md` が pin する SR@1e-10 93.5% / evals 798 はこの環境で 2 回とも再現しない**（92.08% / 677.7）
   ＝ 「MC-ESO を壊さない」ゲートが書かれたとおりには検証できない（キュー 6）。
5. **リポジトリ側（今回も未改善）**: UI の PR 列が全行 0（`web/app_lib/results.py:234` が `pr_1e-4`、
   書き出しは `core/runner.py:258` の `cec_pr_1e-4`。**4 度目の確認**、キュー 4）。`analysis/hm/` 直下に
   **平置き 146 ファイル（CSV 123 本・8522 行）が前回と同数のまま**（キュー 5）。参照ゼロのスクリプト 4 本、
   `CLAUDE.md:35` の `run.sh` 一覧が `loop` / `stop` を落とす（キュー 6）。
   **`mceso.py` の既定値と docs の行番号引用は今回のスポットチェックで全件一致した。**

---

## 次に効く一手

**キュー 1（採用候補の c=1.0 を N18-CF3-10D / N14-CF3-3D に当てる）。** 理由は 3 つ:
(a) **本研究で初めて「勝てる」と書ける可能性がある唯一の場所**（N18 は被覆 6/6、公表最良は 4/6）、
(b) **道具は既に無償と確認済み**（その47 が BBOB 960 セル、その48 が niching 7 関数）で、新規に回すのは
1 腕だけ（base は `analysis/hm/e73/` の保存 CSV と seed 対応で対にできる、枠 10 分）、
(c) **その69 の反例があるので符号は測るまで分からない** —— 先に 3 秒の `_init_state` probe で
`f_init_scale` を数え、c=1.0 が締める側に働くことを確認してから腕を回す。

**キューから外したもの**: **被覆の天井・幅比例・降下に関する問い一式。** その64 で棄却され、
その67-その69 で機序側が枯れ、**天井 0.355 は 3 サイクル動いていない。この軸を動かしても
NMMSO 0.989 との差は埋まらない**ので、面白くてもゴールを動かさない。
