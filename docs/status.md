# 現状（俯瞰）

**この文書は毎回上書きされる。** 追記しない。履歴は [research_loop.md](research_loop.md) のログ、詳細は [acceptance_topology.md](acceptance_topology.md)。

- 更新: 2026-09-19（俯瞰、**その145 まで反映**）。前回は 09-18（その141 まで）。**この 4 サイクルで、前回いちばん弱いと書いた環（手法軸 n=2）が閉じ、2 番目に書いた環（監査の非対称）が<u>こちら側の敗北で発火した</u>。**
- `research-loop` は `origin/main` より **108 commit / 351 ファイル / +12735 −3137 行**先。最古の未マージは 09-11 12:30 UTC ＝ **8.3 日 ＝ マージ規則（1 日 1 回）を 7.3 日超過。**
- **方針欄の最新は 2026-09-11 で (1)(2)(3)(4) は履行済み ＝ 未処理の指示は無い。**
- **今回も実験はしていない。`core/` は 1 行も触っていない。MC-ESO の既定は 1 つも変えていない。**

---

## ユーザーの判断を仰ぐこと

**(0)【操作、いちばん急ぐ。前回から 12 commit 増えた】`research-loop` を `main` にマージしてください。** 108 commit / 8.3 日。
**規則は 1 日 1 回**で、**status.md と report.html は毎日全面書き換えされる 2 本**なので、**溜めるほど人間側のマージが conflict に化けます。** 俯瞰は `main` に commit しません。

**(1)【7 日目 → 8 日目・繰り越し。いちばん重い】ゴール文の読み方。**
ゴール文（「多峰度の高い問題で、既存手法より多くの大域最適を見つける」）は**手法の出自を要求していません。**
**読み (A)「出自を問わない」なら、新 suite に勝ちが 1 つあります**（記憶なし多スタート `Restart-Lander` 0.6238 対 公表最良 0.6080）。
**読み (B)「この研究が作った手法で勝つ」なら、145 サイクルで勝ちはゼロです。** **推奨は変わらず (A)。**
**これが決まらないと論文の頭の文が決まりません。** 一行「(A) でよい」だけで十分です。

**(2)【文面を更新。推奨は不変】論文の型は (A) 主・(B) 方法論節。**
**型 (A)（測定の論文）は今回<u>両方向に</u>動きました** —— **強くなった**（手法軸が n=2 → n=4、しかも 5 配線。下記 1）、**同時に弱くなった**（勝っている側の設定依存が実測された。下記「弱い環」1）。
**型 (B)（再現性の論文）は実測 3 件目が付いた**（`r3pso` を公表帯 400 に直すと +0.1873 で有意だが、**null との差の 39% しか埋めず判定は 1 問も動かない**。その143）。
**推奨が変わらない理由**: **(B) で壊れていたのは公開実装ではなくこの研究の wrapper** ＝ 頭に置くと (A) の信頼性を自分で削ります。**異論があれば書いてください。**

---

## いま論文に書ける主張

**「この研究が作った手法が既存手法より多く見つけた」は 145 サイクル目も書けない。** そのうえで、**今回はじめて「順位」ではなく「機序」の形で書ける文が立った。**

**【今回立った。いちばん強い形】**
1. **記憶なし多スタートが強いのは、広さと深さを<u>両方</u>持つ唯一の手法だからで、その 2 つは分離して測れる。**
   **広さは配置が作る**: hunt 長を 1/8.1（12500 → 1540 評価）に切っても **1e-1 被覆は 0.6031 → 0.5531**（差の 12.9% しか失わない）。**同じ予算配分の MC-ESO は 0.2156。**
   **深さは hunt 長が作る**: 同じ腕の深さ保持率（@1e-5 ÷ @1e-1）は **0.186**（null 0.850 / MC-ESO 0.275）＝ **1540 評価 ＝ 154 世代では D=10 の盆地を 1e-5 まで詰め切れない。**
   **被覆差の寄与は水準で入れ替わる**（1e-1 では 87% が配置、1e-5 では 90% が hunt 長。5 水準平均では hunt 長 59.5% / 配置 40.5%）。**出所はすべて その145。**
   **そして競合は 3 本とも<u>別々の側</u>を欠く**: **MC-ESO は配置**（その113・その118・その145）、**`r3pso` は被覆**（1e-1 → 1e-5 で PR が 0.1125 → 0.1031 ＝ 深さは足り、規則適用後の報告点数が 1.6 点。null 8.9。その142）、**hunt を切った null は深さ**（その145）。
   **＝ 「専用手法に勝つ」ではなく「広さと深さを同時に買える配分が他に無い」と書ける。**

**【今回、手法軸が n=2 → n=4 になった。前回の首位の弱い環が閉じた】**
2. **新 suite D=10 で niching 手法が記憶なし多スタートに負けるのは、4 手法 5 配線で成立する。**
   **16/16 で負けるのが 3 本** —— **MC-ESO 0.1385**（その116）、**`NCDE` 0.1171**（+0.5113、16/0/0、p=3.052e-5、rb=+1.000。その142）、**`r3pso` 0.1468**（+0.4816、同上。その142）。
   **公表帯に直した `r3pso-p400` も 0.3341 で 16/0/0**（p=3.052e-5、rb=+1.000。その143）＝ **配線を直しても符号は動かない。**
   **例外は直した NMMSO だけで 12/16**（16 問平均 +0.2140、p=0.004181、rb=+0.779。その139。**撤回済みの 4 問 M01/M02/M04/M10 のうち範囲が分離するのは M10 のみ**。その140）。

**【堅い】**

3. **素の basin-hopping も 16/16 で負ける**（MPR −0.2913、0/0/16、p=3.052e-5。その133）＝ **強いのは「摂動つき再起動」一般ではなく、記憶を持たない多スタートそのもの。**
4. **この採点器は公表値を再現する**（その127-131）。同一採点器上の RR-CMA-ES は **3 seed 平均 0.5544**で、**公表値 0.5730 との差は 3 本とも ±0.05 の内側。**
5. **順位は正の報告半径の全域（`r/span` 1e-6〜0.5 ＝ 5 桁半）で不変**、**PR 側は半径と無関係**（その130）。**反転は `r=0` の 1 点だけ。**
6. **報告軸は 3 手法とも閉じた**（その115・その116）。合法な規則が **oracle 上限と 16/16 厳密一致**。**その145 の腕でも成り立つ**（MPR と 5 水準平均被覆が 4 桁一致）＝ **報告規則はこの 4 サイクルの結論に 1 つも効いていない。**
7. **【方法論。実測 3 件目】外部実装は、実装の既定ではなく論文の実験節と 1 行ずつ照合しなければならない。**
   **NMMSO**: `swarm_size` が公表設定の 1/D 倍 ＝ Score 0.1548 → 0.4144（+0.2596、15/0/1、p=9.155e-5）。**対公表差は D とともに単調に開く**（その136・その137）。
   **`r3pso`**: 直書き 30 対 Li 2010 §VI-B の 300-800 ＝ CEC2013 5 関数で PR@1e-5 が +0.1233（p=1.757e-7、38/1/11）、**新 suite で 400 にすると +0.1873（15/1/0、p=6.1e-5）**（その141・その143）。
   **`NCDE`**: 30 → 300 は主水準で帰無（+0.0150、p=0.348）だが**内訳は打ち消し**（N15 +0.1000 / N17 +0.0875 対 **N20 −0.1250、0/9/1、p=0.0039**。その141）。
   **【今回いちばん効く 1 行】欠陥は実在して大きいのに、判定を 1 問も動かさない** —— **400 は 30 を +0.1873 上回るが、null との差 −0.4816 の 39% しか埋めない**（その143）。

**【脆い】margin の主張 —— 有意水準を通らない**

8. **`Restart-Lander` が競技次点に勝つ、は 16 問 3 seed で +0.0694 / 11 勝 5 敗 / p=0.07391 / rb=+0.515**（その131）。**MPR 側だけ通る**（+0.0810、p=0.02899）が、**判定指標は Score**（方針欄 2026-09-11）なので通らない側を採る。
9. **`Restart-Lander` が公表最良を上回る（+0.0204）、は報告半径に依存する**（その130）。**上回るのは `r ≥ 0.001·span` の 6 点だけ。**
10. **【今回追加。同じ棚のいちばん上】頭の Score は、この研究が自分で置いた降下予算に依存する** ——
    **`descent_budget` を 12500 → 1540 にすると 0.6284 → 0.3284 で、直した NMMSO の 0.4144 を下回る**（+0.3000、14/0/2、p=1.526e-4、rb=+0.956。その145）。
    **動いた幅 0.3000 は頭の主張の margin（+0.2140）より大きい。** **ただし MC-ESO 0.1385 は 8.1 倍縮めた null にも 2.4 倍で負ける** ＝ **上記 2 の <u>3 本の 16/16</u> はこのつまみに依存しない。依存するのは NMMSO との順位だけ。**

## ゴールとの距離

**ゴール**: 多峰度の高い問題で、既存手法より多くの大域最適を見つける。**読み (B) では 3 つの土俵とも勝ちゼロ。読み (A) では新 suite に検証待ちの勝ちが 1 つ。**

**(i) 新 suite（D=10、16 問、正規予算 50 万、報告規則 `eps_loose+dedup` r=0.05·span、PIN01・seed 0）**

| 16 問平均 Score（公式） | 値 | 出所 |
|---|---|---|
| `NCDE`（出荷既定 30） | **0.1171** | その142 |
| MC-ESO | **0.1385** | その116 |
| `r3pso`（出荷既定 30） | **0.1468** | その142 |
| **`Restart-Lander-1540`（null の hunt を 1/8.1 に切った腕）** | **0.3284** | **その145** |
| `r3pso-p400`（公表帯） | **0.3341** | その143 |
| 素の basin-hopping | **0.3404** | その133 |
| **NMMSO（公表設定 `10·D` に修正後）** | **0.4144** | その139 |
| **RR-CMA-ES（競技次点）実測** | **0.5544** | その131（3 seed 平均） |
| **記憶なし null（`Restart-Lander`）** | **0.6284** | その131（3 seed 平均 0.6238 / 3 インスタンス平均 0.6392 ＝ その122） |
| TRDE-LR（**競技優勝**）公表値 | **0.6080** | 競技資料（**出典 1 本**） |
| RR-CMA-ES（次点）公表値 | **0.5730** | 同上 |

**＝ null は公表最良を +0.0204（seed 0）／ +0.0312（3 インスタンス平均）上回る。ただし 1 seed・自分で選んだ報告半径 対 15 インスタンス平均・出典 1 本。**
**上積みの見込み**（その119、腕ではなく上限）: **16 問平均 +0.1134 / 群 A +0.2269。上限と実装可能な腕を分けているのは「中心の情報」**で、**自己情報だけで作る形は その133 で棄却。**

**(ii) CEC2013 の配線**: NMMSO は `10·D` で 5 関数中 4 本が公表値の ±0.10 内（その137）。**`NCDE` / `r3pso` には照合先が存在しない**（Fieldsend 2014 Table V の 15 手法に無く、原論文は 2012 / 2010 で suite より前。その141）。
**(iii) 2D/3D（N04-N10）—— headroom 無し。(iv) F14-F20 —— 閉じた。**

## 確定した事実

- **報告軸は空**（その115・その116・その130・その145）。**この軸にはもう 1 サイクルも使わない。**
- **MC-ESO の広さの不足は hunt の<u>本数でも長さでもなく、置き場所</u>**（本数 ＝ その113 の 16/16、長さ ＝ その145 の 1e-1 被覆）。**＝ hunt 長の調律を広さに使わない。**
- **記録済みの外部手法の値には配線の但し書きが要る** —— **`r3pso` はすべて母集団 30（公表帯の下端未満）、NMMSO は新 suite のみ修正済みで CEC2013 は 5 関数だけ、`NCDE` は出荷既定 30 を新 suite で一度も振っていない**（帰無の根拠は CEC2013 5 関数のみ）。**既定は変えない。但し書きはキュー 4(a) が入れる。**
- **null の 2 つのつまみはこの研究が置いた値。`descent_budget=12500` は「予算 ÷ 40」、`sigma_ratio=0.1` は その103 の値で、<u>MC-ESO の σ_init（0.2）とは別の値</u>**（`restart_lander.py:56` / `mceso.py:216`。**この俯瞰でコードを直接確認した**）。**`sigma_ratio` はまだ 1 度も振っていない。**
- **引用候補も競技の問題別値も実在しない**（その134）。**GECCO 2025 の競技結果は 09-19 の再検索でもまだ見つからない**（3 度目）＝ **公表値の出典 1 本という限界は 10 日間動いていない。**
- **seed のばらつきは手法によって桁が違う**（n=3 SD は RR 0.0238、null 0.0066）。**1 seed のスクリーンは誤導し**（その135）、**1 run の値で符号は主張できない**（その139・その140）。
- **suite は最適の配置で半分に割れる**（群 A = PID 1-8 が 8/8 で過集中。その118）。**塊の成員は誰も届いていない**（その119）。

## 否定された仮説（再提案しない）

| 仮説 | 結果 |
|---|---|
| PR は evals/K や K で決まる ／ 掘り残しの回収 ／ 天井は best-of-n の性質 ／ F14-F20 に勝てるセル ／ 異方な降下・hill-valley ／ draw 本数・σ0 の掃引 ／ 報告点数・報告規則・archive 容量・cap・trim が差を説明する | 棄却（その17〜その117。全文は [acceptance_topology.md](acceptance_topology.md)） |
| MC-ESO が null に負けるのは hunt 本数が足りないから ／ 最適の配置の構造が差を決めている ／ 塊は飽和している ／ 頭の数字は PIN01 の当たり | 棄却（その113・その118・その119・その122） |
| 自作の配線か採点器のせいで NMMSO / MC-ESO が低い ／ `pynmmso` は公表表を再現しない ／ 新 suite で niching 手法 2 本が 16 問**全敗**する | 配線は成立・採点器と `pynmmso` は無実（その127・その131・その136・その137）。全敗は部分的に棄却（NMMSO が 4 問で上回る。**16 問平均の負けは残る**） |
| `NCDE` / `r3pso` の直書き 30 は NMMSO と同型の欠陥である | `r3pso` は成立（+0.1233）、`NCDE` は主水準で不成立（p=0.348）（その141） |
| **公表設定に直せば外部 niching 手法は null に届く** | **【今回】棄却 —— `r3pso-p400` は 16/0/0 で負け、欠陥の補正は差の 39% しか埋めない**（その143） |
| **MC-ESO の広さの不足は hunt 長が短いことによる** | **【今回】棄却 —— hunt 長を 1/8.1 にした null は 1e-1 被覆を 12.9% しか失わない**（その145） |
| 着地点近傍への再投入は効く ／ 利得は null が弱い問題に集中する ／ 報告半径を変えると順位が変わる | 棄却（その130・その133） |

## いちばん弱い環

1. **【今回、首位が入れ替わった】監査が片側にしかかかっておらず、初めてかけたら<u>こちら側が落ちた</u>** —— **ベースライン 3 本は公表設定と照合したのに、勝っている null の 2 つのつまみはこの研究が置いた値。査読者が最初に突くのはここ。**
   **ただし論点を正確に言うこと** —— **どの手法も detune すれば落ちる**ので、その145 が示したのは「頭の主張が脆い」ことではなく **「12500 が suite の上で選ばれた値でないことを量で示す義務がある」**こと。
   **塞ぐ形は 1 つだけ**: **順位が入れ替わる水準を挟んで「null の優位は降下予算 X 以上で成立する」と書く**（キュー 1）。**「つまみの当たりでは」に数値で答えられる唯一の形。**
2. **【145 サイクル連続】勝ちが「この研究の手法」からは 1 つも出ていない**（判断 (1)）。**この 9 サイクルは腕を 1 本も作っていない**（配線の修復・手法軸の拡張・自分の監査に使った）。
   **新しい機序の候補はいま無い。** **今回 1 件を調べて見送った**（下記「次に効く一手」の末尾）。
3. **直接対決の有意性が出ていない**（+0.0694、p=0.0739）。**問題軸は尽きた**（16 問は suite の全部）。**instance 軸しかない**（キュー 3）。
4. **外部比較の橋** —— **公表値の表が資料 1 本のまま、10 日間動いていない。** **本文取得は CI の `fetch_refs` が唯一の経路**（この俯瞰の環境からは `sites.google.com` も `arxiv` PDF もブロックされる）。
5. **新規性は測定の側にしか残っていない。** 現象は **Lobo & Bazargani（2015）**と **Cano+ 2022** に占有。**主張の<u>型</u>は隣接分野（NAS）に先例**（*"Local Search is a Remarkably Strong Baseline for NAS"*, PPSN 2020）。**占有ではないが、論文ではこれを引いて差分を書くこと。**
6. **`CLAUDE.md` が pin する SR@1e-10 93.5% / evals 798 がこの環境で 3 回とも再現しない**（92.08% / 677.7）。ゲートは `best_f` バイト一致。**pin の数値は書き換えない。**
7. **片付けは今回いちばん余裕がある** —— **`analysis/` は 309 ファイル / 1.74 MB（`du -sb`）＝ 目安 400 まで 91。2 サイクル連続で削除が追加を上回り、1 MB 超の塊はゼロ。規則違反もゼロ。**
   **未処理の記述ずれは 3 件** —— **`web/app_lib/results.py:234` の「PR」列が `pr_1e-4`（6 度目の再確認、未修正）**、**`scripts/` 直下 22 本のうち 11 本が呼び手なし**、**【今回、機械検査で発見】`check_cost_rows.py` が「その145 のコスト行が無い」を返す**（その144 は自分の行を足していたので付け忘れ。**キュー 4(a1) に入れた。5 分**）。

## 次に効く一手

**キューを組み替えた（俯瞰の権限。ゴールは変えていない）。件数は 4 → 4。**

- **1 番「順位が入れ替わる `descent_budget` の水準を挟む」**（旧 1(b) を 1(a) より上に**上げた**）—— **弱い環 1 を塞ぐ唯一の形で、しかも「脆い」10 を量に変える唯一の形。** 旧順は `sigma_ratio` が先だったが、**発火した反証条件の後始末が先。**
- **2 番「`sigma_ratio` を振る」**（旧 1(a)。**腕の値を指定し直した** —— 0.05 と **0.2 ＝ MC-ESO の σ_init**。その145 の 1540 が「MC-ESO の hunt 長」だったのと同じ揃え方にする）。
- **3 番「instance 軸で直接対決の n を増やす」**（内容不変）。**4 番「記述とコードのずれ ＋ 棚卸し」**（内容不変）。

**キューから落とした問い**: **「`NCDE` の 30 を新 suite で振る」は立てない。** その143 の「`NCDE` 側にも監査の非対称が残る」は正しいが、**`r3pso` で実測した補正幅は差の 39% にとどまり、`NCDE` の差 −0.5113 は 4 本中最大**なので **16/16 は動かない見込みが高い。32 run 使って符号が動かない問いはゴールを 1 mm も動かさない。** 代わりに**論文に「`NCDE` は出荷既定で走らせた」と 1 行書く**（キュー 4(a) に含めた）。

**新しい機序の候補を 1 件調べて、今回は見送った**（記録は [research_loop.md](research_loop.md) の「占有されている領域」に追加）。**候補**: その145 の「広さは配置・深さは hunt 長」から出る **精度水準で予算配分を切り替える 2 相の多スタート**（前半は短い降下を多数 ＝ 広さ、後半は着地から長い降下 ＝ 深さ）。
**見送りは先行研究ではなく順序の理由** —— **直接一致は出なかった**（5 件目の隣で、HPO 側を successive halving / Hyperband 系が、最適化側を [adaptive restart](https://link.springer.com/article/10.1007/s10898-020-00937-5) が囲む）**＝ 組み合わせ基準ではまだ死んでいない**が、**勝っている側の監査が開いたまま新しい機序を作るのは、案 (A) で 12 サイクルを失った形そのもの。キュー 1・2 が閉じてから戻ること。**

各問いの測る量・反証条件・始点は [research_loop.md](research_loop.md) の「未解決の問い」に書いた。

**率直に言うと、この 4 サイクルは前回書いた弱い環を上から潰しにいって、1 つ閉じ、1 つで自分が転んだ。閉じたほうが大きい** —— 手法軸 n=4 は 5 配線で成立し、「公表設定に直しても符号は動かない」まで測れている。
**転んだほうは、扱い方を間違えなければ痛手ではない** —— **12500 と 1540 の間に順位の境界がある、という事実は<u>測れば数値になる</u>** ＝ **査読者の最初の一突きに、他の誰も出していない形で答えられる。**
**危ないのはむしろ、9 サイクル連続で腕を 1 本も作っていないこと。測定の論文としては正しい進み方だが、読み (B) のゴールからは 9 サイクルぶん遠ざかっている。判断 (1) が要る理由はそこにある。**
