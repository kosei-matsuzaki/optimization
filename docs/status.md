# 現状（俯瞰）

**この文書は毎回上書きされる。** 追記しない。履歴は [research_loop.md](research_loop.md) のログ、詳細は [acceptance_topology.md](acceptance_topology.md)。

- 更新: 2026-09-17（俯瞰、**その137 まで反映**）。前回は 09-16（その133 まで）。**この 4 サイクルで頭の主張が自分の手で崩れた** —— 比較相手の NMMSO に 1 行の配線欠陥が見つかり、**「niching 手法 2 本が 16 問全敗」は今日は書けない。**
- `research-loop` は `origin/main` より **83 commit / 405 ファイル / +12673 −3127 行**先。最古の未マージは 09-11 12:30 UTC ＝ **6.3 日 ＝ マージ規則（1 日 1 回）を 5.3 日超過。マージが要る。**
- **方針欄の最新は 2026-09-11 で (1)(2)(3)(4) は履行済み** ＝ **未処理の指示は無い。** 09-13 に上げた判断 (1) は**未回答のまま 6 日目**。
- **今回も実験はしていない。MC-ESO の既定は 1 つも変えていない。**（その137 が変えたのは**ベースライン側** `nmmso.py` の 1 行。）

---

## ユーザーの判断を仰ぐこと

**(1)【今回新規・最重要】公開中のレポートが、いま支えの無い主張を書いている。取り下げてよいか。**
**その135-137 で、比較相手 NMMSO の `swarm_size` が公表設定 `10·D` の 1/D 倍（直書き 10）だったと判明した。** CEC2013 でこの 1 行を直すと
**N19 は 0.0917 → 0.3500、N18 は 0.400 → 0.5556**（公表 0.443 / 0.633）＝ **新 suite の NMMSO Score 0.1548（その116）は「一方的に弱くされた NMMSO」の数値で、再測定すれば上がる向きにしか動かない。**
**頭の主張「新 suite D=10 で niching 手法 2 本が 16 問全敗」は、2 本のうち 1 本を失った。[report.html](report.html) は人に配っているページでこの主張を載せていたので、今回「検証中」に書き換えた。**
**元に戻すのはキュー 1 の再測定が済んでからで、戻らない可能性もある。異論があれば書いてください。**

**(2)【今回新規】頭の主張の「手法軸」は n=1 である。外部 niching 手法を 2 本足す許可が要る（2 サイクル）。**
**16/16 は問題軸の n であって手法軸の n ではない。** 新 suite の niching 手法は **NMMSO と MC-ESO の 2 本だけ**で、**MC-ESO は自分の手法、NMMSO は上記のとおり欠陥つきだった
＝ 外部 niching 手法は実質 0 本。査読者はここを最初に突く。`NCDE` と `r3pso` は実装済みで同じ driver から回せる**ので手法軸は 1 → 3 にできるが、**代金がある** ——
**この 2 本も `n_pop=30` / `n_particles=30` を D 非依存で直書きしており**（`r3pso.py:36` のコメントは自ら *"Li used larger swarms"* と書いている）、**NMMSO と同型の欠陥である可能性が高い。
直せば記録済みの BBOB / CEC2013 期の値が「旧既定のもの」になる**（方針欄 2026-09-11 (3)）。**キュー 2 に置いた。止めるなら書いてください。黙認なら進めます。**

**(3) 繰り越し 3 件。** **ゴール文の読み方（6 日目）** —— ゴール文は手法の出自を要求していない。
**読み (A)「出自を問わない」なら検証待ちの勝ちが 1 つ、読み (B)「自作の手法で勝つ」なら勝ちゼロ。推奨は変わらず (A)。**
**論文の型**は測定の論文のまま（**ただし頭の文は判断 (1) で書き直しが要る**）。**採用判断 6 件**の推奨も前回と同じ「いま書き換えない」。

---

## いま論文に書ける主張

**「この研究が作った手法が既存手法より多く見つけた」は依然として書けない。今回それに加えて、頭の文が 1 本足りなくなった。**

**【今回、保留に落ちた】「niching 手法 2 本が 16 問全敗」は今日は書けない** —— NMMSO 側の 0.1548 が旧既定の数値だから。
**MC-ESO 側の 16/16 は崩れていない。** 再測定はキュー 1。

**【堅い】16/16 で崩れない**

1. **MC-ESO は 16 問すべてで「無協調の多スタート」に負ける**（Score **0.1385** 対 `Restart-Lander` **0.6284**、seed 0・PIN01。その116）。
   **ただしこれは自分の手法についての主張**で、「niching 手法は」とは書けない。
2. **素の basin-hopping も 16/16 で無記憶の多スタートに負ける**（MPR 0.2731、**−0.2913、0/0/16、p=3.052e-5**。その133）
   ＝ **強いのは「摂動つき再起動」一般ではなく、記憶を持たない多スタートそのもの。**
3. **この採点器は公表値を再現する**（その127-131）。同一採点器上の RR-CMA-ES は **0.5377 / 0.5439 / 0.5817（3 seed、平均 0.5544）**で、**公表値 0.5730 との差は 3 本とも ±0.05 の内側**（3 seed 平均 **−0.0186** < seed 幅 SD 0.0238）。
4. **順位は正の報告半径の全域（`r/span` 1e-6〜0.5 ＝ 5 桁半）で不変**、**PR 側は半径と無関係**（その130）。**反転は `r=0` の 1 点だけ**（片方にだけ間引きを与えない点）＝ **「任意の正の間引き半径の下で」の限定が要る。**
5. **報告軸は 3 手法とも閉じた**（その115・その116）。合法な規則が **oracle 上限と 16/16 厳密一致**。**機序は F1 の定義**（天井は **2·PR/(1+PR)**）。
6. **MC-ESO が負ける理由の分解**: **5.16 倍 ＝ 被覆 2.80 × 深さ 1.84**、**「蒔いた量」ではない**（その113、16/16）。**「配置の構造」でも説明できない**（その118、ρ=−0.091）。
7. **【今回追加・方法論】外部実装をベースラインに使うときは、実装の既定ではなく論文の実験節と 1 行ずつ照合しなければならない**（その136・その137、実測つき）。
   `pynmmso` の既定 `4+⌊3 ln D⌋` は D=10 でちょうど 10 ＝ 直書き値と一致し、**欠陥を 2 サイクル隠した。旧既定の対公表差は D=3 −0.001 / D=5 −0.121 / D=10 −0.233・−0.351 / D=20 −0.178 ＝ D とともに単調に開く**
   ＝ **低次元での一致は高次元の配線の証拠にならない。** 直すと符号が D で揃わず公表値を挟む（**5 関数とも上昇、関門 4/5 合格。旧既定は 1/5**）。

**【脆い】margin の主張 —— 有意水準を通らない**

8. **`Restart-Lander` が競技次点に勝つ、は 16 問 3 seed で +0.0694 / 11 勝 5 敗 / p=0.07391 / rb=+0.515**（その131）。**MPR 側だけ 3 seed 平均が通る**（+0.0810、p=0.02899）が、**判定指標は Score**（方針欄 2026-09-11）なので通らない側を採る。
9. **`Restart-Lander` が公表最良を上回る（+0.0204）、は報告半径に依存する**（その130）。**公表最良 0.6080 を上回るのは `r ≥ 0.001·span` の 6 点だけ。**

## ゴールとの距離

**ゴール**: 多峰度の高い問題で、既存手法より多くの大域最適を見つける。**読み (B) では 3 つの土俵とも勝ちゼロ。読み (A) では新 suite に検証待ちの勝ちが 1 つ。**

**(i) 新 suite（D=10、16 問、正規予算 50 万、報告規則 `eps_loose+dedup` r=0.05·span）**

| 16 問平均 Score（公式） | 値 | 条件 | 出所 |
|---|---|---|---|
| MC-ESO | **0.1385** | seed 0・PIN01 | その116 |
| NMMSO | **0.1548（無効・再測定待ち）** | seed 0・PIN01、**`swarm_size=10` ＝ 公表設定の 1/D 倍** | その116／欠陥は その136 |
| 素の basin-hopping | **0.3404** | seed 0・PIN01 | その133 |
| **RR-CMA-ES（競技次点）実測** | **0.5544** | 3 seed 平均・PIN01（0.5377 / 0.5439 / 0.5817）| その131 |
| **記憶なし null（`Restart-Lander`）** | **0.6238** | 3 seed 平均・PIN01（seed 0 は 0.6284）| その131 |
| 同（3 インスタンス平均） | **0.6392** | seed 0・PIN01/02/03 | その122 |
| TRDE-LR（**競技優勝**）公表値 | **0.6080** | D=10・15 instance | 競技資料（出典 1 本） |
| RR-CMA-ES（次点）公表値 | **0.5730** | 同上 | 同上 |

**＝ null は公表最良を +0.0204（seed 0・PIN01）／ +0.0312（3 インスタンス平均）上回る。ただし 1 seed・自分で選んだ報告半径 対 15 インスタンス平均・出典 1 本。**
**上積みの見込み**（その119 の反実仮想、腕ではなく上限）: **16 問平均 +0.1134 / 群 A +0.2269。ただし上限と実装可能な腕を分けているのは「中心の情報」**
（上限は `optima_pos` を使う）で、**自己情報だけで中心を作る形は その133 で棄却された。**

**(ii) CEC2013 の配線（今回、照合が進んだ）**: NMMSO は `10·D` で **N15 0.7125 / N17 0.5750 / N18 0.6500 / N19 0.3250 / N20 0.1750**（n=10、公表値 0.668 / 0.538 / 0.633 / 0.443 / 0.178）。
**(iii) 2D/3D（N04-N10）—— headroom 無し。(iv) F14-F20 —— 閉じた。**

## 確定した事実

- **報告軸は空**（その115・その116・その130）。**この軸にはもう 1 サイクルも使わない。**
- **【今回】記録済みの NMMSO の数値はすべて旧既定（`swarm_size=10`）のもの。** 内訳は [acceptance_topology.md](acceptance_topology.md) の その137 の節。
- **【今回】NMMSO が D=10 で低いのは「手法の性質」ではなく 1 行の欠陥だった。** 残る外れは N19 の −0.1180 1 本だけで、**N15 が同じ大きさだけ上に外れている**（t=−2.21 対 +2.33）＝ **「まだ何か弱くしている」の読みは支持されない。**
- **【今回】報告集合の cap は機序ではない**（binding は修正前後で不変。N20 は報告点数 43.4 → 22.1 と半減して PR は 0.000 → 0.1750 に上昇）。
- **【今回】引用候補も競技の問題別値も実在しない**（その134）—— de Nobel+ 2024 の本文 39,607 字に `baseline` が **0 回**、結果資料は D 別 9 行のみで「Results-All Problems」は画像。**弱い環 4 は「無い」で閉じ、5 から引用候補を削除した。**
- **seed のばらつきは手法によって桁が違う**（16 問平均 Score の n=3 SD は RR 0.0238、null 0.0066 ＝ 3.6 倍差）。**部分集合も中立ではない**（その131）。
- **平均回帰は所見に化ける**（その133）＝ **「弱い側ほど伸びる」型の所見は順位づけを独立な標本から取ること。**
- **1 seed のスクリーンは誤導する**（その135。N18 の 1 seed は 0.667 ＝ 公表値超え、15 seed 平均は 0.400）。**スクリーンは所要の見積りにしか使わない。**
- **suite は最適の配置で半分に割れる**（群 A = PID 1-8 が 8/8 で過集中。その118）。**塊の成員は誰も届いていない**（その119）。

## 否定された仮説（再提案しない）

| 仮説 | 結果 |
|---|---|
| PR は evals/K や K で決まる ／ 掘り残しの回収 ／ 天井は best-of-n の性質 ／ F14-F20 に勝てるセル ／ 異方な降下・hill-valley ／ draw 本数・σ0 の掃引 ／ 報告点数・報告規則・archive 容量・cap・trim が差を説明する | 棄却（その17〜その117。全文は [acceptance_topology.md](acceptance_topology.md)） |
| MC-ESO が null に負けるのは hunt 本数が足りないから ／ 最適の配置の構造が差を決めている ／ 塊は飽和している ／ 頭の数字は PIN01 の当たり | **棄却（その113・その118・その119・その122）** |
| 自作の配線か採点器のせいで NMMSO / MC-ESO が低い | **部分的に復活。**採点器は無実（その127・その131）だが、**NMMSO の配線は実際に壊れていた**（その136） |
| **この harness の NMMSO は D=10 で公表値に届かない**（その135）／ **`pynmmso` は公表表を再現しない** | **【今回、撤回・棄却】**その137 が原因（`swarm_size`）を確定させて直し、CEC2013 の関門を 4/5 で通した。**`pynmmso` は無実で、欠陥は wrapper の 1 行** |
| 着地点近傍への再投入は効く ／ 利得は null が弱い問題に集中する ／ 報告半径を変えると順位が変わる | **棄却（その130・その133）** |

## いちばん弱い環

1. **【今回、首位】頭の主張を支える外部 niching 手法が実質ゼロ本になった。16/16 は問題軸の n であって手法軸の n ではない。**
   新 suite の niching 手法は 2 本で、**1 本は自分の手法、1 本は欠陥つきだった。査読者が最初に突くのはここ**（塞ぐ手はキュー 1・2）。
2. **【今回新規】同型の欠陥が他のベースラインにも残っている可能性が高い** —— **`ncde.py:36` の `n_pop=30` と `r3pso.py:36` の `n_particles=30` はどちらも D 非依存の直書きで、
   後者のコメントは自ら *"Li used larger swarms"* と書いている。どちらも公表設定と照合されていない。** 新 suite には載っていないが、**BBOB / CEC2013 期の記録値には入っている。**
3. **【137 サイクル連続】勝ちが「この研究の手法」からは 1 つも出ていない**（判断 (3)）。**この 4 サイクルは腕を 1 本も作っていない**（配線の修復に使った）。
   **新しい機序の候補はいま無い**（構造 ＝ その118、撒き方 ＝ その113 で自分の測定が 2 本閉じており、残りは占有 10 件の外）。
4. **直接対決の有意性が出ていない**（+0.0694、p=0.0739）。**問題軸は尽きた。instance 軸しかない**（キュー 3）。
5. **外部比較の橋** —— **今回、穴 2 つが「無い」で閉じた**（引用候補・競技の問題別値）。**残るのは公表値の表が資料 1 本であること。egress はこの俯瞰の環境から検索は通るようになった**が、**逐語引用には本文取得が要り、そこは CI の `fetch_refs` が唯一の経路のまま。**
6. **新規性は測定の側にしか残っていない。** 現象は **Lobo & Bazargani（初出 2015、arXiv 1504.06859）**と **Cano+ 2022（"Out of the Niche"）**に占有。**今回の追加調査でも新しい占有は出なかった**（EC / niching / 直接探索 / 再現性 の 4 語彙。GECCO'13 companion の multistart 比較は PR を報告しないので一致しない）。
7. **`CLAUDE.md` が pin する SR@1e-10 93.5% / evals 798 がこの環境で 3 回とも再現しない**（92.08% / 677.7）。ゲートは `best_f` バイト一致。**pin の数値は書き換えない。**
8. **片付けは拘束条件から外れたまま。`analysis/` は 363 ファイル / 5.2 MB ＝ 目安 400 まで 37 の余裕、規則違反ゼロ**
   （平置き `README.md` 1 本のみ、300 行超の非 gz CSV ゼロ、`check_doc_links` 67 アンカー破損ゼロ、`check_cost_rows` 穴ゼロ）。
   **docs とコードは今回引いた範囲では一致**（`nmmso.py` の `10·D` と [baselines.md](baselines.md):111,118,124）。**未処理は前回と同じ 2 件でどちらもキュー 5** ——
   **`web/app_lib/results.py:234` の「PR」列が `pr_1e-4`**（4 度目の再確認、未修正）、**`scripts/` 直下 22 本のうち 11 本が呼び手なし。**

## 次に効く一手

**キューを組み替えた（俯瞰の権限。ゴールは変えていない）。件数は 5。** 並べ方は前回と同じ「いちばん堅い結果を守る」順だが、**守る対象が変わった** ——
前回は margin を守る順、今回は**頭の主張そのものが 1 本欠けている**ので、欠けを埋める 2 件を先頭に置いた。

- **まず手順 2 の統合の回が入る**（作業ログ 9 件 ＝ 8 件超。**次の実行サイクルは新しい実験をしない**。その137 の申し送りどおり）。キュー 1 はその次。
- **1 番「直した NMMSO で新 suite を測り直す」**（旧 2 番の残り (c)）—— **頭の主張が戻るか戻らないかを決める唯一の測定。**
- **2 番「外部 niching 手法を 2 本足す」**（新設、判断 (2)）—— **手法軸を 1 → 3 にする。公表設定との照合が先。**
- **3 番「instance 軸で直接対決の n を増やす」**（判断 (3) 待ちだが止める指示は無いので据え置き。**順位は 1 つ下げた**）。
- **4 番「hunt 長か選抜か」**（内容不変）—— **論文の機序節の仕事であってゴールの仕事ではない。キューが詰まったら最初に落とす 1 本。**
- **5 番「記述とコードのずれ ＋ 棚卸し」**（内容不変）。

各問いの測る量・反証条件・始点は [research_loop.md](research_loop.md) の「未解決の問い」に書いた。

**率直に言うと、この 4 サイクルは前進ではなく後退で、しかも後退したほうが正しい。**
**前回の俯瞰は「論文の『堅い』側はほぼ完成している」と書いたが、その頭の文は比較相手を 1/D のスウォームで走らせた結果だった。**
**133 サイクル最重要の資産と呼ばれてきたものが、防衛のつもりのキューを 3 回まわしたら自分の手で崩れた。査読前に見つかった 1 件である。**
**ただし代金は高い** —— **手法軸が n=1 だという露出は前からあり、今回まで誰も（この俯瞰も含めて）弱い環に書いていなかった。**
