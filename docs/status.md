# 現状（俯瞰）

**この文書は毎回上書きされる。** 追記しない。履歴は [research_loop.md](research_loop.md) のログ、詳細は [acceptance_topology.md](acceptance_topology.md)。

- 更新: 2026-09-18（俯瞰、**その141 まで反映**）。前回は 09-17（その137 まで）。**この 4 サイクルで、前回崩れた頭の主張が測り直されて決着した** —— **崩れたのは「全敗」の部分だけで、順位は崩れなかった。**
- `research-loop` は `origin/main` より **96 commit / 424 ファイル / +13363 −3137 行**先。最古の未マージは 09-11 12:30 UTC ＝ **7.3 日 ＝ マージ規則（1 日 1 回）を 6.3 日超過。マージが要る（今回の通知の主題）。**
- **方針欄の最新は 2026-09-11 で (1)(2)(3)(4) は履行済み** ＝ **未処理の指示は無い。**（(3) の「採用判断 6 件を書き切る」は**今回この俯瞰が閉じた。**下記。）
- **今回も実験はしていない。MC-ESO の既定は 1 つも変えていない。**（その141 が触ったのは driver の `_METHODS` 2 行 ＝ ベースライン側の腕の追加のみ。`core/` は 1 行も変わっていない。）

---

## ユーザーの判断を仰ぐこと

**(0)【操作、今回いちばん急ぐ】`research-loop` を `main` にマージしてください。** 96 commit / 7.3 日。
**規則は 1 日 1 回**で、**status.md と report.html は毎日全面書き換えされる 2 本**なので、**溜めるほど人間側のマージが conflict に化けます。** 俯瞰は `main` に commit しません。

**(1)【6 日目 → 7 日目・繰り越し。いちばん重い】ゴール文の読み方。**
ゴール文（「多峰度の高い問題で、既存手法より多くの大域最適を見つける」）は**手法の出自を要求していません。**
**読み (A)「出自を問わない」なら、新 suite に勝ちが 1 つあります**（記憶なし多スタート `Restart-Lander` 0.6238 対 公表最良 0.6080）。
**読み (B)「この研究が作った手法で勝つ」なら、141 サイクルで勝ちはゼロです。** **推奨は変わらず (A)。**
**これが決まらないと論文の頭の文が決まりません。** 一行「(A) でよい」だけで十分です。

**(2)【今回、内容が変わった】論文の型に選択肢が 2 本立ちました。**
**型 (A)（従来）: 測定の論文**（「新 suite D=10 で記憶なしの多スタートが専用手法と競技手法に勝つ」。今回生き残ったが弱くなった ＝ 下記 1）。
**型 (B)（今回立った）: 再現性の論文**（「外部 niching 手法は公表設定と照合しないと一方的に弱くなる」。**実測 2 件** —— NMMSO の `swarm_size` は公表設定の 1/D 倍で新 suite の Score 0.1548 → 0.4144、`r3pso` の母集団 30 は公表帯 300-800 未満で CEC2013 の PR@1e-5 が +0.1233）。
**推奨は (A) を主・(B) を方法論の節に置く。** 理由: **(B) で壊れていたのは公開実装ではなく<u>この研究の wrapper</u>**（`pynmmso` は無実）＝ **「自分の配線が 2 回壊れていた」を頭に置くと、同じ配線で測った (A) の信頼性を自分で削ります。異論があれば書いてください。**

**(3)【今回この俯瞰が閉じた。判断待ちから外します】採用判断 6 件（`c=1.0` / `_sig10` / `_fl08` / `soltrim_rho` / `commit_place` / 合成腕）は全件 <u>不採用</u>。**
方針欄 2026-09-11 (3) の「このサイクルで書き切り、判断待ちから消すこと。先送りは不可」が **7 サイクル守られていませんでした。**
**根拠**: 案 (C) の下で MC-ESO はベースライン側 ＝ 既定を変えることは比較手法を変えること。**6 件はいずれも判定水準で帰無**（`commit_place` は 30 seed の両水準で完全帰無、`best_f` は全腕全 seed で base と一致 ＝ [acceptance_topology.md](acceptance_topology.md):2184-2246）＝ **得るものが無く、代金だけが残ります。復活させたい場合のみ書いてください。**

---

## いま論文に書ける主張

**「この研究が作った手法が既存手法より多く見つけた」は 141 サイクル目も書けない。** そのうえで、前回保留に落ちた頭の文が**弱い形で戻った。**

**【今回、保留から戻った。ただし文面が変わった】**
1. **「新 suite D=10 で niching 手法が記憶なし多スタートに負ける」は書ける。「16 問全敗」は書けない。**
   **書ける形**: **MC-ESO は 16/16 で負ける。直した NMMSO は 12/16 で負ける**（16 問平均 Score 差 **+0.2140、12 勝 0 分 4 敗、p=0.004181、rb=+0.779**。その139）。
   **撤回された 4 問は M01 / M02 / M04 / M10**（5 seed 平均差 +0.0648 / +0.0754 / +0.0173 / +0.0858、その140）。
   **ただし 5 本の Score の範囲が分離しているのは M10 だけ**で、**残り 3 問は「勝つ」ではなく「並ぶ」としか書けない**（M04 は範囲がほぼ同一）。

**【堅い】**

2. **素の basin-hopping も 16/16 で無記憶の多スタートに負ける**（MPR 0.2731、**−0.2913、0/0/16、p=3.052e-5**。その133）
   ＝ **強いのは「摂動つき再起動」一般ではなく、記憶を持たない多スタートそのもの。**
3. **この採点器は公表値を再現する**（その127-131）。同一採点器上の RR-CMA-ES は **0.5377 / 0.5439 / 0.5817（3 seed 平均 0.5544）**で、**公表値 0.5730 との差は 3 本とも ±0.05 の内側**。
4. **順位は正の報告半径の全域（`r/span` 1e-6〜0.5 ＝ 5 桁半）で不変**、**PR 側は半径と無関係**（その130）。**反転は `r=0` の 1 点だけ** ＝ **「任意の正の間引き半径の下で」の限定が要る。**
5. **報告軸は 3 手法とも閉じた**（その115・その116）。合法な規則が **oracle 上限と 16/16 厳密一致**。**機序は F1 の定義**（天井は **2·PR/(1+PR)**）。
6. **MC-ESO が負ける理由の分解**: **5.16 倍 ＝ 被覆 2.80 × 深さ 1.84**、**「蒔いた量」ではない**（その113、16/16）。**「配置の構造」でも説明できない**（その118、ρ=−0.091）。
7. **【方法論・今回 2 件目の実測が付いた】外部実装は、実装の既定ではなく論文の実験節と 1 行ずつ照合しなければならない。**
   **NMMSO**: `pynmmso` の既定 `4+⌊3 ln D⌋` が D=10 でちょうど 10 ＝ 直書き値と一致し**欠陥を 2 サイクル隠した。旧既定の対公表差は D=3 −0.001 / D=5 −0.121 / D=10 −0.233・−0.351 / D=20 −0.178 ＝ D とともに単調に開く**（その136・その137）。
   **`r3pso`**: **Li 2010 §VI-B は D=8〜20 で "a population size from 300 to 800"**（≈ `40·D`）に対し**直書きは 30。300 にすると CEC2013 5 関数 10 seed の PR@1e-5 が +0.1233（p=1.757e-7、38/1/11、rb=0.928）、5 関数すべてで正**（その141）。
   ＝ **低次元での一致は高次元の配線の証拠にならない、が 2 例で言える。**
8. **【今回追加】母集団を増やすのは niching 手法で一様に良くはない ——`NCDE` では被覆と深さのトレードオフ。**
   主水準は帰無（+0.0150、p=0.348）だが**内訳は打ち消し**（N15 +0.1000 / N17 +0.0875 が有意、**N20 は −0.1250、0/9/1、p=0.0039**）。**N20 の落ち方は深さだけ**（1e-3 までは base と同値、1e-5 で 0.0000）＝ **400,000 ÷ 300 ≈ 1,333 世代では 20 次元を 1e-5 まで詰め切れない。`r3pso` には出ない**（pbest が niche の記憶）。

**【脆い】margin の主張 —— 有意水準を通らない**

9. **`Restart-Lander` が競技次点に勝つ、は 16 問 3 seed で +0.0694 / 11 勝 5 敗 / p=0.07391 / rb=+0.515**（その131）。**MPR 側だけ 3 seed 平均が通る**（+0.0810、p=0.02899）が、**判定指標は Score**（方針欄 2026-09-11）なので通らない側を採る。
10. **`Restart-Lander` が公表最良を上回る（+0.0204）、は報告半径に依存する**（その130）。**公表最良 0.6080 を上回るのは `r ≥ 0.001·span` の 6 点だけ。**

## ゴールとの距離

**ゴール**: 多峰度の高い問題で、既存手法より多くの大域最適を見つける。**読み (B) では 3 つの土俵とも勝ちゼロ。読み (A) では新 suite に検証待ちの勝ちが 1 つ。**

**(i) 新 suite（D=10、16 問、正規予算 50 万、報告規則 `eps_loose+dedup` r=0.05·span）**

| 16 問平均 Score（公式） | 値 | 条件 | 出所 |
|---|---|---|---|
| MC-ESO | **0.1385** | seed 0・PIN01 | その116 |
| **NMMSO（公表設定 `10·D` に修正後）** | **0.4144** | seed 0・PIN01 | **その139** |
| 同（旧既定 `swarm_size=10`、**無効**） | 0.1548 | 参考。**+0.2596、15/0/1、p=9.155e-5** | その116／その139 |
| 素の basin-hopping | **0.3404** | seed 0・PIN01 | その133 |
| **RR-CMA-ES（競技次点）実測** | **0.5544** | 3 seed 平均・PIN01 | その131 |
| **記憶なし null（`Restart-Lander`）** | **0.6238** | 3 seed 平均・PIN01（seed 0 は 0.6284）| その131 |
| 同（3 インスタンス平均） | **0.6392** | seed 0・PIN01/02/03 | その122 |
| TRDE-LR（**競技優勝**）公表値 | **0.6080** | D=10・15 instance | 競技資料（出典 1 本） |
| RR-CMA-ES（次点）公表値 | **0.5730** | 同上 | 同上 |
| `NCDE` / `r3pso`(30) / `r3pso`(≈400) | **未測定** | キュー 2(b) が測る | — |

**＝ null は公表最良を +0.0204（seed 0・PIN01）／ +0.0312（3 インスタンス平均）上回る。ただし 1 seed・自分で選んだ報告半径 対 15 インスタンス平均・出典 1 本。**
**上積みの見込み**（その119、腕ではなく上限）: **16 問平均 +0.1134 / 群 A +0.2269。ただし上限と実装可能な腕を分けているのは「中心の情報」**で、**自己情報だけで中心を作る形は その133 で棄却された。**

**(ii) CEC2013 の配線**: NMMSO は `10·D` で **N15 0.7125 / N17 0.5750 / N18 0.6500 / N19 0.3250 / N20 0.1750**（n=10、公表値 0.668 / 0.538 / 0.633 / 0.443 / 0.178、4/5 合格）。**`NCDE` / `r3pso` には照合先が存在しない**（両手法とも Fieldsend 2014 Table V の 15 手法に無く、原論文は 2012 / 2010 で suite より前。その141）。
**(iii) 2D/3D（N04-N10）—— headroom 無し。(iv) F14-F20 —— 閉じた。**

## 確定した事実

- **報告軸は空**（その115・その116・その130）。**この軸にはもう 1 サイクルも使わない。**
- **記録済みの NMMSO の数値はすべて旧既定（`swarm_size=10`）のもの。**新 suite については その139 が測り直し済み、**CEC2013 は 5 関数だけ測り直し済みで残りは旧既定のまま。**
- **【今回】記録済みの `r3pso` の数値はすべて母集団 30 ＝ 公表帯の下端未満のもので、CEC2013 の難しい 5 関数では実測で −0.1233 ぶん低く出ている。**
  **既定は変えない**（方針欄 2026-09-11 (3) の代金に見合わない）が、**[baselines.md](baselines.md) にこの但し書きを入れる必要がある**（キュー 4(a) に入れた）。
- **【今回】`NCDE` の 30 は主水準では欠陥ではない**（+0.0150、p=0.348）。**出荷既定のままキュー 2(b) を回してよい。**
- **NMMSO が D=10 で低いのは「手法の性質」ではなく 1 行の欠陥だった**（その136・その137）。**`pynmmso` は無実。**
- **引用候補も競技の問題別値も実在しない**（その134）。**GECCO 2025 の競技結果も 09-18 の再検索でまだ公開が見つからない**（前回 09-09）＝ **公表値の出典 1 本という限界は 9 日間動いていない。**
- **【今回・新情報】同じ suite 系列は `MMO@CEC2026` として継続し、そこでは変数間 linkage を加えた<u>更新版</u>が使われる**（＝ この研究の土俵は引退していないが、**すでに次版がある**）。出典は検索結果のみ（配布ページは egress ブロックで本文未取得）。
- **seed のばらつきは手法によって桁が違う**（16 問平均 Score の n=3 SD は RR 0.0238、null 0.0066）。**部分集合も中立ではない**（その131）。
- **平均回帰は所見に化ける**（その133）。**1 seed のスクリーンは誤導する**（その135）。**1 run の値で符号を主張できない**（その139・その140 —— M02 の k2 を 1 本落とすと平均差が半減する）。
- **suite は最適の配置で半分に割れる**（群 A = PID 1-8 が 8/8 で過集中。その118）。**塊の成員は誰も届いていない**（その119）。

## 否定された仮説（再提案しない）

| 仮説 | 結果 |
|---|---|
| PR は evals/K や K で決まる ／ 掘り残しの回収 ／ 天井は best-of-n の性質 ／ F14-F20 に勝てるセル ／ 異方な降下・hill-valley ／ draw 本数・σ0 の掃引 ／ 報告点数・報告規則・archive 容量・cap・trim が差を説明する | 棄却（その17〜その117。全文は [acceptance_topology.md](acceptance_topology.md)） |
| MC-ESO が null に負けるのは hunt 本数が足りないから ／ 最適の配置の構造が差を決めている ／ 塊は飽和している ／ 頭の数字は PIN01 の当たり | **棄却（その113・その118・その119・その122）** |
| 自作の配線か採点器のせいで NMMSO / MC-ESO が低い ／ `pynmmso` は公表表を再現しない | **配線は成立していた（その136）、採点器と `pynmmso` は無実（その127・その131・その137）** |
| **新 suite D=10 で niching 手法 2 本が 16 問<u>全敗</u>する** | **【今回、部分的に棄却】**NMMSO は 4 問で上回る（その139・その140）。**16 問平均の負けは残る**（+0.2140、p=0.004181） |
| **`NCDE` / `r3pso` の直書き 30 は NMMSO と同型の欠陥である** | **【今回】`r3pso` は成立（+0.1233、p=1.8e-7）、`NCDE` は主水準で不成立（p=0.348）** |
| 着地点近傍への再投入は効く ／ 利得は null が弱い問題に集中する ／ 報告半径を変えると順位が変わる | **棄却（その130・その133）** |

## いちばん弱い環

1. **【首位・不変】頭の主張の手法軸がまだ n=2 である**（MC-ESO ＋ 直した NMMSO）。**16/16 は問題軸の n。査読者が最初に突くのはここ。** 塞ぐ手は**キュー 1（= 旧 2(b)）で、次のサイクルがそれを回す。**
2. **【今回新規】監査が片側にしかかかっていない。** ベースラインは公表設定で走り、**null は<u>この研究が選んだ</u>設定で走っている** ——
   **NMMSO と `r3pso` は論文と照合したのに、`Restart-Lander` の `descent_budget=12500` と `sigma_ratio=0.1` はこの研究が置いた値で、この suite の上で一度も振っていない**（`restart_lander.py:56`）。
   **勝っている側の 2 つのつまみが未検証。** **キュー 2 を「機序の問い」から「頭の結果の頑健性の問い」に書き換えた**（同じ 16 run で両方測れる）。
3. **【141 サイクル連続】勝ちが「この研究の手法」からは 1 つも出ていない**（判断 (1)）。**この 7 サイクルは腕を 1 本も作っていない**（配線の修復と監査に使った）。
   **新しい機序の候補はいま無い**（構造 ＝ その118、撒き方 ＝ その113 で自分の測定が 2 本閉じており、残りは占有 10 件の外）。
4. **直接対決の有意性が出ていない**（+0.0694、p=0.0739）。**問題軸は尽きた。instance 軸しかない**（キュー 3）。
5. **外部比較の橋** —— **公表値の表が資料 1 本のまま、9 日間動いていない。** **本文取得は CI の `fetch_refs` が唯一の経路**（この俯瞰の環境からは `sites.google.com` も `arxiv` PDF もブロックされる）。
6. **新規性は測定の側にしか残っていない。** 現象は **Lobo & Bazargani（2015）**と **Cano+ 2022** に占有。
   **【今回の追加調査】隣接分野（NAS）に主張の<u>型</u>の先例がある** —— *"Local Search is a Remarkably Strong Baseline for NAS"*（PPSN 2020）。**占有ではない**（別分野・別指標・niching が相手ではない）が、**「素朴なベースラインが専用手法に勝つ」という驚き自体は他分野で使用済み。** 論文ではこれを引いて差分を書くこと。
7. **`CLAUDE.md` が pin する SR@1e-10 93.5% / evals 798 がこの環境で 3 回とも再現しない**（92.08% / 677.7）。ゲートは `best_f` バイト一致。**pin の数値は書き換えない。**
8. **片付けの余裕がほぼ無い。`analysis/` は 382 ファイル / 5.7 MB ＝ 目安 400 まで 18。規則違反はゼロ**（平置きは `analysis/README.md` 1 本のみ、300 行超の非 gz CSV ゼロ）。
   **キュー 1 は 48 run を出すので、先に閉じた路線を 1 本消す必要がある**（キュー 1 の本文に拘束として書いた）。
   **未処理の記述ずれは 2 件で前回と同じ** —— **`web/app_lib/results.py:234` の「PR」列が `pr_1e-4`（5 度目の再確認、未修正）**、**`scripts/` 直下 22 本のうち 11 本が呼び手なし。**

## 次に効く一手

**キューを組み替えた（俯瞰の権限。ゴールは変えていない）。件数は 5 → 4。**
**落としたのは旧 5 番の (e)（`presentation/` の棚卸し）** —— 方針欄が「触らない」と決めており、棚卸しだけでゴールは 1 mm も動かない。内容は 4 番に 1 行で残した。

- **1 番「外部 niching 手法 2 本を新 suite で回す」**（旧 2(b)）—— **手法軸を n=2 → n=4 に。弱い環 1 を塞ぐ唯一の手。**
- **2 番「null 自身の設定を振り、頭の結果が自分のつまみに依存しないことを示す」**（旧 4 番を書き換えて 1 つ上げた）—— **旧 4 番は「落とす候補」と書いてあったが、同じ 16 run が弱い環 2 の答えにもなる。**
- **3 番「instance 軸で直接対決の n を増やす」**（内容不変、1 つ下げた）。**4 番「記述とコードのずれ ＋ 棚卸し」**（`r3pso` の但し書きを先頭に足した）。

各問いの測る量・反証条件・始点は [research_loop.md](research_loop.md) の「未解決の問い」に書いた。

**率直に言うと、この 4 サイクルは前回の後退をきれいに回収した。** 前回「頭の主張が自分の手で崩れた」と書いたが、**測り直したら崩れたのは「全敗」という強調語だけで、順位（p=0.004181）は崩れなかった。**
**だが安心する場所ではない。** **同じ監査を 2 本目のベースラインにかけたら 2 本目も外れていた**（`r3pso`、+0.1233）。
**3 本中 2 本が公表設定から外れていたのに、勝っている側の設定は誰も監査していない。** **次に自分の手で崩れるとしたらそこである。**
