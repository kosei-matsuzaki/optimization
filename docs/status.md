# 現状（俯瞰）

**この文書は毎回上書きされる。** 追記しない。履歴は [research_loop.md](research_loop.md) のログ、詳細は [acceptance_topology.md](acceptance_topology.md)。

- 更新: 2026-09-20（俯瞰、**その149 まで反映**）。前回は 09-19（その145 まで）。**この 4 サイクルで、前回いちばん弱いと書いた環（勝っている側の監査）が<u>両方のつまみで閉じ</u>、3 番目に書いた環（直接対決の有意性）は<u>増やす道そのものが塞がった</u>。**
- `research-loop` は `origin/main` より **124 commit / 389 ファイル / +16167 −3141 行**先。最古の未マージは 09-11 12:30 UTC ＝ **9.3 日 ＝ マージ規則（1 日 1 回）を 8.3 日超過。**
- **方針欄の最新は 2026-09-11 で (1)(2)(3)(4) は履行済み ＝ 未処理の指示は無い。**
- **今回も実験はしていない。`core/` は 1 行も触っていない。MC-ESO の既定は 1 つも変えていない。**

---

## ユーザーの判断を仰ぐこと

**(0)【操作、いちばん急ぐ。前回から 16 commit 増えた】`research-loop` を `main` にマージしてください。** 124 commit / 9.3 日。
**status.md と report.html は毎日全面書き換えされる 2 本**なので、**溜めるほど人間側のマージが conflict に化けます。** 俯瞰は `main` に commit しません。

**(1)【8 日目 → 9 日目・繰り越し。いちばん重い】ゴール文の読み方。**
**読み (A)「出自を問わない」なら、新 suite D=10 に勝ちが 1 つあります**（記憶なし多スタート `Restart-Lander` 0.6284 対 公表最良 0.6080）。
**読み (B)「この研究が作った手法で勝つ」なら、149 サイクルで勝ちはゼロです。** **推奨は変わらず (A)。** 一行「(A) でよい」で十分です。
**今回 1 つ足します** —— **その勝ちは D=10 の 1 次元でしか測っていません**（下記「弱い環」1）。**(A) を採るなら、次の 2 サイクルは次元方向に使います**（キュー 1・2）。**これは俯瞰の権限内なので、止める場合だけ書いてください。**

**(2)【文面は不変。推奨も不変】論文の型は (A) 主・(B) 方法論節。**
**今回、型 (A) は<u>強くなりました</u>** —— 前回「いちばん弱い」と書いた設定依存が、**量として閉じた**（下記「書ける主張」3）。
**弱くなった点は 1 つだけ** —— **有意性を稼ぐ唯一の道だと思われていた instance 軸が、疑似反復だと実測された**（その149）。

---

## いま論文に書ける主張

**「この研究が作った手法が既存手法より多く見つけた」は 149 サイクル目も書けない。** そのうえで、**今回はじめて「自分の設定を自分で監査した」という形が量になった。**

**【今回いちばん効いた。査読者の第一撃への答え】**
1. **勝っている側の 2 つのつまみは両方とも振り終わり、符号はどちらでも動かない。**
   **`descent_budget`（5 水準）**: 1540 → 0.3284 / 2200 → 0.4713 / 3000 → 0.5633 / 6000 → 0.6141 / 12500 → 0.6284（その145・146・148）。
   **＝ null が直した NMMSO（0.4144）を抜くのは正規予算 50 万の 1/325〜1/227 以上で、既定はその下限の 8.1 倍上。**
   **上半分は 1 枚の台地**（12500−6000 は +0.0143・p=0.2188 で、**16 問中 9 問は Score が完全に同値**。その146）。
   **「崖」という語は落とした** —— 5 点で見ると 0.4144 を跨ぐ位置に不連続は無く、隣接差は +0.1430 / +0.0920 / +0.0508 / +0.0143 と単調に縮む飽和曲線（その148）。
   **`sigma_ratio`（3 水準）**: 0.05 → 0.6004 / 0.1 → 0.6284 / 0.2（＝ MC-ESO の σ_init）→ 0.5857。**差はどちらも有意でない**（p=0.1294 / p=0.08032）。**4 倍振っても 3 水準とも NMMSO の 0.17 以上上**（その147）。
   **＝ 「12500 は点数を見て選んだ当たりではないか」に、境界の位置と台地の平らさで答えられる。**

**【強化された。5 水準で全域が見えた】**
2. **記憶なし多スタートが強いのは、広さと深さを<u>別々に</u>買えるからで、坂を作っているのは深さだけ。**
   **広さ（1e-1 被覆）は 8.1 倍の予算差に 9.0% しか動かず（0.5531 → 0.6031）、単調ですらない**（最大は 6000 の 0.6125）。
   **深さ保持率（@1e-5 ÷ @1e-1）は 0.186 → 0.377 → 0.661 → 0.801 → 0.850 と 4.6 倍・完全に単調。降下本数は 323 → 95 の 3.4 分の 1。**（その148）
   **1 語だけ訂正が入った** —— **広さを作るのは「配置」だけでなく「配置と σ」**（σ を 2 倍にすると 1e-1 被覆が −0.0656 で有意に落ちる。p=0.03711。**hunt 長を 8.1 分の 1 に切るより高くつく** ＝ −0.0656 対 −0.0500。その147）。
   **競合は 3 本とも別々の側を欠く**: MC-ESO は配置、`r3pso` は被覆、hunt を切った null は深さ。

**【堅い。前回から不変】**
3. **新 suite D=10 で niching 手法が記憶なし多スタートに負けるのは 4 手法 5 配線で成立**（MC-ESO 0.1385 / `NCDE` 0.1171 / `r3pso` 0.1468 が 16/16、公表帯 `r3pso-p400` 0.3341 も 16/0/0。例外は直した NMMSO の 12/16）。
4. **素の basin-hopping も 16/16 で負ける**（その133）＝ 強いのは「摂動つき再起動」一般ではなく記憶を持たない多スタートそのもの。
5. **採点器は公表値を再現する。今回 2 本目のインスタンスでも成立** —— RR-CMA-ES の PIN02 は **0.5940** 対 公表 0.5730（+0.0210。PIN01 は −0.0353 で符号は反転するが両方とも ±0.05 の内側。その149）。**順位は正の報告半径の全域（5 桁半）で不変、報告軸は 3 手法とも閉じた。**
6. **【方法論。実測 3 件】外部実装は実装の既定ではなく論文の実験節と 1 行ずつ照合する**（NMMSO +0.2596 / `r3pso` +0.1873 / `NCDE` は主水準で帰無）。**欠陥は実在して大きいのに判定を 1 問も動かさない**（補正は差の 39% しか埋めない。その143）。

**【脆い】**
7. **`Restart-Lander` が競技次点に勝つ、は有意でない。採る数字は <u>問題単位の p=0.0833</u>**（+0.0654、10 勝 1 分 5 敗、rb=+0.517。その149）。
   **同じ回は `(問題, インスタンス)` n=32 で p=0.02027 も出したが、これは採らない** —— **同じ回が 2 インスタンスの差の相関 r=+0.4899（符号一致 13/16）を実測しており、n=32 は疑似反復だから。**
   **この選択は俯瞰の判断**（実行役が上げた未決事項への回答）。**有意でない側を採る。**
8. **公表最良を上回る（+0.0204）は報告半径に依存する**（`r ≥ 0.001·span` の 6 点のみ）。**PIN02 でも同じ向きで +0.0261**（その149）。
9. **【今回いちばん重い。未測定】頭の Score は D=10 でしか測っていない**（下記「弱い環」1）。

## ゴールとの距離

**ゴール**: 多峰度の高い問題で、既存手法より多くの大域最適を見つける。**読み (B) では勝ちゼロ。読み (A) では新 suite D=10 に検証待ちの勝ちが 1 つ。**

**(i) 新 suite（D=10、16 問、正規予算 50 万、報告規則 `eps_loose+dedup` r=0.05·span、PIN01・seed 0）**

| 16 問平均 Score（公式） | 値 | 出所 |
|---|---|---|
| `NCDE` 0.1171 ／ MC-ESO 0.1385 ／ `r3pso` 0.1468（16/16 で負け） | **0.117-0.147** | その116・その142 |
| `RL-1540` 0.3284 ／ `r3pso-p400` 0.3341 ／ 素の basin-hopping 0.3404 | **0.328-0.340** | その133・143・145 |
| **NMMSO（公表設定 `10·D` に修正後）** | **0.4144** | その139 |
| `RL-2200` 0.4713 ／ `-3000` 0.5633 ／ `-6000` 0.6141（降下予算の坂） | **0.471-0.614** | その146・その148 |
| **RR-CMA-ES（競技次点）実測 0.5544** ／ **null（`Restart-Lander`）0.6284** | PIN02 は 0.5940 / 0.6341 | その131・その149 |
| TRDE-LR（競技優勝）公表値 **0.6080** ／ RR-CMA-ES 公表値 **0.5730** | — | 競技資料（**出典 1 本**） |

**(ii) 他の次元は 1 本も測っていない。** **公表最良は 4 次元ぶん存在する** —— MPR で D=2 0.984 / D=5 0.844 / D=10 0.651 / D=20 0.476、
**Score に直すと（この俯瞰が資料の MPR と mean-F1 から算出。D=10 の 0.573 / 0.608 が記録値と一致することで検算済み）D=5 0.7145 / D=20 0.4445。**
**(iii) CEC2013 の配線**: NMMSO は 5 関数中 4 本が公表値の ±0.10 内。`NCDE` / `r3pso` には照合先が存在しない。
**(iv) 2D/3D（N04-N10）—— headroom 無し。(v) F14-F20 —— 閉じた。**

## 確定した事実

- **勝っている側のつまみは尽きた**（`descent_budget` 5 水準・`sigma_ratio` 3 水準）。**この軸にはもう 1 サイクルも使わない。**
- **報告軸は空**（その115・116・130・145）。**MC-ESO の広さの不足は hunt の本数でも長さでもなく置き場所。**
- **instance を増やしても独立な標本は増えない** —— **2 インスタンスの差は r=+0.49 で相関**（その149）。**問題は 16 問で suite の全部なので、問題単位の n=16 は固定端。**
- **記録済みの外部手法の値には配線の但し書きが要る**（`r3pso` は母集団 30、NMMSO は新 suite と CEC2013 5 関数のみ修正済み、`NCDE` は出荷既定 30）。**但し書きはキュー 4 が入れる。**
- **null の 2 つのつまみはこの研究が置いた値**（`descent_budget=12500` ＝ 予算 ÷ 40、`sigma_ratio=0.1` ＝ その103 の値）。**両方とも監査済みになった。**
- **seed / インスタンスのばらつきは手法によって桁が違う**（n=3 SD は RR 0.0238、null 0.0066）。**1 run の値で符号は主張しない。**
- **GECCO 2025 の競技結果は 09-20 の再検索でも見つからない**（4 度目）＝ **公表値の出典 1 本という限界は 11 日間動いていない。**

## 否定された仮説（再提案しない）

| 仮説 | 結果 |
|---|---|
| PR は evals/K や K で決まる ／ 掘り残しの回収 ／ F14-F20 に勝てるセル ／ 報告点数・報告規則・cap・trim が差を説明する ／ 最適の配置の構造が差を決めている ／ 着地点近傍への再投入は効く | 棄却（その17〜その133。全文は [acceptance_topology.md](acceptance_topology.md)） |
| 公表設定に直せば外部 niching 手法は null に届く | 棄却（その143。補正は差の 39%） |
| MC-ESO の広さの不足は hunt 長が短いことによる | 棄却（その145・その148。広さは 8.1 倍の予算差に 9.0% しか動かない） |
| **12500 はつまみの当たりで、null の優位は設定依存である** | **【今回】棄却 —— 3000 以上は 1 枚の台地（9/16 問が完全同値）、σ は 4 倍振っても有意差なし**（その146・147・148） |
| **instance 軸を増やせば直接対決の n が増える** | **【今回】棄却 —— 対は 2 倍になったが独立な標本は 16 問のまま（r=+0.49）。差はむしろ +0.0907 → +0.0654 に縮んだ**（その149） |

## いちばん弱い環

1. **【今回、首位が入れ替わった】頭の主張は D=10 の 1 次元でしか測っていない。** ゴール文は「多峰度の高い問題」と言い、分野が未解決として挙げているのは**次元と多峰度のスケーラビリティ**である。**この研究は 16 問 × 1 次元で「勝った」と書こうとしている。**
   **しかも自分の記録が「D=20 では負ける」と予測している** —— **その93 のクラス上限は D=20 で 16 問平均 MPR 0.3149〜0.3729 対 公表最良 0.476。**
   **D=10 でこの screen は当たっている**（改訂後の上限 0.5807 に対し実測 0.5644 ＝ 97.2%）ので、**同じ比で外挿すると D=20 の実測 MPR は 0.39 前後 ＝ 0.476 に届かない。**
   **ただし Score では読みが割れる**（null は F1 側が強く、Score/MPR ＝ 1.113。外挿 0.433 対 公表最良 0.4445）＝ **どちらに転ぶか予測できない ＝ 測る価値がある。キュー 1・2。**
2. **【149 サイクル連続】勝ちが「この研究の手法」からは 1 つも出ていない。** **この 13 サイクルは腕を 1 本も作っていない。**
   **新しい機序の候補は今回も見送った** —— **2 相配分（前半は短い降下を多数、後半は長い降下）は<u>今回の調査で占有が確定した</u>**（下記 5）。
3. **直接対決の有意性は出ておらず、増やす道も塞がった**（p=0.0833）。**問題は 16 問で suite の全部、インスタンスは疑似反復。**
   **算術で言うと**: 相関 r=0.49 なら 2 → 3 インスタンスで差の SD は **6% しか縮まない**（(1+2r)/3 ÷ (1+r)/2 の平方根 = 0.94）。**p=0.0833 は 0.05 を跨がない。**
   **残っている道は「標本を増やす」ではなく「別の次元で同じ符号を出す」** ＝ キュー 1・2（D=20 の 16 問は D=10 の 16 問より相関が低い見込み）。
4. **外部比較の橋** —— **公表値の表が資料 1 本のまま、11 日間動いていない。** 本文取得は CI の `fetch_refs` が唯一の経路。
5. **新規性は測定の側にしか残っていない。今回 1 件増えた（11 件目の占有）** —— **「前半に安い探索で niche を見つけ、後半に選んだ点だけ深く詰める」は "two-stage niching" という<u>名前のついた手法クラス</u>**で、**MLSL（global phase / local phase）と、archive の解を 2 段の局所探索で磨く先行例**がある。**＝ 2 相配分は腕としては撃てるが、機序としては主張できない。**
6. **`CLAUDE.md` が pin する SR@1e-10 93.5% / evals 798 がこの環境で 3 回とも再現しない**（92.08% / 677.7）。ゲートは `best_f` バイト一致。**pin の数値は書き換えない。**
7. **片付けは規則違反ゼロ、余裕 53** —— `analysis/` は **347 ファイル / 2.82 MB**（平置きゼロ、300 行超の非 gz CSV ゼロ、1 MB 超の塊ゼロ、`check_cost_rows.py` / `check_doc_links.py` とも通過）。
   **ただし 4 サイクル連続で削除ゼロ・追加だけ**（309 → 347）。**次の実行サイクルはログ 9 件 > 8 件なので統合の回**（手順 2）＝ **掃除が本体になる。**
   **未処理の記述ずれは 2 件** —— **`web/app_lib/results.py:234` の「PR」列が `pr_1e-4`（7 度目の再確認、未修正）**、**`scripts/` 直下 22 本のうち 11 本が呼び手なし。**

## 次に効く一手

**キューを組み替えた（俯瞰の権限。ゴールは変えていない）。件数は 2 → 4。**

- **1・2 番「次元方向に 1 段伸ばす」**（**新設・最優先**）—— **弱い環 1 と 3 に同時に効く唯一の問い。** 1 番は `Restart-Lander` だけを D=20 で 16 本回して公表値の D=20 行と突き合わせる**安いスクリーン**、2 番はその上で RR-CMA-ES を同条件で回す**直接対決**。
  **どちらに転んでも書く文が決まる** —— 勝てば「主張は 2 次元ぶんに広がり、しかも相関の低い 16 問が足される」、負ければ **「この優位は D ≦ 10 の性質である」という<u>範囲つきの主張</u>**になり、**同時に その93 の screen が当たったことの検証にもなる**（screen が外れたら案 (C) の根拠側に跳ね返る）。
- **3 番「D=5 も測る」** —— 上下から挟んで「どこまで成立するか」を書けるようにする。**予算は D=10 の半分（25 万）なので 2 手法 32 本が 1 サイクルに入る見込み。**
- **4 番「記述とコードのずれ ＋ 棚卸し」**（旧 2 番、内容ほぼ不変。**統合の回に混ぜる**）。

**キューから落とした問い**: **「PIN03 を足して問題単位の検定を取り直す」は消した。** 実行役が その149 の結末として書いたものだが、**その149 自身のデータがこの問いを否定している** ——
**(a) 増えるのは対の本数だけで独立な標本ではない（r=+0.49）、(b) 2 → 3 インスタンスの分散低減は SD で 6%、
(c) 実測として 1 → 2 インスタンスでは p が 0.0739 → 0.0833 と<u>悪化</u>し、差の推定も +0.0907 → +0.0654 に縮んだ。**
**＝ 32 run を使って p=0.0833 を 0.05 の向こうに運べる見込みが無い。** 有意性は**次元方向**（キュー 1・2）で取りにいく。

各問いの測る量・反証条件・始点は [research_loop.md](research_loop.md) の「未解決の問い」に書いた。

**率直に言うと、この 4 サイクルは前回いちばん弱いと書いた環を自分の手で閉じた。** 12500 は台地の内側、σ は 4 倍振っても動かない —— **「baseline を自分に都合よく調律したのでは」という第一撃に、数字で答えられる状態になった。**
**同時に、有意性を稼ぐ最後の道（instance 軸）が疑似反復だと分かり、この土俵での標本は 16 問で打ち止めだと確定した。**
**そして今回いちばん大きいのは、閉じた環の跡に出てきた問いのほうだ** —— **149 サイクル、16 問、1 次元。** ゴール文が言う「多峰度の高い問題」に対して、**次元を 1 段も動かしていないことに 149 サイクル気づいていなかった。**
**しかも自分の記録（その93）は D=20 では届かないと予測している。** **これは危険な問いだが、査読者が最初に開く問いでもある。次の 2 サイクルをそこに使う。**
