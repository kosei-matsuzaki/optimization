# 現状（俯瞰）

**この文書は毎回上書きされる。** 追記しない。履歴は [research_loop.md](research_loop.md) のログ、詳細は [acceptance_topology.md](acceptance_topology.md)。

- 更新: 2026-09-21（俯瞰、**その153 まで反映**）。前回は 09-20（その149 まで）。**この 4 サイクルで次元軸が 3 点そろい、前回「いちばん弱い」と書いた環は閉じた。だが<u>閉じ方は悪いほう</u>で、代わりに<u>より深い場所</u>に新しい首位の弱点が出た** —— **この harness と公表値は、同じ手法の内訳を 0.09〜0.20 食い違って測っている。**
- `research-loop` は `origin/main` より **138 commit / 393 ファイル / +16561 −3142 行**先。最古の未マージは 09-11 12:30 UTC ＝ **10.3 日 ＝ マージ規則（1 日 1 回）を 9.3 日超過。**
- **方針欄の最新は 2026-09-11 で (1)(2)(3)(4) は履行済み ＝ 未処理の指示は無い。**
- **今回も実験はしていない。`core/` は 1 行も触っていない。MC-ESO の既定は 1 つも変えていない。**

---

## ユーザーの判断を仰ぐこと

**(0)【操作、いちばん急ぐ。前回から 14 commit 増えた】`research-loop` を `main` にマージしてください。** 138 commit / 10.3 日。
**status.md と report.html は毎日全面書き換えされる 2 本**なので、**溜めるほど人間側のマージが conflict に化けます。** 俯瞰は `main` に commit しません。

**(1)【9 日目 → 10 日目・繰り越し。ただし<u>内容が変わりました</u>】ゴール文の読み方。**
**読み (A)「出自を問わない」なら勝ちが 1 つ、読み (B)「この研究が作った手法で勝つ」なら 153 サイクルで勝ちはゼロ。** ここは不変です。
**変わったのは (A) の勝ちの足元です** —— **その勝ちは「公表最良を Score で上回る」という<u>harness をまたぐ比較</u>で、
その153 が<u>その比較の分解能を実測しました</u>。** **この harness は同じ手法（RR-CMA-ES）の MPR を 0.09〜0.19 低く、mean-F1 を 0.12〜0.20 高く測り、
Score の一致はその 2 つの相殺です**（下記「弱い環」1）。**上回った幅は +0.0059〜+0.0625 ＝ 食い違いの 1/3 以下。**
**推奨は変わらず (A)。ただし条件つきになりました** —— **(A) の勝ちが使えるかはキュー 1 の結果で決まります**（食い違いが報告点数で説明できれば使える、できなければ harness 間比較そのものが使えない ＝ **(A) でも勝ちはゼロになる**）。
**一行「(A) でよい」で十分です。キュー 1 は (A)(B) どちらでも撃つので、待たずに進めます。**

**(2)【新規。実行役が その151・その152 で 2 度上げた件。俯瞰の推奨つき】その93 のクラス上限が D=20 で外れていました。**
**その151 の実測 MPR 0.3831 は、その93 が引いた D=20 の上限 4 通り（0.3149 / 0.3273 / 0.3350 / 0.3729）を<u>すべて超えました</u>**（D=10 では同じ screen が上限の 97.2% で当たっていたのに、D=20 は 102.7%）。
**同じ型の screen が、2026-09-08 のユーザー決定・案 (C)（MC-ESO の調律をやめ一族の外に出る）の根拠です。**
**俯瞰の推奨は「案 (C) は変えない」です。** 理由は 1 行 —— **根拠が推定から実測に置き換わっているから。**
**案 (C) の当時は「MC-ESO は上限の側から勝てない」という<u>見積もり</u>でしたが、いまは新 suite D=10 で
MC-ESO 0.1385 対 記憶なし多スタート 0.6284（16/16）という<u>実測</u>があります。** **上限が甘かったことは、この実測を 1 ミリも動かしません。**
**判断が要るのは 1 点だけです** —— **「クラス上限」という語を論文と記録から降ろしてよいか。** 降ろす場合の作業はキュー 3 です。
**止める場合だけ書いてください**（そのままにすると、査読者が D=20 の 1 点で screen 全体を崩せます）。

**(3)【文面は不変。推奨も不変】論文の型は (A) 主・(B) 方法論節。**
**今回、型 (A) は<u>弱くなりました</u>** —— 上の (1) のとおり、主張の外部アンカー（公表値）の分解能が実測で下がったためです。

---

## いま論文に書ける主張

**「この研究が作った手法が既存手法より多く見つけた」は 153 サイクル目も書けない。** そして今回、**「代わりに書ける (A) 側の主張」の範囲が 3 次元で確定し、同時にその足場が 1 段下がった。**

**【今回確定した。範囲つきで書ける】**
1. **`Restart-Lander`（記憶なし多スタート）が競技次点 RR-CMA-ES を上回るのは、D ≦ 10 で符号が正、<u>有意なのは D=10 だけ</u>、D=20 では差が消える。**
   **D=5 +0.0550（9/3/4、p=0.057373、rb=+0.604）／ D=10 +0.0907（seed 0、p=0.029541。3 seed 平均 +0.0694 / p=0.0739、2 インスタンス平均 +0.0654 / p=0.0833）／ D=20 −0.0071（9/2/5、p=0.6698、rb=+0.143）。**
   **D=20 の正しい 1 行は「2 手法の差は消える」**（「RR が勝つ」ではない —— 問題数では RL が 9 勝 2 分 5 敗、中央値 +0.0498、平均を負にしているのは M11 と M09 の 2 問だけ）。
   **対差の並び +0.0550 → +0.0907 → −0.0071 は単調ですらなく、D=10 が最大で両側に縮む。**（その152・その153）

**【堅い。前回から不変】**
2. **新 suite D=10 で niching 手法が記憶なし多スタートに負けるのは 4 手法 5 配線で成立**（MC-ESO 0.1385 / `NCDE` 0.1171 / `r3pso` 0.1468 が 16/16、公表帯 `r3pso-p400` 0.3341 も 16/0/0。例外は直した NMMSO の 12/16）。**この比較は harness 内なので、下記の弱い環 1 の影響を受けない。**
3. **素の basin-hopping も 16/16 で負ける**（その133）＝ 強いのは「摂動つき再起動」一般ではなく記憶を持たない多スタートそのもの。
4. **勝っている側の 2 つのつまみは両方とも振り終わり、符号はどちらでも動かない**（`descent_budget` 5 水準 0.3284→0.6284、交差点は正規予算の 1/325〜1/227 で既定はその 8.1 倍上、上半分は 16 問中 9 問が完全同値の台地。`sigma_ratio` 3 水準は 4 倍振って有意差なし）。**ただしこの点検はすべて D=10 の上。**（その145〜その148）
5. **【方法論。実測 3 件】外部実装は実装の既定ではなく論文の実験節と 1 行ずつ照合する**（NMMSO +0.2596 / `r3pso` +0.1873 / `NCDE` は主水準で帰無）。**欠陥は実在して大きいのに判定を 1 問も動かさない。**（その143）
6. **【新規・方法論】公表値との照合は Score 単独で判定してはいけない。** **MPR と mean-F1 を並べ、両者が同じ向きにそろって初めて「再現した」と書く。**（その153 が新設した運用規則）

**【脆い。今回さらに弱くなった】**
7. **`Restart-Lander` が公表最良を上回る（D=5 +0.0625 / D=10 +0.0204 / D=20 +0.0059）は、<u>内訳が一致しない比較の上の差</u>である。**（下記「弱い環」1）
8. **公表最良を上回る（D=10）は報告半径にも依存する**（`r ≥ 0.001·span` の 6 点のみ。その130）。
9. **【前回の「書ける主張」5 を降格】「採点器は公表値を再現する」は<u>そのままでは書けない</u>。** 書けるのは **「Score の上で ±0.05 以内に一致する。ただし内訳は MPR が 0.09〜0.19 低く mean-F1 が 0.12〜0.20 高く、一致は相殺である」**まで。

## ゴールとの距離

**ゴール**: 多峰度の高い問題で、既存手法より多くの大域最適を見つける。**読み (B) では勝ちゼロ。読み (A) の勝ちはキュー 1 の結果待ちで条件つき。**

**(i) 3 次元の並び**（新 suite 16 問、PIN01・seed 0、正規予算 `50000·D`、報告規則 `eps_loose+dedup` r=0.05·span。その153 §5）

| D | 正規予算 | `Restart-Lander` | RR-CMA-ES | 対差 | p | 公表最良 | RL − 公表 |
|---|---|---|---|---|---|---|---|
| 5 | 250,000 | **0.7770** | 0.7220 | +0.0550 | 0.05737 | 0.7145 | **+0.0625** |
| 10 | 500,000 | **0.6284** | 0.5377 | +0.0907 | 0.029541 | 0.6080 | +0.0204 |
| 20 | 1,000,000 | 0.4504 | **0.4575** | −0.0071 | 0.6698 | 0.4445 | +0.0059 |

**(ii) D=10 のその他の手法**: `NCDE` 0.1171 ／ MC-ESO 0.1385 ／ `r3pso` 0.1468 ／ `RL-1540` 0.3284 ／ `r3pso-p400` 0.3341 ／ 素の basin-hopping 0.3404 ／ **NMMSO（公表設定に修正後）0.4144。**
**(iii) 公表値との内訳の食い違い（RR-CMA-ES、同一手法どうし。その153 §3）**: **MPR 差 −0.1821 / −0.1916 / −0.0916、F1 差 +0.1972 / +0.1210 / +0.1176、Score 差 +0.0075 / −0.0353 / +0.0130**（D=5 / 10 / 20）。**この harness の報告点数は 5.50〜11.31 点、競技の上限は `max(100, 2K)`。**
**(iv) D=2 は未測定。(v) 2D/3D（N04-N10）—— headroom 無し。(vi) F14-F20 —— 閉じた。**

## 確定した事実

- **n を増やす道は 2 本とも塞がった。** **instance は r=+0.4899、次元は r(5,10)=+0.6594・r(10,20)=+0.6238 ＝ どちらも instance 以上に相関する疑似反復**（機序: 次元は関数族・K・地形の型をそのまま残す）。**独立な標本は 16 問で打ち止め。**（その149・その152・その153）
- **勝っている側のつまみは尽きた**（D=10 の上で）。**報告軸は PR の上では空**（その115・116・130・145）。**ただし F1 / Score の上では一度も振っていない ＝ キュー 1。**
- **機序の分解は D では成立するが次元では成立しない。** D=10 で「広さと深さは別々に買える」（`descent_budget` 8.1 倍で広さ 9.0%・深さ保持率 4.6 倍）のに、**D を上げると両方が落ち、RL は広さの側が速い**（D=5→D=20 で広さ 0.584 倍 / 深さ 0.761 倍）。**降下本数は増えている**（95.2 → 117.7）＝ **落ちているのは本数ではなく 1 本あたりの当たり率。** **RR の劣化は D=10 付近で頭打ちになり、RL の劣化は次元に沿って続く**（Score 比 D=5→10 は RR 0.745 / RL 0.809、D=10→20 は RR 0.851 / RL 0.717）＝ **その152 の「RR は次元耐性が高い」は訂正済み。**（その151・その153）
- **その93 のクラス上限は D=20 で保守的すぎた**（実測 MPR 0.3831 が 4 通りすべてを超過）。**D=10 では 97.2% で当たっていた。**
- **GECCO 2025 の競技結果は 09-20 の再検索でも見つからない**（4 度目）＝ **公表値の出典 1 本という限界は 11 日間動いていない。**
- **記録が黙って帰無を作れる経路が約 30 サイクル生きていた**（その150 §3。`e115/analyze.py` が exit 0 で `nan` / `p=1  0/0/0` の完成形の表を刷った）。**実在しない入力を指すスクリプトは `analysis/` 全体で 8 本、実害のあるものが 4 本。1 本は直したが残り 3 本は未処理。**

## 否定された仮説（再提案しない）

| 仮説 | 結果 |
|---|---|
| PR は evals/K や K で決まる ／ 掘り残しの回収 ／ F14-F20 に勝てるセル ／ 報告点数・報告規則・cap・trim が差を説明する（**PR の上で**）／ 最適の配置の構造が差を決めている ／ 着地点近傍への再投入は効く ／ 公表設定に直せば外部 niching 手法は null に届く ／ MC-ESO の広さの不足は hunt 長による ／ 12500 はつまみの当たり ／ instance 軸を増やせば n が増える | 棄却（その17〜その149。全文は [acceptance_topology.md](acceptance_topology.md)） |
| **次元軸を増やせば独立な n が増える** | **【今回】棄却 —— r(5,10)=+0.6594 / r(10,20)=+0.6238 は instance 間 +0.4899 <u>より高い</u>**（その152・その153） |
| **頭の主張は次元によらない** | **【今回】棄却 —— D=20 で符号が反転し有意性も消える。範囲は「D ≦ 10 で符号が正、有意なのは D=10」**（その152・その153） |
| **その93 のクラス上限は一族の到達上限である** | **【今回】棄却 —— D=20 で同じ一族の実測が 4 通りすべてを超えた**（その151） |

## いちばん弱い環

1. **【今回、首位が入れ替わった。査読者の第一撃】公表値との比較は、同じ量を比べていない。**
   **この harness は公表手法（RR-CMA-ES）の MPR を 3 次元とも 0.09〜0.19 <u>低く</u>、mean-F1 を 0.12〜0.20 <u>高く</u>測る。**
   **Score が ±0.05 で一致するのは、この 2 つが平均の中で相殺するからである。** **機序は報告集合の大きさ**（競技は `max(100, 2K)` まで報告できるのに、この harness は 5.5〜11.3 点しか出さない ＝ precision が上がり recall が落ちる）。
   **＝ 頭の (A) 側の主張「公表最良を +0.0059〜+0.0625 上回る」は、自分で測った食い違い（0.09〜0.20）の<u>1/3 以下</u>の幅を、その食い違いをまたいで主張している。**
   **これはキュー 1 で測れる**（追加評価ゼロ）。**測って説明がつかなければ、harness 間比較は落とし、harness 内の直接対決（弱い環 2）だけが残る。**
2. **その直接対決に有意性は無く、増やす道も無い。** **α=0.05 を跨ぐのは 3 次元 × 複数の読みのうち D=10 の seed 0 の 1 本だけ**（D=10 の慎重な読みは p=0.0739 / 0.0833、D=5 は 0.0574、D=20 は 0.6698）。**16 問は suite の全部で、instance も次元も疑似反復。**
3. **【153 サイクル連続】勝ちが「この研究の手法」からは 1 つも出ていない。** **この 17 サイクルは腕を 1 本も作っていない。**
   **新しい機序の候補は今回も無い。** 占有 11 件に加え、**自分の測定 2 件（その113「撒き方」16/16 で否定、その118「配置の構造」ρ=−0.091）が残りを潰している。** **今回も 2 語彙で検索したが候補は出なかった**（下記「次に効く一手」）。
4. **【新規】記録そのものが「有意でなかった」を黙って作れる。** その150 §3 が見つけた 4 本のうち **3 本が未処理**で、うち `e113/analyze.py` は **いま頭の主張の機序側（4.5 倍差の内訳）に効いている節**のもの。**測定の論文を書こうとしている以上、これは体裁ではなく中身の問題。キュー 2。**
5. **外部比較の橋** —— **公表値の表が資料 1 本のまま、11 日間動いていない。** 本文取得は CI の `fetch_refs` が唯一の経路。
6. **`CLAUDE.md` が pin する SR@1e-10 93.5% / evals 798 がこの環境で 3 回とも再現しない**（92.08% / 677.7）。ゲートは `best_f` バイト一致。**pin の数値は書き換えない。**
7. **片付けは規則違反ゼロ、余裕 49** —— `analysis/` は **351 ファイル / 1.83 MB**（平置きゼロ、300 行超の非 gz CSV ゼロ、1 MB 超の塊ゼロ、`check_cost_rows.py` / `check_doc_links.py` とも通過）。**その150 の統合で 4 サイクル続いた「削除ゼロ」は止まった。**
   **未処理の記述ずれは 2 件** —— **`web/app_lib/results.py:234` の「PR」列が `pr_1e-4`（7 度目の再確認、未修正）**、**`scripts/` 直下 22 本のうち 11 本が呼び手なし。**

## 次に効く一手

**キューは 2 → 4 件。1 番は動かさず、2・3 番を新設した。ゴールは変えていない。**

- **1 番「報告点数を競技の上限側に寄せて 3 次元で採点し直す」**（実行役が その153 の結末として書いた問い。**そのまま最優先**）—— **弱い環 1 の唯一の直接の検査で、追加評価ゼロ（保存物の再採点のみ）。** **どちらに転んでも書く文が決まる** —— 食い違いが縮めば「原因は報告点数」と書けて (A) の勝ちが生き、縮まなければ **harness 間比較を落として (B) 側の構成に切り替える**。
- **2 番「黙って帰無を作る経路の棚卸し」**（新設）—— **その150 §3 の残り 3 本を潰し、とくに `e113/analyze.py` が出した結論を生き残った入力から引き直す。追加評価ゼロ。** **記録の信頼性は、測定の論文では主張そのもの。**
- **3 番「その93 の上限推定は<u>どこで</u>外れたのか」**（新設）—— **4 通りのどの仮定が D=20 で破れたかを、その151 の保存物と `e92`/`e93` の script で切り分ける。新規 run ゼロ。** **「クラス上限」を論文に書けるか書けないかがこれで決まる**（判断 (2)）。
- **4 番「記述とコードのずれ ＋ 棚卸し」**（旧 2 番、内容不変）。

**キューに<u>入れなかった</u>問い**: **(a) D=2 を測る** —— D=5 で既に RR も公表帯の上に出ており、次元は疑似反復だと確定したので、**4 点目を足しても書く文が 1 語も変わらない。**
**(b) D=20 で seed を増やす**（その151 の提案）—— **差が 1 SD 未満なのは事実だが、seed は最も強い疑似反復で、D=10 では その123・その124 が既に効かないと測っている。**
**(c) `NCDE` の母集団を新 suite で振る** —— 前回と同じ理由で見送り（`r3pso` で埋まったのは差の 39%、`NCDE` の差は 4 本中最大）。

各問いの測る量・反証条件・始点は [research_loop.md](research_loop.md) の「未解決の問い」に書いた。

**率直に言うと、この 4 サイクルは前回の計画どおり次元を 3 点そろえ、そして<u>悪いほうの答えを 3 つ持ち帰った</u>。**
**(1) 頭の主張は D=20 で消える。(2) 次元は標本を増やす道ではない。(3) そして<u>いちばん重いのは 3 つ目</u> —— 公表値との比較は、内訳が 0.09〜0.20 食い違ったまま Score の相殺で成立していた。**
**153 サイクルかけて積んだ (A) 側の主張は、いま「harness 内では有意でなく、harness 間では分解能が足りない」という位置にある。**
**これは失敗ではなく、点検が自分の足元に届いたということだ** —— **4 サイクル前に同じ点検を勝っている側のつまみに向けたときは疑いが晴れた。今回は晴れなかった。**
**キュー 1 は、この 153 サイクルでいちばん結果が読めない問いである。** 縮めば (A) は生き、縮まなければ **この研究が書ける論文は「ベンチマークの公式指標が harness 間で比較可能でない」という方法論の側に、主張ごと移る。**
