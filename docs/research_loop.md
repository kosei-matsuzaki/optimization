# 研究ループ（1 時間ごと 1 サイクル）

自動実行される 1 時間ごとの研究サイクルの手順書と、その状態。**このファイルが唯一の引き継ぎ**で、各回はここを読んで続きから始め、ここに結果を書いて終わる。

現在のテーマは**多峰度の高い問題での peak ratio の改善**（下記ゴール）。

**確定した結論は全て [acceptance_topology.md](acceptance_topology.md) にある。このファイルには残さない。**
同ファイルは 2 テーマ分に分かれている: 前半が前テーマ「受容集合と多様解の監査」（2026-09-02 に新規測定を止め、末尾の終了節に確定した 7 点と新ゴールへ引き継いだ 1 点がある）、
末尾の [「多峰テーマ（peak ratio）の確定事項」](acceptance_topology.md#多峰テーマpeak-ratioの確定事項) が現テーマ。
**このファイルの「作業ログ」は直近 3 件だけの一時領域**で、統合の回（手順 2）ごとに集約先へ移して畳む。
MC-ESO 側は [mceso.md](mceso.md) / [history.md](history.md)、分野調査は [related_work.md](related_work.md)。

---

## この研究のゴール（毎回、着手する問いをここに照らすこと）

**多峰度の高い問題（大域最適が多い問題）で、既存手法より多くの大域最適を見つける。**

分野の系統的レビュー（[Algorithms 17(10):448, 2024](https://doi.org/10.3390/a17100448)）が
未解決課題として「次元と多峰度が高いときのスケーラビリティ」を挙げ、今後の方向として
"space and time-scalable methods" を名指ししている。実測でも headroom はここに集中する:
K=36 の N07 では最良手法が PR 0.97 に届くのに、K=216 の N09 では 0.49、他手法はほぼ 0.25 未満。

- **主指標**: CEC2013 niching の peak ratio（精度 ε = 1e-1 〜 1e-5）。**特に K の大きい関数**
  （N06 K=18 / N07 K=36 / N08 K=81 / N09 K=216）。
  **ただし手法の判定は ε ≤ 1e-3 で行う**（その28）。1e-1 は報告規則の質を測っており、
  報告規則を揃えると 7 手法中 7 手法が N07 で 1.00 に張り付いて識別力を失う。
  1e-1 / 1e-2 は参考値として併記してよいが、順位の根拠にしない。
- **この研究における「多様性」の定義**: 精度 ε 以内で発見された相異なる大域最適の数。
  分野の標準定義を採り、自作の定義は使わない。
- **比較対象**: NMMSO / NCDE / Crowding-DE / r3pso / NM-Restart / MC-ESO。現状の首位は NMMSO。
- **手法は自由**。MC-ESO を改善する必要はない。比較対象の 1 つとして残すだけ。
  ただし MC-ESO 自体を壊さない（BBOB-24 dim2 の SR@1e-10 93.5% は既存資産として保つ）。

**問いを選ぶときの条件**: その問いが片付くと、K の大きい関数の peak ratio が上がるか、
上がらない方向を確実に潰せるか。どちらでもない問いは、面白くても後回しにする。

**占有されている領域**（実装前に必ず確認したこと）:

- **報告規則を独立した設計対象にすること**は済み。[Peak Identification in Evolutionary
  Multimodal Optimization](https://pmc.ncbi.nlm.nih.gov/articles/PMC11505590/) が探索と選別を
  2 相に分け、niche 半径不要の HVPI と冗長解を罰する F 値を提案している。
- **報告効率を測ること**も済み。GECCO competition の Dynamic F1（報告解のうち相異なる
  大域最適だった割合の、評価回数に沿った積分）。
- **持続性による盆地分解**は [arXiv 2605.18351](https://arxiv.org/pdf/2605.18351) が占有。

**分かっている手がかり**（過去の測定から。**数値・表・機序の全文は
[acceptance_topology.md の「多峰テーマ（peak ratio）の確定事項」](acceptance_topology.md#多峰テーマpeak-ratioの確定事項)**
にある。ここは見出しだけ）:

- **受理規則が深さと広さを同時に決めている。** 親比較なら深さ、最近傍置換なら広さ。
  1 つのつまみでは両立しない（MC-ESO で SR@1e-10 100% → 0%、DE / PSO でも同傾向）。
- **効くのは返す個数ではなく、上位が互いに別物であること。**
- **MC-ESO はどちらの地形でも「盆地は見つけるが掘り切らない」**（その20 で統一された）。
  Shubert は PR@1e-1 0.80 → PR@1e-5 0.15。Vincent は報告規則のせいで見えにくかっただけで、
  履歴を見れば N09 の 216 個すべてに 1e-1 まで近づいており、1e-3 まで降りるのは約 28 個。
  **かつて「Vincent はそもそも見つけない」と書いていたのは誤り**（報告集合しか見ていなかった）。
- **hunt 単位で見ると `reported ≈ min(distinct hunts, deep hunts)`**（その21）。
  **Vincent は distinct（被覆）側、Shubert は deep（深さ）側が binding。**
  Vincent には局所最適が存在しないので、盆地に入りさえすれば必ず降り切る。
- **PR は evals/K でも K でも決まらない**（その17）。分けているのは
  **予算を新しい最適解に変換し続けられるか**。r3pso は完全に飽和、MC-ESO は高 K でだけ飽和、
  NMMSO だけが高 K でも変換し続ける。
- **報告枠は制約になっていない**（その19・その20）。採点上限 max(100, 2K) に対し
  MC-ESO は 88 点しか報告していないが、履歴から選び直しても利得は **eps=1e-1 に集中**する。
  1e-3 以下では実質同等なので、**報告規則側の残り代は浅い**。
- **報告規則の路線は 7 手法で測って閉じた**（その28）。選び直しを全手法に与えると
  利得は**全手法で eps=1e-1 に集中**し（N07 は 7/7 手法が PR@1e-1 = 1.00 に張り付く）、
  **eps ≤ 1e-3 では上位 2 位が全 5 関数で一切動かない**。
  **NMMSO が最も利得を受けない手法**なので、この路線で首位との差は埋まらない。
  → **運用規則: PR@1e-1 で手法を比較しない。判定は 1e-3 以下で行う**（下記）。
- **再起動の被覆を決めているのは描画ではなく選抜**（その22）。盆地切替は 20 スロット全部を
  独立に反発描画するので描画は全 36 解を覆うが、μ+λ 貪欲の best-of-20 が系統的に広い盆地の
  点を選ぶため着地は 11-15 解に留まる。**描画規則や σ_init（箱に対する固定比）を変えても
  この偏りは動かない。**
- **`sigma_only` の利得は hunt 数ではない**（その25）。予算を 40% 増やして hunt 数を
  追い越させた base（51.5 対 49.1）でも N07 の PR@1e-3 は届かず 15 seed 全敗。
  ただし **σ と予算は別の精度水準に効く**（Shubert の粗い水準は予算律速で σ は動かさない）。
  **解放規則（`hunt_no_improve_mult` / `exhausted_sigma_tol`）では base の hunt 数は上がらない。**
- **ただし「σ を局所盆地間隔に合わせる」は別のつまみで、これは効く**（その23）。
  再起動後の σ を `0.1 × d_local`（観測済み盆地重心の最近傍距離、追加評価ゼロ）にすると
  N07 の PR は全 eps で 0.22 → 0.34（15/15 seed）、さらに 20 本の描画を 1 本に commit すると
  0.44。着地した相異なる最適解は 11/36 → 26/36。**箱に対する比と局所盆地間隔に対する比を
  混同しないこと** — その22 が振ったのは前者、効いたのは後者。
- **しかしその利得は 2D 限定で、路線ごと畳んだ**（その26・その27）。**N08-Shubert3D では符号が反転し**
  （PR@1e-1 が base 0.34 に対し 0.20、5 seed 全敗）、**比を 0.1 / 0.25 / 0.5 と振っても
  内点に最適は無い**（全カラムが単調に base へ戻る）。**痛みが消える 0.5 は効果も消えている**
  （深精度の利得が 5 seed 全部で base と同値）。3D では深さの利得が**有償**になる
  ＝ **次元が上がると深さと広さが再結合する**。詳細は
  [acceptance_topology.md の該当節](acceptance_topology.md#局所盆地間隔スケーリングは-2d-限定で確定--3d-では比が単調なトレードオフダイヤルになるその26その27-analysishmn08_csv)。
  **BBOB-24 dim2 はこの路線に対する識別力を持たない** — 盆地が 3 個未満だと base の経路に
  落ちるので、全 24 関数で MC-ESO とビット一致する（＝安全の証拠ではなく未発火の証拠）。
- **N08-Shubert3D の 1e-5 は判定に使えない** — base が 5 seed すべてちょうど 1 解（PR = 0.01）。
  **3D の判定は 1e-1 / 1e-3 で行う。**
- **規則側の梃子は全部閉じた**（その29）。**σ のスケールと受理規則という構造的に無関係な 2 つの介入が、
  同じ深さ↔広さの線の上を動く**（どちらも distinct を増やし `blocked` を 4-5 倍にし landed を減らす、
  同一の署名）。位相分け（枯渇まで親比較、枯渇後だけ最近傍置換）も**深さを分離しない** —
  最初の枯渇までに稼げるのは最初の 1 盆地の深さだけで、**必要な深さは境界の守られない側に全部ある**。
  **1 本の hunt に落ちる評価回数を変えない限り線から出られない**ので、残るのは配分側。
- **BBOB-24 dim2 / 5000 評価の gate は「枯渇後だけを触る介入」に対して構造的に盲目**（その29）。
  `phased` は**全生成のおよそ半分で発火している**のに全 24 関数で tie_count = 20。理由も測った:
  **24 run のうち最初の枯渇より後に best_f が改善した run は 0 個**。
  **この gate を通ったことは安全の証拠にならない**（その26 の「発火していない」より強い）。

---

## 1 サイクルの手順

**このループは 1 時間ごとに走る。前の回がまだ走っている可能性があるので、手順 0 と 1 を飛ばすと二重着手になる。**

0. **ブランチを合わせる** — `git fetch origin && git checkout research-loop && git pull --rebase origin research-loop`（無ければ main から作る）。**成果は全部この 1 本に積む。** 実行ごとにブランチを切らない。切ると前回の続きから進めなくなり、統合されない成果物が散る。main には触らない（人がマージする）。
1. **問いを 1 つ claim する** — 「未解決の問い」の上から、まだ claim されていないものを 1 つ選び、その行に `(claimed YYYY-MM-DD HH:MM UTC)` と書いて**即コミット・push する**。これで他の回は別の問いを選ぶ。**2 時間以上前の claim は放棄されたものとみなして奪ってよい**（奪ったら時刻を書き換える）。
2. **統合の回かを判定する** — 「作業ログ」の件数が **8 件を超えていたら、新しい実験をせずに統合作業をする**。すなわち: ログの結論を `acceptance_topology.md` の該当節に集約し、決着した問いを「未解決の問い」から消し、ログを直近 3 件＋要約に畳む。積み上がらない一番の原因は記録が増えるだけで整理されないことなので、これは実験より優先する。
3. **先行研究を潰す** — 実装の前に、隣接分野の語彙で検索する（下記「先行調査の鉄則」）。既にあるなら問いを畳んで記録し、次の問いへ。
4. **設計する** — 何を測れば白黒つくか、どういう結果なら仮説が否定されるかを先に書く。撤退条件のない実験はしない。
5. **実装して回す** — 小さく回して配線を確認してから本測定。既存スクリプトで足りるなら新規作成しない。**1 回の計算は 40 分以内**に収める。収まらないなら関数か seed を削り、削ったと書く。
6. **分析する** — 対応のある検定と効果量、seed 方向一致率。平均だけで結論を出さない。
7. **記録してコミットする** — 「作業ログ」に追記し、claim を外して結果を書く（決着したら問いを消す、続きがあれば次の問いとして書く）。**否定的結果も機序つきで残す**。`research-loop` に push する。push が競合したら `git pull --rebase` して再試行。

**1 回で全部やろうとしない。** claim した問いが 40 分で終わらないと分かったら、そこまでの結果と「次はここから」をログに書いて終える。中途半端な状態を残すより、次の回が続きから入れる形にすることが優先。

---

## 守る規則（これまでの失敗から）

**先行調査の鉄則** — 新しい定式化を思いついたら、**実装より先に 4 分野以上の語彙で検索する**。この研究では 2 回、提案の中核が先行研究と一致して撤退した。

- 進化計算（niching / QD / EDO）だけでなく、ロバスト最適化（K-adaptability, ROMS）、設計工学（solution space engineering, set-based design）、計算化学（disconnectivity graph, barrier tree, NEB）、統計（cluster tree）、TDA（merge tree, persistence）、能動学習（level set estimation）の用語で引く。
- 「barrier tree は調べたが disconnectivity graph では引かなかった」で 1 日を失った。**同じ概念の別名を列挙してから検索する。**

**測定の鉄則**

- **単一 seed で結論を出さない。** 1 seed では NM-Restart が最良と出た関数で、3 seed では最下位になった。
- **集計から落ちた対象を必ず報告する。** 比が定義できない関数を黙って除くと順位が変わる（実際に変わった）。
- **定義を変えたら結果がどう動くかを測る。** τ・重複除去半径・行動記述子・シナリオモデルはいずれも答えを桁で動かした。定義依存性は欠陥ではなく報告事項。
- **推定器の誤りの向きを述べる。** 線分による連結判定は繋がりを見落とす側にしか誤らない、など。
- **後処理の利得は 1 手法で測らない。** 報告規則のように**手法非依存に当てられる**ものは、
  1 手法で測ると必ず「効く」ように見える。その20 は MC-ESO 単独で PR@1e-1 を 0.25 → 1.00 に
  上げたが、その28 で 7 手法に与えたら**全員が上がり順位は 1e-3 以下で不動**だった。
  **他手法にも与えられる介入は、他手法にも与えてから読む。**
- **同一 run を 2 通りに採点できるなら、そうする。** その28 は 1 セットの run を
  2 つの報告規則で採点したので、ペアリングが厳密で追加評価はゼロだった。
  比較のために run を 2 回回すと、seed 差が交絡として入る。

**MC-ESO を壊さない**

- `SR@1e-10` を下げる構成は採用しない（多解の改善でも例外なし）。
- 既定値を変えたら **BBOB-24 dim2 / n=20 で SR@1e-10 93.5% / evals_succ_mean 798** と一致することを確認してからコミットする。
- 診断用の変種は本体を書き換えず別ファイルに置く（例: `mceso_crowding.py`）。

---

## 未解決の問い（上から着手する）

claim 中のものは `(claimed ...)` が付く。2 時間以上前の claim は奪ってよい。

**方針（2026-09-02、2 度目の転換）**: MC-ESO の改善という制約を外し、「多峰度の高い問題での
peak ratio」をゴールに置き直した。監査テーマ（受容集合）は新規測定を止め、記録は
[acceptance_topology.md の終了節](acceptance_topology.md#監査テーマの終了2026-09-02--何が残り何がゴールに引き継がれたか)
に集約済み。報告規則の路線は先行研究に占有されている（上記「占有されている領域」）。

1. **掘り残した盆地に戻る（予算配分側の最初の梃子）** (claimed 2026-09-03 05:32 UTC) — その29 で**規則側の梃子が全部閉じた**。
   σ のスケール（その26・その27）と受理規則（その29）という構造的に無関係な 2 つの介入が、
   **同じ深さ↔広さの線の上を動く**ことが実測で確定したので、
   **1 本の hunt に落ちる評価回数を変えない限り線から出られない**。
   そこで規則ではなく**配分**を触る。**現在の再起動は掘った盆地から必ず反発する方向にしか出ないので、
   浅いまま置き去りにした niche に二度と戻らない。**
   その在庫は測ってある: eps=1e-3 で **N06 は 20.2 個、N07 は 15.7 個が `blocked`**
   （相異なる niche なのに eps 未達。base ではそれぞれ 5.2 / 3.2 しかない ＝
   **浅い在庫は crowding 相を入れると作れる**）。
   **やること**: 予算の末尾（例えば最後の 20%）で反発をやめ、**記録済みの `blocked` 盆地重心に
   σ を小さくして再突入する**「回収相」を足す。追加評価は要らない（重心は既に `ir_basin_centroids` にある）。
   `mceso_commit_reseed.py` の重心幾何をそのまま再利用でき、本体 `mceso.py` は触らない。
   **判定は N06-Shubert2D の PR@1e-5 が上がること**（15 seed / 20000 評価、対応のある Wilcoxon、
   base = `analysis/commit15_base.csv` を再利用すれば再測定は 1 腕で済む）。
   **棄却条件**: PR@1e-5 が上がらない、または 1e-1 / 1e-3 が下がる。
   その場合「浅い niche は原理的に回収できない（浅さは盆地の質であって予算不足ではない）」
   が確定するので、**Shubert の深さは MC-ESO の枠組みでは閉じたと結論してよい**。
   **注意 1**: **BBOB-24 dim2 / 5000 評価の gate はこの種の介入に対して盲目**（その29）。
   枯渇後にしか動かない変種は全 24 関数で必ずビット一致するので、
   **通っても安全の証拠にならない**。安全性を測るなら予算か次元を上げること。
   **注意 2**: 判定は 1e-3 以下で行う（その28）。3D なら 1e-1 / 1e-3（その27）。

2. **r3pso と NCDE の飽和の機序を切り分ける**（MC-ESO 側は決着済み） — その17 より、手法を
   分けているのは **予算を新しい最適解に変換し続けられるか**。止まる手法について原因を
   3 つに切り分ける: **(a) 集団やアーカイブが埋まっている**（容量）、
   **(b) 同じ最適解を再発見し続けている**（重複回避）、**(c) 新しい盆地が尽きている**（探索）。
   **MC-ESO については その19〜その21 で決着した**（Shubert は deep 律速 = (c)、
   Vincent は distinct 律速 = (b)、eps=1e-1 でだけ (a)。詳細は
   [多峰テーマの確定事項](acceptance_topology.md#多峰テーマpeak-ratioの確定事項)）。
   **残っているのは他手法** — 特に **r3pso の完全な飽和**（10 倍予算で N10 0.48 → 0.47、
   N09 0.03 → 0.02）と **NCDE**（N06 で 0.00 → 0.10 しか動かない）。
   `diagnose_niching.py` は `_CountingMCESO` に依存しているので、他手法に当てるには
   visited / reported を手法非依存に取り直す必要がある（`history_x` と `final_solutions` は
   共通の `OptimizeResult` にあるので、カウンタ類を諦めれば汎用化できる）。
   **その28 が材料を 1 つ足した**: 選び直しを与えたときの利得が
   r3pso は 1e-3 以下でほぼゼロ（+0.017 / +0.005 / +0.000）なので、
   **r3pso の飽和は報告側ではない**（(a) 容量ではなく (b) か (c)）。
   棄却条件は特になし（機序の特定が目的）。**優先度は低い** — 他手法の負け方が分かっても
   こちらの PR は上がらないので、問い 1 の後。
3. **NM-Restart の Shubert 行を直すか、ベースラインから外す** — その28 で
   **N06-Shubert2D の restart 回数が 1 回**（20000 評価を 1 回の Nelder-Mead が使い切る）
   と判明した。原因は `nelder_mead.py` の `_XATOL = 1e-12` / `_FATOL = 1e-14` で、
   局所最適 760 個の Shubert では simplex が収束しない。N07 は 112 回、N10 は 117 回なので
   Vincent / ModRastrigin の行は有効。**やること**: 1 回の restart に評価上限を与える
   （例 `maxfev = min(残予算, c·dim)`）か、Shubert 用に許容を緩める。
   **どちらも「多スタート法の定義を変える」ことになるので、変更前後の両方を報告すること**
   （その15 / その17 の Shubert 行は無効として扱う）。**優先度は低い**
   — ベースラインの整合性の問題で、こちらの PR は上がらない。
4. **正規予算での比較を Actions に載せる** — 現状の測定は正規の 1/10 予算なので順位の細部は
   確定しない。正規予算（5 万〜40 万評価）での比較を GitHub Actions で回す。
   **その28 の運用規則（判定は 1e-3 以下）をここにも適用すること。**
5. **投稿先の選定** — GECCO。締切と要求形式を調べる。

**畳んだ問い**（結論は全て `acceptance_topology.md` に残っている）:

- 報告規則の転用 — 先行研究に占有（上記「占有されている領域」）。
- 監査側の全て — 全滅の機序の次元不変性、共通探索での比較、断片化ゲート、適応 τ、
  参照プールの解像度。
- **PR は evals/K や K で決まるか** — その17 で棄却。決めているのは予算応答。
- **Vincent は「見つけていない」のか「報告していない」のか** — その19〜その21 で決着。
  **報告枠は制約ではない**（採点上限 max(100, 2K) に対し 88 点）。
  1e-1 では記録の損失、1e-3 以下では**盆地の重複**（一様描画 ＋ 固定反発半径 対 対数間隔の盆地）。
- **反発半径を盆地スケールに適応させれば被覆が直るか** — その22 で棄却（0.24 → 0.27、
  要 0.50）。描画は既に全 36 解を覆っており、律速は best-of-20 の選抜。
  **箱が混んでいない限り描画規則の作り込みは無意味**（`adaptive` と `adaptive_maxmin` が全行一致）。
- **σ を局所盆地間隔に合わせれば深さと広さが両立するか** — その26・その27 で 2D 限定として棄却。
  3D では比が単調なトレードオフ・ダイヤルで内点に最適が無い。
- **選び直しの報告規則を全手法に与えたら順位は変わるか** — その28 で決着。
  **eps ≤ 1e-3 では上位 2 位が全 5 関数で不変**、利得は全手法で 1e-1 に集中する。
  **報告規則の路線は閉じた**（NMMSO は最も利得が小さく、報告集合が既に選び直し後と同じ）。
- **NM-Restart が Shubert で 1 点しか報告しない件** — その28 で機序が判明（restart 回数が 1）。
  対処は問い 3 に移した。
- **受理規則の 2 段構えで深さと広さが両立するか** — その29 で棄却。PR@1e-5 は N06 0.17 → 0.10、
  N07 0.22 → 0.14 と**両関数で下がる**。N07 の粗い側は大きく上がる（1e-1 で 0.26 → 0.45、14/1/0）が、
  それは深さを売った代金。**crowding は広さを買い、深さで払う** — σ と同じ線の上。

---

## 作業ログ

新しい回ほど上に書く。1 回 1 節、日付・やったこと・分かったこと・次に繋がる問い。
**8 件を超えたら次の回は統合の回**（手順 2）。

### 2026-09-03 その29（**受理規則の路線も畳んだ** — 位相を分けても深さと広さは分離しない。σ と受理規則は同じ 1 本の線の上を動く。おまけに BBOB gate が構造的に盲目であることが確定した）

問い 1。**決着したので問い 1 は削除し、本文は
[acceptance_topology.md の該当節](acceptance_topology.md#位相を分けても深さと広さは分離しない--受理規則は-σ-と同じ-1-本の線の上を動くその29-analysishmphased15crowd15_csv)
に移した。MC-ESO の既定値は不変**（`mceso.py` は触っていない）。

**やったこと。** `core/optimizers/mceso_phased_accept.py` に `PhasedAcceptMCESO` を新規作成。
最初の枯渇（`st.has_exhausted`）までは出荷版の μ+λ 親比較、**その後の hunt だけ最近傍置換**。
crowding の本体は `mceso_crowding.py` のメソッドを呼ぶだけなので `accept_phase="always"` は
`MCESOCrowding` と**構成上ビット一致**する。`scripts/diagnose_niching.py` に
`phased` / `phased_off` / `phased_always` / `crowd_always` を追加、`quick_check.py` に `phased` を追加。
**恒等チェック（本測定の前に実施）**: `phased_off` は base と、`phased_always` は `crowd_always` と、
N06 / 2 seed の**全カラムが数値完全一致**。
本測定は N06-Shubert2D + N07-Vincent2D、**15 seed / 20000 評価 / eps 1e-1,1e-3,1e-5**、2 プロセス並列で 14 分。
base は **その23 の `analysis/commit15_base.csv` をそのまま再利用**（同じ seed 列・同じ予算）したので再測定ゼロ。
解析は既存の `scripts/hunt_confound.py`。

**判定（事前登録どおり棄却）。** 基準は「PR@1e-5 が上がり、かつ BBOB-24 dim2 の SR@1e-10 を割らない」。
**PR@1e-5 は両関数とも下がった**: N06 0.17 → **0.10**（2/2/11、p = 0.0220）、
N07 0.22 → **0.14**（0/2/13、p = 0.0009、A12 = 0.07）。

**正の結果も出ている（Vincent の粗い側）。** N07 の PR@1e-1 は 0.26 → **0.45**（**14/1/0**、
p = 0.0007、A12 = 0.97）、PR@1e-3 も 0.22 → **0.30**（14/1/0）。**hunt 数は 35.5 → 24.9 と減っている**
ので、その25 の「利得は hunt 数ではない」がここでも成り立つ。
**丸ごと crowding に swap した対照より、位相を分けた方が全 eps で良い**（N07: 0.45/0.30/0.14 対 0.29/0.19/0.07）
ので、位相境界そのものは何かを守っている。**守れていないのが、狙っていた深さだった。**

**機序 — 増えるのは「浅い相異なる niche」で、これは σ を締めたときと同じ署名である**（eps=1e-3 の診断カラム）:

| 関数 | 構成 | hunts | distinct | **blocked** | landed |
|---|---|---|---|---|---|
| N06 | base | 29.5 | 19.9 | **5.2** | 28.9 |
| N06 | **phased** | 26.1 | **31.5** | **20.2** | 18.9 |
| N07 | base | 35.5 | 11.3 | **3.2** | 35.4 |
| N07 | **phased** | 24.9 | **26.6** | **15.7** | 22.7 |

その26 が 3D の `sigma_only` で見た表（distinct 56.6 → 70.8、blocked 29.0 → 54.6）と**同じ形**。

**ゴールに対する結論 — 梃子の「族」が閉じた。**
**σ のスケールと受理規則という構造的に無関係な 2 つの介入が、同じ深さ↔広さの線の上を動く。**
このトレードオフは特定のつまみの癖ではなく **hunt あたりの予算配分の性質**なので、
**受理規則や描画規則をどう書き換えても、1 本の hunt に落ちる評価回数を変えない限り線から出られない。**
**位相境界が効かなかった理由も分かった**: 最初の枯渇までに稼げるのは**最初の 1 盆地の深さ**だけで、
peak ratio が要求する深さは**2 個目以降の盆地**にあり、それは丸ごと crowding 相に入る。
**深さを守るつもりで区切った境界の、守られない側に、必要な深さが全部あった。**

**副産物（これが今回いちばん再利用が効く）— BBOB-24 dim2 の gate は枯渇後の介入に対して構造的に盲目。**
BBOB-24 dim2 / n=20 は**全 24 関数で tie_count = 20**、SR@1e-10 **92.08%** / evals_succ_mean **677.7** が両腕で一致
（`analysis/hm/bbob24_phased_{summary,wilcoxon}.csv`。92.08% は この環境の MC-ESO 自身の値で、
pin の 93.5% との食い違いは その26 と同じ）。
**その26 と違い、今回は変種が発火している**: 生成回数を直接数えると F01 は base 規則 404 / crowding 規則 573、
F21 は 306 / 668、F23 は 690 / 295 で、**全生成のおよそ半分が crowding 相**。
それでも一致する理由を独立に測ると、**8 関数 × 3 seed = 24 run のうち最初の枯渇より後に best_f が
改善した run は 0 個**（枯渇する run は枯渇時点で既に最終値、枯渇しない run は F24 と F17 の 1 seed で
そもそも失敗している）。
**運用規則: 枯渇後の hunt だけを触る変種は、この gate を通っても安全性の情報を何も得ていない。**
本当に測るには予算を伸ばすか次元を上げる必要がある。

**環境の記録。** その28 の手順で全部通った（`pip install ioh cma numpy scipy multiprocess matplotlib`
＋ pynmmso sdist を site-packages にコピー ＋ `random.sample(<set>, k)` 4 箇所を `list(...)` 化）。
**この 3 点はコンテナが変わるたびに再適用が要る。**

**次に開くこと。** 規則側（描画・報告・受理・σ）の梃子はこれで全部閉じたので、
**残っているのは予算配分側**だけになった。新しい問い 1 に書いた:
**掘り残した盆地（`blocked`）に戻る**。今の再起動は掘った盆地から**必ず反発する方向にしか出ない**ので、
浅いまま置き去りにした niche に二度と戻らない。今回の測定はその在庫を直接数えている
（N06 で 20.2 個、N07 で 15.7 個が distinct なのに eps 未達）。

### 2026-09-03 その28（**報告規則の路線を畳んだ** — 選び直しは 7 手法全部を押し上げ、eps ≤ 1e-3 では順位を 1 つも動かさない。以後 PR@1e-1 は判定に使わない）

問い 1。**決着したので問い 1 は削除し、本文は
[acceptance_topology.md の該当節](acceptance_topology.md#報告規則の路線は閉じた--選び直しは-7-手法全部を押し上げeps--1e-3-では順位を一切動かさないその28-analysishmresel_csv)
に移した。MC-ESO の既定値は不変**（`mceso.py` は触っていない）。

**やったこと。** `scripts/niching_baseline.py` に `--report-rule current|reselect|both` を追加。
`both` は**同一の run を 2 通りの報告規則で採点する**ので、ペアリングが厳密で追加評価はゼロ。
選び直しは既存の `scripts/diagnose_niching.reselect_from_history` をそのまま import した
（新規実装なし）。採点は `core.runner._niching_counts` を両側で共用し、
`dataclasses.replace` で `final_solutions` だけ差し替えるので、cap = max(100, 2K) の
トリムも rho 貪欲も**両腕で完全に同じコード**が走る。
**7 手法 × N06-N10 × 5 seed**、予算は その15 と同じ 0.1×suite（2D 20000 / 3D 40000）。
2D 3 関数 / N08 / N09 の 3 プロセス並列（その25 の「同時 3 本まで」を守った）、**計 11 分**。
`analysis/hm/resel_{2d,n08,n09}.csv`、解析は新規の `scripts/resel_rule.py`。

**配線確認（本測定の前に実施）。** N07-Vincent2D / MC-ESO / 3 seed で
**選び直しが PR@1e-1 = 1.00 を再現**（その20 の値）。NMMSO も現行規則で 1.00 を再現。

**(b) 利得は全手法で eps=1e-1 に集中する — MC-ESO 固有ではない**（35 セル = 5 関数 × 7 手法）:

| eps | 改善したセル | 平均利得 | 利得 ≥ +0.05 のセル |
|---|---|---|---|
| **1e-1** | **25/35** | **+0.315** | **19** |
| 1e-2 | 23/35 | +0.090 | 13 |
| 1e-3 | 20/35 | +0.048 | 8 |
| 1e-4 | 16/35 | +0.029 | 4 |
| 1e-5 | 10/35 | +0.019 | 2 |

**N07 では 7/7 手法、N09 では 5/7 手法が PR@1e-1 = 1.00 に張り付く。**

**(a) 順位は動く。ただし動くのは 1e-1 / 1e-2 だけ。**
上位 2 位が動いた関数の数は **1e-1 で 4/5、1e-2 で 2/5、1e-3 / 1e-4 / 1e-5 では 0/5**。

**判定（事前登録どおり）。** (b) が全手法で起きたので、**PR@1e-1 は手法を識別していない**。
**以後の判定は 1e-3 以下で行う**（運用規則として本ファイル冒頭にも書いた）。

**ゴールに対する結論 — 報告規則ではこの差は埋まらない。**
**NMMSO は選び直しから最も利得を受けない手法**（1e-2 で +0.006、1e-3 以下で +0.000）で、
その報告集合は既に選び直し後の集合とほぼ同じ。**最も利得を受けるのは DE**（+0.469 / +0.231 /
+0.167 / +0.134 / +0.104、唯一 1e-5 まで残る）で、DE は最終集団 30 点しか報告していない。
**選び直しは「報告規則が貧しい手法の修理」であって探索の改善ではない。**
`core/runner.py:198` の警告は実測で正しかった。

**機序 — 効くのは被覆律速の地形だけ。** その21 の二分法がそのまま出る:
**1.00 への collapse は Vincent（N07 / N09）でだけ起き、Shubert（N06 / N08）では 1 手法も届かない。**
**N08-Shubert3D は全 5 水準で順位が完全に不変**で、MC-ESO は |rep| が 80 → 162 と倍になっても
全水準で利得ゼロ。**深さが binding な地形では報告集合をどう選び直しても何も出ない。**

**副産物 — 問い 3 が決着した（NM-Restart は Shubert で多スタートになっていない）。**
N06 で報告点 2、**履歴全体から選び直しても 2**。直接数えると **restart 回数 = 1**
（20000 評価を 1 回の Nelder-Mead が使い切る）。N07 は 112 回、N10 は 117 回。
原因は報告側ではなく終了判定で、`nelder_mead.py` の `_XATOL = 1e-12` / `_FATOL = 1e-14` が
厳しすぎて局所最適 760 個の Shubert では simplex が収束しない。
**その15 / その17 の NM-Restart の Shubert 行（N06 / N08）はベースラインとして無効**
（多スタート法ではなく単一の Nelder-Mead を測っている）。Vincent / ModRastrigin の行は有効。

**環境の記録（次の回への注意 — その26 の手順は不完全だった）。**
(i) **`pynmmso` 1.0.1 は Python 3.11 で実行時に落ちる**。`multiprocess` を入れて sdist を
site-packages にコピーするだけでは足りず、**`random.sample(<set>, k)` が 4 箇所ある**
（`pynmmso.py` の 238 / 414 / 440 / 446 行）。Python 3.11 は set を拒否するので
`random.sample(list(...), k)` に書き換える必要がある。その26 は NMMSO を実際には
走らせていなかったと思われる。**site-packages 側のパッチなので、コンテナが変わるたびに要再適用。**
副作用として、set のイテレーション順は object id 依存なので **NMMSO は seed を固定しても
run 間で完全再現しない**。今回の判定は両腕が同一 run を採点するので影響しない。
(ii) **`analysis/niching_baseline.csv`（その15）の MC-ESO 行はこの環境で再現しない**
（N07 の PR@1e-1 が 0.194 → 0.25、|rep| も 63/61/60 → 64/60/59）。
その15 以降の `mceso.py` の変更による drift と思われる。NMMSO 行は再現する。
**両腕が同一 run を採点するので今回の判定には影響しないが、その15 の絶対値を引用するときは注意。**

**次に開くこと。** 報告規則の路線は**全手法で測って閉じた**ので、残りは探索側だけになった。
問い 3 も上記で決着したので削除した。**次は問い 4（Shubert の深さ / 受理規則の 2 段構え）**
— その27 が σ の路線を閉じ、今回が報告規則の路線を閉じたので、
**深さの問題に残っている梃子は受理規則しかない**。今回の N08 の結果
（|rep| 倍増で利得ゼロ）はその問いの動機を独立に裏づけている。

### 2026-09-03 その27（**路線を畳んだ** — 比は 3D では単調なトレードオフ・ダイヤルで、内点に最適が無い。痛みが消える 0.5 は効果も消えている）

問い 1（その26 が残した最後の分岐）。**結論: 「局所盆地間隔スケーリング」は 2D 限定で確定し、
路線ごと畳んだ。** 決着したので問い 1 は削除し、本文は
[acceptance_topology.md の該当節](acceptance_topology.md#局所盆地間隔スケーリングは-2d-限定で確定--3d-では比が単調なトレードオフダイヤルになるその26その27-analysishmn08_csv)
に移した。**MC-ESO の既定値は不変**（`mceso.py` は触っていない）。

**やったこと。** `scripts/diagnose_niching.py` の `_VARIANTS` に `sigma_only_r250` /
`sigma_only_r500` を追加（`sigma_only` と同じ `commit_mode="sigma_only"` /
`commit_sigma_mode="run"` で `commit_sigma_ratio` だけ 0.25 / 0.5）。
N08-Shubert3D、`--evals 40000 --seeds 5 --eps 1e-1,1e-3,1e-5`、**2 本だけ並列**（各 32 分）。
`analysis/hm/n08_sigma_only_r{250,500}.csv`。base / `sigma_only` は既存 CSV を再利用して**再測定していない**。
比較は `scripts/hunt_confound.py --func N08-Shubert3D --ref base`。

**配線確認（本測定の前に実施）。** N06-Shubert2D / 20000 評価 / 2 seed で比を振ると
hunt 数が **43.5 → 38.5 → 35.5**（比 0.1 / 0.25 / 0.5）と単調に動き、つまみが生きていて
かつ**予測どおりの向き**（σ を緩める → hunt が長くなる → 本数が減る）であることを確認した。
また `hunt_confound.py` が その26 の base 対 `sigma_only` の表を**数値完全に再現**することを
確認してから本測定に入っている。

**主表（N08-Shubert3D、5 seed、対応のある Wilcoxon、reference = base）:**

| 構成 | hunts | `blocked` | visited@1e-1 | **PR@1e-1** | 1e-1 の w/t/l | PR@1e-3 | PR@1e-5 | visited@1e-5（seed 別） |
|---|---|---|---|---|---|---|---|---|
| base | 53.6 | 29.0 | 27.6 | **0.34** | — | 0.12 | 0.01 | 1,1,1,1,1 |
| **r500**（比 0.5） | 61.2 | 33.8 | 27.4 | **0.34** | 3/0/2 | 0.14 | 0.01 | **1,1,1,1,1** |
| **r250**（比 0.25） | 66.8 | 47.2 | 22.2 | 0.27 | **0/0/5** | 0.14 | 0.03 | 2,2,4,4,1 |
| `sigma_only`（比 0.1、その26） | 72.6 | 54.6 | 16.2 | 0.20 | **0/0/5** | 0.10 | 0.03 | 1,2,2,3,4 |

**判定。** 事前登録した基準は「どちらかの比で PR@1e-1 が base の 0.34 以上なら路線は生きる、
判定は 5/5 の方向一致で行う」だった。**r500 は平均で 0.34 に到達するが、方向一致は 3/0/2 の
引き分けで 5/5 ではない**（勝ってはいない）。**r250 は 0.27 で 5 seed 全敗。**
よって**基準は満たされず、路線を畳む**。

**その26 より強い結論が取れた理由 — 1 点ではなく曲線を測ったから。**
その26 は比 0.1 の 1 点だけを見て「符号が反転する」と言えたが、**「0.1 が 2D 調律の誤りで、
3D の最適比は大きい側にある」という可能性を排除できていなかった**。今回それを排除した:
**全カラム（hunts / `blocked` / visited / PR）が比について単調で、緩めるほど base に戻る。**
比 → hunt 数 → `blocked` → visited → PR の連鎖が用量反応として読め、
その26 が 1 点から推測した機序（σ を締める → hunt が短く多くなる → 3D では 1 本が
1e-1 にも届かない）は**確認された**。**3D にはダイヤルの内点に最適が無い。**

**核心（新規）— 回復と無効化が同じ設定で起きる。**
痛みが消える唯一の設定（0.5）は、**同時に効果も消えている**設定である。
r500 は PR@1e-1 で base と並ぶが、**深精度側の利得も完全に失う**:
visited@1e-5 が **5 seed すべてで base と同じ 1**。
一方、深精度の利得自体は 3D でも実在する（r250 で visited@1e-5 が 1.0 → 2.6、4/1/0）。
**つまり 2D との違いは「利得が消えた」ことではなく「利得が有償になった」こと。**
2D では深さの利得が粗い水準を一切削らずに得られた（N06 の 1e-1 は 0.82 → 0.82、その23）のに、
3D では**同じ利得が必ず 1e-1 の 5 seed 全敗を連れてくる**。
**次元が上がると深さと広さが再結合し、σ ひとつでは分離できなくなる。**
監査側の「1 つのつまみでは深さと広さを両立しない」は、**2D で破れて見えただけで 3D で元に戻る。**

**副次的な負の結果（測定上、次の回への注意）。**
**N08-Shubert3D の 1e-5 水準は手法比較に使えない。** base は **5 seed すべてでちょうど 1 個**しか
1e-5 に到達しない（PR = 1/81 = 0.01）。K=81 に対して絶対値が小さすぎるので、
この水準での比較は **±1〜3 解の揺れを見ているにすぎない**。**3D の判定は 1e-1 / 1e-3 で行う。**
（その26 が `sigma_only` の 1e-5 を「4/1/0 だが絶対値 0.01 → 0.03 で実質無意味」と書いたのは
正しかったが、**理由は絶対値の小ささではなく base 側が床に張り付いていること**である。）

**次に開くこと。** σ 側の梃子は 2D 限定として閉じたので、**深さの問題は受理規則側に戻る**
（問い 4）。今回の結果はその問いの動機を**強める**: 単一のつまみで深さと広さが両立しないことが
3D で再確認されたので、**位相を分ける** 2 段構え（σ が枯れるまで親比較、枯れた後だけ最近傍置換）は
σ の大小では届かない場所を狙っている。問い 4 に判定水準の注意（3D は 1e-1 / 1e-3）を書き足した。

### 2026-09-03 その26（**取り込みは却下** — N08-Shubert3D で符号が反転する。BBOB では変種が一度も発火せず MC-ESO と全 24 関数ビット一致なので (c) は無害だが**情報も無い**）

問い 1 の (b) と (c)。**判定は「本体に取り込まない」。** MC-ESO の既定値は不変。

**(b) N08-Shubert3D — 符号が反転した（棄却条件が成立）。**
`diagnose_niching.py --evals 40000 --seeds 5 --eps 1e-1,1e-3,1e-5`、base と `sigma_only` の
2 本だけを並列（各 14-34 分）。`analysis/hm/n08_{base,sigma_only}.csv`、
比較は `scripts/hunt_confound.py --func N08-Shubert3D --ref base`。

| eps | base | `sigma_only` | w/t/l | p | A12 |
|---|---|---|---|---|---|
| **1e-1** | **0.34** | **0.20** | **0/0/5** | 0.0625 | **0.00** |
| 1e-3 | 0.12 | 0.10 | 1/1/3 | 0.50 | 0.30 |
| 1e-5 | 0.01 | 0.03 | 4/1/0 | 0.125 | 0.90 |

**n=5 では両側 p の下限が 0.0625 なので、どの行も α=0.05 には届かない。**
それでも 1e-1 の **5 seed 全敗**は n=5 で取れる最強の証拠で、
**2D では「粗い水準を一切削らない」ことが `sigma_only` の売りだった**（N06 で 0.82 → 0.82、
その23・その25）のに、**3D ではそこが −0.14 で全敗する**。**方向が次元で反転している。**
深精度側（1e-5）の方向は 2D と同じ（4/1/0）だが、絶対値が 0.01 → 0.03 で実質無意味。

**機序（診断カラムから直接読める）。** `sigma_only` は hunt を増やすが 1 本 1 本を浅くする:

| 構成 | hunts | distinct | \|rep\| | **blocked** | visited@1e-1 |
|---|---|---|---|---|---|
| base | 53.6 | 56.6 | 80 | **29.0** | **27.6** |
| `sigma_only` | 72.6 | 70.8 | 99 | **54.6** | **16.2** |

**相異なる niche は 56.6 → 70.8 と増えているのに、eps を満たさない `blocked` が 29.0 → 54.6 と
ほぼ倍**になり、visited は 27.6 → 16.2 に落ちる。σ を `0.1·d_local` に締めると σ が底を打つのが
早く hunt が短くなる。2D ではその短さが深さに変換されたが、**3D は盆地を降り切るのに
より多くの評価が要るので、短い hunt は 1e-1 にすら届かず、増えた hunt は浅い点で空回りする。**
その22 (b) で見た「新しい niche が浅い点で埋まる」現象と同じ形。

**(c) BBOB-24 dim2 / n=20 — 全 24 関数で MC-ESO と数値完全一致。**
`quick_check.py` の `_OPTIMIZERS` に `sigma_only` エントリを追加（本体 `mceso.py` は不変）、
`./run.sh quick --all --n-runs 20 --methods MC-ESO,sigma_only`。
`analysis/hm/bbob24_q1c_{summary,wilcoxon}.csv`。
**wilcoxon.csv は 24 行すべて `tie_count=20`**（勝敗ゼロ）で、summary の SR も evals も全関数一致。

**これは「安全」の証拠ではなく「発火していない」の証拠である。**
`commit_sigma` は掘れた盆地が 3 個以上ないと base の経路に落ちるので、
**BBOB dim2 / 5000 評価では枯渇時の盆地切替がこの条件下で一度も走っていない。**
つまり (c) は**恒等チェックとしては通ったが、SR@1e-10 に関する情報を何も与えていない**。
取り込みの可否は (b) だけで決まる。

**参考値の食い違い（次の回への注意）。** この run の MC-ESO は
**SR@1e-10 = 92.08%（24 関数平均）/ evals_succ_mean 677.7（F24 は inf なので 23 関数平均）**で、
規則が pin している **93.5% / 798** とは一致しなかった。両腕がビット一致なので今回の判定には
影響しないが、**pin の数値がこの環境で再現しない**ことは記録しておく
（n=20 の seed 差か、pin 側の集計方法が違うかは未確認）。

**環境（その25 の手順を更新）。** `pynmmso` は sdist の `setup.py` を編集しなくても、
**`pip install multiprocess` を入れてから sdist の `pynmmso/` を site-packages にコピーするだけで動く**
（落ちていたのは `setup.py` ではなく依存 `multiprocess` の欠落だった）。
`matplotlib` も要る（`core/visualize.py` が無条件に import するので `quick_check.py` が動かない）。
`pip install ioh cma numpy scipy multiprocess matplotlib` ＋ 上記コピーで全部通る。

**次に開くこと（問い 1 を書き直した）。** 「σ を局所盆地間隔に合わせる」は
**2D 限定の梃子**として確定し、本体には入らない。残る分岐は
**失敗したのが比 0.1 なのか、考え方そのものなのか** — 比 0.1 は 2D で調律された値で、
3D では盆地が spacing に対して相対的に広いので最適比が大きい側にある可能性がある。
N08 で `commit_sigma_ratio` を 0.25 / 0.5 に振れば 1 回で白黒つく（問い 1 に書いた）。

### 2026-09-03 その25（交絡は晴れた — `sigma_only` の利得は hunt 数ではない。予算を 40% 増やし hunt 数を追い越させた base でも届かない）

問い 1 の (a)。**交絡テストは通った**。以下すべて MC-ESO の既定値は不変。

**まず、問いが指定していた対照（`--variant fast`）は使えない。**
`fast` は `hunt_no_improve_mult=0.5` を渡すが、これは `mceso.py:392` の**既定値そのもの**なので
`base` と完全に同一の変種だった（その24 が対照として書いたのは誤り）。
代わりに解放規則の 2 つのつまみを振る変種を `diagnose_niching.py` に足して掃引したが、
**どちらも hunt 数を動かさなかった**（N07、3 seed、20000 評価）:

| つまみ | 振った範囲 | hunt 数 |
|---|---|---|
| `hunt_no_improve_mult` | 0.35 / 0.25 / 0.15 / 0.05 | **35.0 / 35.0 / 35.0 / 35.0**（全行が数値完全一致） |
| `exhausted_sigma_tol` | 3 / 6 / 12 / 25 | 35.7 / 36.0 / 36.0 / 36.3 |

**機序**: Vincent は大域最適が全て同じ高さなので、hunt は σ 下限ではなく
**`hunt_level_tol` の経路で終わる**（`mceso.py:919-928`）。σ 下限の判定を緩めても
level_tol が先に発火するので効かず、停滞窓を縮めても no_improve は既に閾値を越えている。
**base の hunt 数は解放規則では上げられない。**

**そこで対照を予算に取った。** base に評価回数を渡して hunt 数が `sigma_only` に並ぶまで増やし、
そこで PR を読む。**base に評価を余分に与えるので帰無仮説に有利な（保守的な）対照**である。
base @ 28000 評価、15 seed、seed 列は既存の 15 seed 測定と同一（`seed*100`）。
`analysis/hm/base28k_n0{6,7}.csv`、解析は新規の `scripts/hunt_confound.py`（対応のある Wilcoxon）。

**N07-Vincent2D（15 seed、reference = `sigma_only` @ 20000）— hunt 数で追い越しても届かない:**

| 構成 | 評価 | hunts | PR@1e-1 | PR@1e-3 | PR@1e-5 | 1e-3 の w/t/l | p | A12 |
|---|---|---|---|---|---|---|---|---|
| base | 20000 | 35.5 | 0.26 | 0.22 | 0.22 | | | |
| **`sigma_only`** | 20000 | 49.1 | 0.37 | **0.34** | 0.33 | — | — | — |
| **base（対照）** | **28000** | **51.5** | 0.32 | **0.26** | 0.26 | **0/0/15** | **0.0006** | **0.00** |

**base は hunt 数で 51.5 対 49.1 と追い越し、評価回数も 40% 多いのに、
PR@1e-3 は 15 seed 全部で `sigma_only` に負ける**（1e-5 でも 0/1/14、p = 0.0007）。
**利得は hunt 数では説明されない。**

**N06-Shubert2D（15 seed、hunt 数はほぼ完全一致 43.3 対 43.9）— 精度水準で役割が割れる:**

| 構成 | 評価 | hunts | PR@1e-1 | PR@1e-3 | **PR@1e-5** | 1e-5 の w/t/l（対 `sigma_only`） | p |
|---|---|---|---|---|---|---|---|
| base | 20000 | 29.5 | 0.82 | 0.82 | 0.17 | | |
| **`sigma_only`** | 20000 | 43.9 | 0.82 | 0.81 | **0.39** | — | — |
| base（対照） | 28000 | 43.3 | **0.92** | **0.91** | 0.22 | **0/4/11** | **0.0017** |

深精度側は N07 と同じ結論で、**hunt 数を揃えた base（しかも予算 40% 増）でも 0.22 止まりで
`sigma_only` の 0.39 に届かない**（15 seed 中 11 敗 4 分 0 勝）。

**副次的な負の結果（新規、これは `sigma_only` の弱点）。**
Shubert の**粗い水準（1e-1 / 1e-3）は予算律速**だった。base に 8000 評価足すだけで
0.82 → 0.92 に上がる（13/2/0、p = 0.0008）のに、`sigma_only` は同じ水準を
**0.82 → 0.82 と一切動かさない**（6/2/7、p = 0.80）。
つまり **σ のつまみと予算は別々の水準に効いている**: σ は深精度側だけ、予算は粗い側だけ。
`sigma_only` は「無償」ではあるが「万能」ではなく、**Shubert の粗い水準には何も足さない**。
等予算では削らないので採用の障害にはならないが、**σ を予算の代わりに使えると読んではいけない。**

**環境の記録（次の回への注意）。**
(i) **`pip install -r requirements.txt` は `pynmmso` のビルドで落ちる**（`setup.py` の
`test_suite='nose.collector'` が setuptools の廃止経路を踏む）。`core/optimizers/__init__.py` が
無条件に import するので**全スクリプトが動かなくなる**。回避策は sdist を展開して
`test_suite=` / `tests_require=` の 2 行を消し、`pynmmso` パッケージを site-packages に直接コピーする
（`pip download --no-deps --no-binary :all: pynmmso` → 展開 → `cp -r pynmmso $(python3 -c
'import site;print(site.getsitepackages()[0])')`）。他の依存（ioh / cma / numpy / scipy）は
`pip install ioh cma numpy scipy` で入る。
(ii) **このコンテナは 4 コア**。5 本並列で流したところ後処理が詰まり、40 分の枠内に
N08 と 40000 評価の run が終わらなかった。**同時に回すのは 3 本まで。**

**まとめ（問い 1 の 3 条件）**: (a) **通った**（交絡は晴れた、両関数とも）。
(b) N08-Shubert3D は**未了** — 40000 評価 × 5 seed を回したがコンテナ再起動で落ちた（約 12 分経過時点）。
(c) BBOB-24 dim2 の SR@1e-10 は**未着手**。

**次の回への正確な続き（問い 1 として書き直した）。** 残りは (b) と (c) だけ。
(b) は `python3 scripts/diagnose_niching.py --evals 40000 --seeds 5 --eps 1e-1,1e-3,1e-5
--variant sigma_only --funcs N08-Shubert3D --csv analysis/hm/n08_sigma_only.csv` と
同じものを `--variant base` で 1 本、**この 2 本だけを並列で**（N08 は 3D で重い。
1 本あたり 15 分前後を見込む。他の run と同時に回さない）。
比較は `python3 scripts/hunt_confound.py --func N08-Shubert3D --ref base ...`。
(c) は `quick_check.py` の `_OPTIMIZERS` に一時エントリ
`("sigma_only", CommitReseedMCESO, {"commit_mode":"sigma_only", "commit_sigma_mode":"run",
"commit_sigma_ratio":0.1})` を足し、`./run.sh quick --all --n-runs 20 --methods MC-ESO,sigma_only`。
**本体（`mceso.py`）は触らない。**

### 2026-09-02 その24（統合の回 — 多峰テーマの結論を `acceptance_topology.md` に集約し、ログを 3 件に畳んだ）

**新規測定なし**（手順 2: ログ 9 件 > 8 件）。並行回との衝突を避けるため、統合そのものを
claim してから push し、それから作業した（その18 と同じ運用）。依存の導入
（`pip install -r requirements.txt`）はしていない。**MC-ESO の既定値は変更していない。**

**やったこと 1 — 多峰テーマの本文が初めて `research_loop.md` の外に出た。**
`acceptance_topology.md` は監査テーマの記録として閉じており（末尾に終了節がある）、
2026-09-02 に新ゴールへ切り替えてから その15〜その23 の結論は**ログにしか無かった**。
ログを畳むと失われるところだったので、同ファイルに新しい最上位節
**「多峰テーマ（peak ratio）の確定事項」**（7 節）を起こして移した:

| 移した節 | 中身 |
|---|---|
| 出発点の順位表 | その15 の 7 手法表。差はほぼ Vincent だけで作られている |
| PR は evals/K でも K でも決まらない | その17 の予算応答表。r3pso は完全飽和、NMMSO だけが高 K でも変換し続ける |
| 報告集合の天井 | その19。`\|rep\| − spillover = 26.0` ちょうど。天井が binding なのは Vincent だけ |
| 選び直しは visited の天井にぴったり届く | その20。45 行すべてで `resel == visited`、利得は eps=1e-1 に集中。公平性の留保つき |
| reported ≈ min(distinct hunts, deep hunts) | その21。Vincent は被覆律速・Shubert は深さ律速。一様描画 対 対数間隔の機序と hit 表 |
| 反発半径の適応は効かない | その22。描画は既に 36/36、着地は 11-15。律速は best-of-20 の選抜 |
| σ を局所盆地間隔に合わせる | その23。現時点で唯一確定した梃子。分解（+0.12 σ / +0.10 commit）と留保 3 つ |
| 多峰テーマの測定上の教訓 | eps 1 水準で visited を判断しない / 解析解は箱内 K 個を assert / N09 は並列 2 まで / 無効化した変種の数値一致を毎回確認 |

**やったこと 2 — ログを 3 件（その24 / その23 / その22）＋ 畳んだ内訳の表に切り詰めた。**
その15〜その21 は本文を上記に移したので、ログ側には「どこへ移したか」の対応表だけを残した。
その23 / その22 を残したのは、問い 1 がこの 2 件から直接続いているため。

**やったこと 3 — 問いの整理。**
(i) **問い 3 を縮めた**。MC-ESO についての 3 分岐の切り分けはその19〜その21 で決着しているので、
残りの **r3pso / NCDE の飽和**だけにし、「他手法の負け方が分かってもこちらの PR は上がらない」
という理由で優先度が低いことを明記した。
(ii) **問い 5（Shubert の深さ）に着手前の確認を足した**。その23 の `sigma_only` は
N06 の PR@1e-5 を 0.17 → 0.39 に上げており（15 seed、13/15、p = 0.0014）、
これが問い 1 (a) の交絡テストを通れば**受理規則に触らずに半分片付いている**ことになる。
**問い 1 の前に問い 5 を始めると、同じ利得を二重に測る。**
(iii) **「畳んだ問い」に多峰テーマの棄却 2 件を追記した**（evals/K 仮説、反発半径の適応）。
同じ検証の再実施を防ぐのがこの欄の役目なので、棄却の理由と数値も 1 行ずつ入れてある。
(iv) 冒頭の「分かっている手がかり」に、hunt 律速の二分法・予算応答・報告枠が制約でないこと
の 3 行を足した。**この欄は見出しだけにして、数値は集約先を見る運用に統一した。**

**次の回は通常どおり問い 1 から**（統合の回ではない）。claim して測る。
問い 1 の (a) 交絡テストが最初の一手で、対照は `--variant fast`（`hunt_no_improve_mult` を下げて
base の hunt 数を 49 に揃える）。**hunt 数を揃えた base が N07 で 0.34 に届くなら σ の寄与は無い。**

### 2026-09-02 その23（撤退条件は満たさない（0.44 < 0.50）が、N07 の PR は 2 倍になった。効いているのは「1 本への commit」ではなく**再起動後の σ を盆地スケールに落とすこと**で、それは Shubert では無償）

問い 1。**事前に書いた判定（N07 の PR@1e-3 が 0.50 以上）は満たさなかった**（最良 0.44）。
だが**その22 までのどの介入より桁違いに大きく動き**（base 0.22 → 0.44、15 seed 中 15 seed で改善、
A12 = 1.00、p = 0.0006）、しかも**機序が分解できた**ので、棄却ではなく「効くつまみが 1 つ確定した」
という扱いにする。以下すべて MC-ESO の既定値は不変（診断用は別ファイル）。

**やったこと。** `core/optimizers/mceso_commit_reseed.py` に `CommitReseedMCESO` を新規作成
（`AdaptiveRepelMCESO` を継承して `_adaptive_radii` の幾何を再利用）。枯渇時の盆地切替で:

- **anchor**: その event の最初の `_diversified_reseed` 呼び出しの描画を anchor とし、
  残り 19 スロットは全部その周りに置く（**20 本の独立描画をやめる**）。
- **σ_place**: 重複潰し済みの `ir_basin_centroids` のうち anchor に最も近いものの
  最近傍距離 `d_local` に対し `commit_sigma_ratio × d_local`（追加評価ゼロ、`[0.005, 0.2]×span` で clip）。
  盆地が 3 個未満しか掘れていない間は base の経路に落ちる。
- `commit_sigma_mode`: `place`（配置だけ）/ `run`（再起動後の `st.sigma` もこの値にする）。
- **恒等チェック済み**: `commit_off` は base と数値完全一致（patch 後にも再確認）。

`scripts/diagnose_niching.py` の `_VARIANTS` に追加、`scripts/reseed_to_landing.py` に
`commit_tight` を追加。N06-Shubert2D（対照）+ N07-Vincent2D、20000 評価、**15 seed**、
eps 3 水準。`analysis/commit_2d_*.csv`（5 seed 掃引）、`analysis/commit15_*.csv`（15 seed 本測定）。

**主表（N07-Vincent2D、15 seed、対応のある Wilcoxon、reference = base）:**

| 構成 | PR@1e-1 | **PR@1e-3** | PR@1e-5 | w/t/l | p | A12 |
|---|---|---|---|---|---|---|
| base | 0.26 | **0.22** | 0.22 | — | — | — |
| その22 の adaptive（参考、5 seed） | 0.36 | 0.27 | 0.27 | | | |
| `sigma_only`（**σ だけ**、描画は base のまま） | 0.37 | **0.34** | 0.33 | 15/0/0 | 0.0006 | 1.00 |
| `commit_sigma`（commit、σ = 0.25·d_local） | 0.36 | 0.34 | 0.34 | 15/0/0 | 0.0006 | 0.99 |
| **`commit_tight`（commit、σ = 0.1·d_local）** | **0.46** | **0.44** | **0.43** | **15/0/0** | **0.0006** | **1.00** |

**N06-Shubert2D（対照、15 seed）— ここで 2 つの構成の性格が割れる:**

| 構成 | PR@1e-1 | PR@1e-3 | PR@1e-5 | 1e-5 の w/t/l | 1e-5 の p |
|---|---|---|---|---|---|
| base | 0.82 | 0.82 | 0.17 | — | — |
| **`sigma_only`** | 0.82 | 0.81 | **0.39** | **13/2/0** | **0.0014** |
| `commit_tight` | **0.76** | **0.74** | 0.26 | 10/2/3 | 0.0300 |

**機序 — 効いているのは commit ではなく σ。ただし commit は Vincent で上乗せする。**
`sigma_only` は **描画を base のまま**（20 本の独立描画も best-of-20 の競争もそのまま）にして
**再起動後の σ だけ** `0.1·d_local` に落とす対照で、これだけで N07 は 0.22 → 0.34 動く。
commit を足すと 0.34 → 0.44 でさらに上乗せされる。つまり **+0.12 が σ、+0.10 が commit**。
その22 は「描画規則も σ_init も着地を動かさない」と結論したが、そこで振ったのは
**σ_init（箱に対する固定比）**で、今回振ったのは **その時点で観測された局所盆地間隔に対する比**。
Vincent の盆地は対数間隔なので、この 2 つは同じつまみではない。

**中間指標（問いが指定したもの、`scripts/reseed_to_landing.py`、3 seed プール）:**

| 構成 | 描画が触れた相異なる最適解 | **着地した相異なる最適解** | hunt 数 |
|---|---|---|---|
| base | 36 / 36 | **11 / 36** | 102 |
| commit_tight | 33 / 36 *(注)* | **26 / 36** | 146 |

*(注)* commit では tracer が記録する「描画」は anchor ＋ その周りの雲なので、
**base の 36/36 とは別の量**（独立描画の被覆ではなく、commit した配置の被覆）。
比較できるのは着地側だけ。**11 → 26 で、その22 が指した「描画から着地までの落ち込み」は実際に埋まった。**

**報告の水増しではない。** `visited@1e-3`（履歴が eps 以内で触れた相異なる最適解、報告規則に依存しない）が
N07 で **12.0 → 17.3**（commit_tight、14/15 seed で増、p = 0.0010）。
**本当に多くの盆地を降り切っている。**

**負の結果・留保 3 つ。**
(a) **σ 比は単調ではない。** `commit_sigma_ratio` を 0.25 / 0.10 / 0.05 / 0.02 / 0.01 と振ると
N07 の PR@1e-3 は 0.37 / **0.46** / 0.37 / 0.38 / 0.38（5 seed）で **0.10 に山がある**。
締めすぎると盆地に入る確率自体が落ちる。**「commit すればよい」ではなく「盆地間隔の 1/10 で commit する」。**
(b) **commit は Shubert の粗い水準を有意に削る**（1e-1 / 1e-3 で −0.07、11/15 seed で悪化、p ≈ 0.03）。
一方 `sigma_only` は **N06 の 1e-1 / 1e-3 を一切削らずに 1e-5 を 0.17 → 0.39 に上げる**。
監査側で確定していた「1 つのつまみで深さと広さは両立しない」に対し、
**σ を局所盆地スケールにするのは（この 2 関数では）両立している側**に見える。
(c) **hunt 数が交絡している。** σ を落とすと σ が底を打つのが早く、hunt が
N07 で 35.5 → 49.1（sigma_only）/ 49.3（commit_tight）、N06 で 29.5 → 43.9 に増える。
sigma_only の base に対する利得はこの増加と分離できていない。
ただし sigma_only と commit_tight は **hunt 数がほぼ同じ**（49.1 対 49.3）なので、
**両者の差 0.34 対 0.44 は hunt 数では説明されない**（commit の寄与は交絡していない）。

**測っていないもの（次の回への引き継ぎ）。** 3D（N08 / N09）は測っていない。
問いの指示どおり N09 は天井 0.29 で識別力が無いので落とし、時間を N07/N06 の
**seed を 3 → 15 に増やす方**に使った（そのぶん検定力が上がり、15/15 の一致が取れた）。
N08-Shubert3D は測る価値があるが今回は入れていない。
**BBOB-24 dim2 の SR@1e-10 は未確認**（本体を変えていないので不要だが、`sigma_only` を
本体に取り込むなら必須）。

**次に開くこと（問い 1 として書き直した）。** 梃子は「再起動後の σ を、箱ではなく
**その時点で観測された局所盆地間隔**に合わせること」で確定した。次は
(i) hunt 数の交絡を潰す（base の hunt 数を揃えて σ の効果だけを取り出す）、
(ii) `sigma_only` を N08 と BBOB-24 dim2 に当てて、**無償に見えるのが本当か**を確かめる。

### 2026-09-02 その22（棄却 — 反発半径を盆地スケールに直しても PR は動かない。描画は既に全 36 解を覆っており、律速は「20 本の独立描画から best-of-20 を選ぶ」選抜側）

問い 1。**棄却条件が成立した**。その21 が指した「一様描画 ＋ 固定反発半径 0.02×span」を
盆地スケール適応に置き換えたが、**N07 の PR@1e-3 は 0.24 → 0.27** で、問いが先に書いた
撤退条件（**0.5 以上に動かなければ被覆仮説は棄却**）を満たさない。

**やったこと。** 本体は書き換えず `core/optimizers/mceso_adaptive_repel.py` に診断用の
`AdaptiveRepelMCESO` を置いた（`_diversified_reseed` の枯渇時経路だけを override）。
反発半径は追加評価ゼロで観測できる量から決める: (1) `ir_basin_centroids` を
0.02×span で重複潰し（この固定半径は**被覆半径としては役立たないが重複判定としては正しい** —
両地形の最小解間隔より小さい。同一盆地への再着地が自分の最近傍距離を 0 にするのを防ぐ）、
(2) 生き残った centroid ごとに最近傍距離 d_i、(3) `r_i = clip(0.5·d_i, 0.02·span, 0.25·span)`。
α=0.5 なので隣接する 2 つの掘削済み盆地の排除球はちょうど接する（自分の盆地は覆い、
隣は塞がない）。描画側は 2 通り測った: `adaptive`（棄却再描画。半径変更だけを切り出す）と
`adaptive_maxmin`（同じ描画本数で `min_i ||c_i − x|| / r_i` を最大化する候補を選ぶ）。
`repel_mode="off"` が base と厳密一致することを確認済み（恒等チェック）。
`scripts/diagnose_niching.py` の `--variant` を（クラス, kwargs）表に変え、計数用の
override を mixin（`_HuntCounters`）に切り出したので base と変種が同じ形で dump される。
3 変種 × {N06 対照, N07 決定的, N09} × 3 seed、その21 と同じ予算（2D 2 万 / 3D 4 万）。
`analysis/repel_2d_*.csv`、`analysis/repel_hunts_2d_*.csv`。

| 関数 | 指標 | base | adaptive | 判定 |
|---|---|---|---|---|
| **N07-Vincent2D** | **PR@1e-3** | **0.24** | **0.27** | **棄却**（要 0.50） |
| N07-Vincent2D | PR@1e-1 | 0.25 | 0.36 | |
| N07-Vincent2D | PR@1e-5 | 0.24 | 0.27 | |
| N07-Vincent2D | **distinct hunt** | **8.7** | **9.7** | ほぼ不動 |
| N06-Shubert2D（対照） | PR@1e-1 / 1e-3 | 0.85 / 0.85 | 0.80 / 0.80 | 微減 |
| N06-Shubert2D（対照） | PR@1e-5 | 0.17 | **0.31** | 増（3 seed、要追試） |

**機序 — 描画は既に全 36 解を覆っている。落ちているのは描画から着地までの間。**
`scripts/reseed_to_landing.py` を新規作成し、hunt ごとに全ての diversified 描画と
その hunt の終端を Vincent の真の最適解に最近傍で写像した（3 seed、102-130 hunt）:

| 構成 | 描画が触れた相異なる最適解 | **着地した相異なる最適解** | 着地先の解間隔（中央値） | 描画先の解間隔（中央値） |
|---|---|---|---|---|
| base（σ=0.2） | **36 / 36** | **11 / 36** | 1.918 | 1.023 |
| adaptive（σ=0.2） | **36 / 36** | 14 / 36 | 1.918 | 1.023 |
| adaptive（σ=0.05） | **36 / 36** | 15 / 36 | 1.918 | 1.023 |

**描画側は 3 構成とも全解を覆い、着地は 11-15 解にしか行かない。**
着地先の解間隔の中央値は 1.918、描画先は 1.023 で、**3 構成すべてで同一**。
つまり広い盆地への偏りは**描画でも σ でも生成されていない**。

**律速は選抜だった。** 盆地切替は `n_pop = max(20, 4·dim) = 20` スロット全部を
再描画する（`div_ratio = 1.0`）ので、**1 回の再起動につき互いに独立な反発描画が 20 本**あり、
そのあと μ+λ 貪欲が **best-of-20 を 1 つ選ぶ**。20 本は（実測どおり）多くの盆地に散るが、
勝つのは系統的に広い盆地の点である。**描画の分布をいくら直しても、20 本の競争と
best-of-20 の選抜が下流にある限り被覆には変換されない。**
σ_init = 0.2×span = 1.95 を 0.05×span = 0.488（解間隔の中央値 1.023 より小さい）に
落としても着地は 14 → 15 にしかならず、**σ 単独でも説明できない**ことを確認した。

**副次的な負の結果 2 つ。**
(a) `adaptive` と `adaptive_maxmin` は**全行で数値が完全一致**した。20 回の試行内に
必ず受理される候補が見つかるため max-min 経路が一度も発火しない（両者は同じ RNG 列で
同じ点を返す）。**箱が混んでいない限り描画規則の作り込みは無意味**。
(b) 報告集合側の distinct は 9.0 → 16.3 と増えたのに `blocked` が 0.3 → 6.7 に増え、
PR は動かなかった。**新しく報告された niche は eps を満たさない浅い点で埋まっている。**

**測定の欠損（次の回への引き継ぎ）。** N09-Vincent3D は 3 変種を 3 並列で回して
**OOM でワーカーごと落ち**、eps=1e-1 の行だけが stdout に残った（base 0.12 / adaptive 0.13 /
adaptive_maxmin 0.13、CSV は未生成）。base のこの行はその21 と厳密一致（216.0 / 27.0 / 38.3 /
88 / 62.0）なので同じ run を見ている。**N09 を 4 万評価 × 3 seed で回すときは並列を 2 までに
すること**（3 並列は落ちる）。ただし N09 は天井 0.29 で識別力が無く、判定は N07 で足りるので、
再測定は必須ではない。

**次に開くこと（問い 1 として書き直した）。** 梃子は描画でも σ でもなく**再起動の選抜**。
20 本の独立描画から 1 本を選ぶのをやめ、**1 本の反発描画に集団ごと commit する**
（全スロットをその 1 点の周りに、局所の盆地間隔にスケールした σ で置く）。そうすれば
描画が既に持っている被覆が選抜で潰されない。判定は同じく N07 の PR@1e-3 が 0.24 → 0.5 以上。

MC-ESO の既定値は変更していない（変種は別ファイル、`--variant base` は従来どおり）。

### 畳んだログ（その15〜その21、2026-09-02 その24 で統合）

結論は全て [acceptance_topology.md の「多峰テーマ（peak ratio）の確定事項」](acceptance_topology.md#多峰テーマpeak-ratioの確定事項)
に移した（同ファイル末尾の 7 節）。ログ側の内訳と、移した先の節名:

| ログ | 内容 | 移した先の節 |
|---|---|---|
| その15 | 出発点の順位表（MC-ESO は 2 位、差はほぼ Vincent だけで作られている） | 出発点の順位表 |
| その16 | 監査テーマ最後の測定（全滅の機序は次元不変） | 監査側の該当節に既収録。引き継いだ 1 点は問い 1 の予測に埋め込み済み |
| その17 | PR は evals/K でも K でも決まらない（分けているのは予算応答） | PR は evals/K でも K でも決まらない |
| その18 | 前回の統合の回（監査ログを畳んだ） | 成果は `acceptance_topology.md` 側にある |
| その19 | 報告集合の天井（再起動 1 回につき 1 点しか書かない） | 報告集合の天井 |
| その20 | 履歴からの選び直し（visited の天井に届くが利得は eps=1e-1 だけ） | 選び直しは visited の天井にぴったり届く |
| その21 | hunt 単位の分解（reported ≈ min(distinct, deep)、Vincent は被覆律速・Shubert は深さ律速） | reported ≈ min(distinct hunts, deep hunts) |

測定上の教訓（eps 1 水準で visited を判断しない / 解析解は箱内 K 個を assert / N09 は並列 2 まで /
無効化した変種が base と数値一致することを毎回確認）は
「多峰テーマの測定上の教訓」節にまとめた。
