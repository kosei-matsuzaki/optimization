# 研究ループ（1 時間ごと 1 サイクル）

自動実行される 1 時間ごとの研究サイクルの手順書と、その状態。**このファイルが唯一の引き継ぎ**で、各回はここを読んで続きから始め、ここに結果を書いて終わる。

現在のテーマは**多峰度の高い問題での peak ratio の改善**（下記ゴール）。前テーマ「受容集合と多様解の監査」は 2026-09-02 に新規測定を止め、全記録を [acceptance_topology.md](acceptance_topology.md) に集約した（同ファイル末尾の終了節に、確定した 7 点と新ゴールへ引き継いだ 1 点がある）。MC-ESO 側は [mceso.md](mceso.md) / [history.md](history.md)、分野調査は [related_work.md](related_work.md)。

---

## この研究のゴール（毎回、着手する問いをここに照らすこと）

**多峰度の高い問題（大域最適が多い問題）で、既存手法より多くの大域最適を見つける。**

分野の系統的レビュー（[Algorithms 17(10):448, 2024](https://doi.org/10.3390/a17100448)）が
未解決課題として「次元と多峰度が高いときのスケーラビリティ」を挙げ、今後の方向として
"space and time-scalable methods" を名指ししている。実測でも headroom はここに集中する:
K=36 の N07 では最良手法が PR 0.97 に届くのに、K=216 の N09 では 0.49、他手法はほぼ 0.25 未満。

- **主指標**: CEC2013 niching の peak ratio（精度 ε = 1e-1 〜 1e-5）。**特に K の大きい関数**
  （N06 K=18 / N07 K=36 / N08 K=81 / N09 K=216）。
- **この研究における「多様性」の定義**: 精度 ε 以内で発見された相異なる大域最適の数。
  分野の標準定義を採り、自作の定義は使わない。
- **比較対象**: NMMSO / NCDE / Crowding-DE / r3pso / NM-Restart / MC-ESO。現状の首位は NMMSO。
- **手法は自由**。MC-ESO を改善する必要はない。比較対象の 1 つとして残すだけ。
  ただし MC-ESO 自体を壊さない（BBOB-24 dim2 の SR@1e-10 93.5% は既存資産として保つ）。

**問いを選ぶときの条件**: その問いが片付くと、K の大きい関数の peak ratio が上がるか、
上がらない方向を確実に潰せるか。どちらでもない問いは、面白くても後回しにする。

**占有されている領域**（実装前に必ず確認したこと）:

- **報告規則を独立した設計対象にすること**は済み。[Peak Identification in Evolutionary
  Multimodal Optimization](https://pmc.ncbi.nlm.nih.gov/articles/PMC11505590/) が探索と選別を
  2 相に分け、niche 半径不要の HVPI と冗長解を罰する F 値を提案している。
- **報告効率を測ること**も済み。GECCO competition の Dynamic F1（報告解のうち相異なる
  大域最適だった割合の、評価回数に沿った積分）。
- **持続性による盆地分解**は [arXiv 2605.18351](https://arxiv.org/pdf/2605.18351) が占有。

**分かっている手がかり**（過去の測定から。詳細は [acceptance_topology.md](acceptance_topology.md)）:

- **受理規則が深さと広さを同時に決めている。** 親比較なら深さ、最近傍置換なら広さ。
  1 つのつまみでは両立しない（MC-ESO で SR@1e-10 100% → 0%、DE / PSO でも同傾向）。
- **効くのは返す個数ではなく、上位が互いに別物であること。**
- **MC-ESO はどちらの地形でも「盆地は見つけるが掘り切らない」**（その20 で統一された）。
  Shubert は PR@1e-1 0.80 → PR@1e-5 0.15。Vincent は報告規則のせいで見えにくかっただけで、
  履歴を見れば N09 の 216 個すべてに 1e-1 まで近づいており、1e-3 まで降りるのは約 28 個。
  **かつて「Vincent はそもそも見つけない」と書いていたのは誤り**（報告集合しか見ていなかった）。
- **再起動の被覆を決めているのは描画ではなく選抜**（その22）。盆地切替は 20 スロット全部を
  独立に反発描画するので描画は全 36 解を覆うが、μ+λ 貪欲の best-of-20 が系統的に広い盆地の
  点を選ぶため着地は 11-15 解に留まる。**描画規則や σ_init（箱に対する固定比）を変えても
  この偏りは動かない。**
- **ただし「σ を局所盆地間隔に合わせる」は別のつまみで、これは効く**（その23）。
  再起動後の σ を `0.1 × d_local`（観測済み盆地重心の最近傍距離、追加評価ゼロ）にすると
  N07 の PR は全 eps で 0.22 → 0.34（15/15 seed）、さらに 20 本の描画を 1 本に commit すると
  0.44。着地した相異なる最適解は 11/36 → 26/36。**箱に対する比と局所盆地間隔に対する比を
  混同しないこと** — その22 が振ったのは前者、効いたのは後者。

---

## 1 サイクルの手順

**このループは 1 時間ごとに走る。前の回がまだ走っている可能性があるので、手順 0 と 1 を飛ばすと二重着手になる。**

0. **ブランチを合わせる** — `git fetch origin && git checkout research-loop && git pull --rebase origin research-loop`（無ければ main から作る）。**成果は全部この 1 本に積む。** 実行ごとにブランチを切らない。切ると前回の続きから進めなくなり、統合されない成果物が散る。main には触らない（人がマージする）。
1. **問いを 1 つ claim する** — 「未解決の問い」の上から、まだ claim されていないものを 1 つ選び、その行に `(claimed YYYY-MM-DD HH:MM UTC)` と書いて**即コミット・push する**。これで他の回は別の問いを選ぶ。**2 時間以上前の claim は放棄されたものとみなして奪ってよい**（奪ったら時刻を書き換える）。
2. **統合の回かを判定する** — 「作業ログ」の件数が **8 件を超えていたら、新しい実験をせずに統合作業をする**。すなわち: ログの結論を `acceptance_topology.md` の該当節に集約し、決着した問いを「未解決の問い」から消し、ログを直近 3 件＋要約に畳む。積み上がらない一番の原因は記録が増えるだけで整理されないことなので、これは実験より優先する。
3. **先行研究を潰す** — 実装の前に、隣接分野の語彙で検索する（下記「先行調査の鉄則」）。既にあるなら問いを畳んで記録し、次の問いへ。
4. **設計する** — 何を測れば白黒つくか、どういう結果なら仮説が否定されるかを先に書く。撤退条件のない実験はしない。
5. **実装して回す** — 小さく回して配線を確認してから本測定。既存スクリプトで足りるなら新規作成しない。**1 回の計算は 40 分以内**に収める。収まらないなら関数か seed を削り、削ったと書く。
6. **分析する** — 対応のある検定と効果量、seed 方向一致率。平均だけで結論を出さない。
7. **記録してコミットする** — 「作業ログ」に追記し、claim を外して結果を書く（決着したら問いを消す、続きがあれば次の問いとして書く）。**否定的結果も機序つきで残す**。`research-loop` に push する。push が競合したら `git pull --rebase` して再試行。

**1 回で全部やろうとしない。** claim した問いが 40 分で終わらないと分かったら、そこまでの結果と「次はここから」をログに書いて終える。中途半端な状態を残すより、次の回が続きから入れる形にすることが優先。

---

## 守る規則（これまでの失敗から）

**先行調査の鉄則** — 新しい定式化を思いついたら、**実装より先に 4 分野以上の語彙で検索する**。この研究では 2 回、提案の中核が先行研究と一致して撤退した。

- 進化計算（niching / QD / EDO）だけでなく、ロバスト最適化（K-adaptability, ROMS）、設計工学（solution space engineering, set-based design）、計算化学（disconnectivity graph, barrier tree, NEB）、統計（cluster tree）、TDA（merge tree, persistence）、能動学習（level set estimation）の用語で引く。
- 「barrier tree は調べたが disconnectivity graph では引かなかった」で 1 日を失った。**同じ概念の別名を列挙してから検索する。**

**測定の鉄則**

- **単一 seed で結論を出さない。** 1 seed では NM-Restart が最良と出た関数で、3 seed では最下位になった。
- **集計から落ちた対象を必ず報告する。** 比が定義できない関数を黙って除くと順位が変わる（実際に変わった）。
- **定義を変えたら結果がどう動くかを測る。** τ・重複除去半径・行動記述子・シナリオモデルはいずれも答えを桁で動かした。定義依存性は欠陥ではなく報告事項。
- **推定器の誤りの向きを述べる。** 線分による連結判定は繋がりを見落とす側にしか誤らない、など。

**MC-ESO を壊さない**

- `SR@1e-10` を下げる構成は採用しない（多解の改善でも例外なし）。
- 既定値を変えたら **BBOB-24 dim2 / n=20 で SR@1e-10 93.5% / evals_succ_mean 798** と一致することを確認してからコミットする。
- 診断用の変種は本体を書き換えず別ファイルに置く（例: `mceso_crowding.py`）。

---

## 未解決の問い（上から着手する）

claim 中のものは `(claimed ...)` が付く。2 時間以上前の claim は奪ってよい。

**方針（2026-09-02、2 度目の転換）**: MC-ESO の改善という制約を外し、「多峰度の高い問題での
peak ratio」をゴールに置き直した。監査テーマ（受容集合）は新規測定を止め、記録は
[acceptance_topology.md の終了節](acceptance_topology.md#監査テーマの終了2026-09-02--何が残り何がゴールに引き継がれたか)
に集約済み。報告規則の路線は先行研究に占有されている（上記「占有されている領域」）。

1. **`sigma_only` の利得が本物か（hunt 数の交絡を潰し、N08 と BBOB で確かめる）** — その23 で
   梃子が確定した: **再起動後の σ を、箱に対する固定比（σ_init = 0.2×span）ではなく、
   その時点で観測された局所盆地間隔 `d_local` に対する比に置く**。
   `sigma_only`（描画は base のまま、σ = 0.1·d_local。`core/optimizers/mceso_commit_reseed.py` の
   `commit_mode="sigma_only"`、`scripts/diagnose_niching.py --variant sigma_only`）は 15 seed で
   **N07 の PR を全 eps 水準で 0.22 → 0.34（15/15 seed、p = 0.0006、A12 = 1.00）**、
   **N06 の PR@1e-5 を 0.17 → 0.39（13/15、p = 0.0014）**に上げ、
   **N06 の 1e-1 / 1e-3 は削っていない**（0.82 → 0.82 / 0.81、w/t/l 6/2/7 で無差）。
   これが本当なら「深さと広さは 1 つのつまみで両立しない」の例外になるので、慎重に潰す。
   **やること（順に）**:
   (a) **交絡を潰す。** σ を落とすと σ が底を打つのが早く hunt が N07 で 35.5 → 49.1、
   N06 で 29.5 → 43.9 に増える。利得が σ の質なのか hunt 数なのかを分ける。
   対照は「base のまま hunt 数だけを 49 に増やす」構成（`hunt_no_improve_mult` を下げる
   既存の `--variant fast` が使える。`exhausted_sigma_tol` を上げる手もある）。
   **hunt 数を揃えた base が N07 で 0.34 に届くなら σ の寄与は無く、届かなければ本物。**
   (b) **N08-Shubert3D を測る**（その23 は 2D 2 関数しか測っていない）。3D で符号が反転しないこと。
   (c) **BBOB-24 dim2 / n=20 で SR@1e-10 93.5% / evals_succ_mean 798 を割らないこと**を確認する。
   ここまで通れば本体（`mceso.py:1058` の `sigma_restart`）に取り込む候補になる。割ったら取り込まない。
   **判定**: (a) で交絡が晴れ、(b) で N08 が悪化せず、(c) が 93.5% を割らない、の 3 つ全部。
   どれか 1 つでも落ちたら本体には入れず、診断用の知見として畳む。
   **注意**: σ 比は単調ではない（0.25 / 0.10 / 0.05 / 0.02 / 0.01 で N07 PR@1e-3 は
   0.37 / **0.46** / 0.37 / 0.38 / 0.38）。**0.1 付近以外を既定にしない。**
   **N09 は天井 0.29 で識別力が無いので判定に使わない。**
   **運用**: N09 を 4 万評価 × 3 seed で回すときは並列を 2 まで（3 並列は OOM で落ちる）。
   N07/N06 は 2 万評価なら 1 seed 数秒なので、3 seed ではなく **15 seed** で回せる。
2. **選び直しの報告規則を全手法に与えて測り直す** — その20 で、履歴からの rho 貪欲な選び直し
   （`reselect_from_history`、追加評価ゼロ、cap = max(100, 2K) 厳守）は MC-ESO の
   N07 / N09 の PR@1e-1 を 0.25 / 0.12 → **1.00** に上げた。だが `core/runner.py:198` が
   警告するとおり、**これは密なサンプリングを報う利得**で、同じ規則を与えれば他手法も伸びる。
   現行の順位表（その15）は選び直し無しで測られているので、**この利得を根拠に NMMSO と
   比較してはいけない**。**やること**: `reselect_from_history` は `OptimizeResult` の
   `history_x` / `history_f` しか使わないので手法非依存に当てられる。
   `niching_baseline.py` に「報告集合 = 現行 / 選び直し」の 2 通りで採点するフラグを足し、
   7 手法 × N06-N10 で並べる。**判定**: (a) 選び直しで順位が変わるか、
   (b) 利得が eps=1e-1 に集中する現象が他手法でも起きるか。
   (b) が全手法で起きるなら、**PR の 1e-1 水準は手法を識別していない**ことになり、
   以後の判定は 1e-3 以下で行うべきという運用上の結論になる。
3. **予算を増やしても新しい最適解に変わらなくなる原因を切り分ける** — その17 より、手法を
   分けているのは評価回数でも K でもなく **予算を新しい最適解に変換し続けられるか**。
   r3pso は 10 倍にしても完全に横ばい（N10 で 0.48 → 0.47）、MC-ESO も Vincent2D では
   0.11 → 0.28 しか伸びない。一方 NMMSO は 0.64 → 1.00 まで伸びる。
   発見曲線（報告集合に入った相異なる大域最適の数 対 評価回数）を取り、止まる手法について
   原因を 3 つに切り分ける: **(a) 集団やアーカイブが埋まっている**（→ 容量の問題）、
   **(b) 同じ最適解を再発見し続けている**（→ 重複回避の問題）、
   **(c) 本当に新しい盆地が尽きている**（→ 探索の問題）。
   **MC-ESO については その19 ＋ その20 で片付いた**: Shubert は (c)（visited = reported が
   全 seed 一致）。Vincent は **eps=1e-1 でだけ (a)**（記録器の容量。spillover 1 回に 1 点。
   選び直しで 1.00 まで回復する）で、**1e-3 以下では (a) ではなく収束不足**
   （掠めた 188 盆地を降り切れていない。→ 問い 1）。**残っているのは他手法** —
   特に **r3pso の完全な飽和**（10 倍予算で N10 0.48 → 0.47）と **NCDE**。
   `diagnose_niching.py` は `_CountingMCESO` に依存しているので、他手法に当てるには
   visited / reported を手法非依存に取り直す必要がある（`history_x` と `final_solutions` は
   共通の `OptimizeResult` にあるので、カウンタ類を諦めれば汎用化できる）。
   棄却条件は特になし（機序の特定が目的）。
4. **NM-Restart が Shubert で 1 点しか報告しない件を調べる** — N06 で報告点数 1、N08 で 7、
   PR は全予算で 0.00。N10 では 100 点報告して PR 1.00 なので、手法の性質ではなく
   **こちらの実装の不具合の可能性**が高い。**ベースラインとして使う前に確認する**
   （不具合なら その15 / その17 の NM-Restart 行は Shubert について無効）。
   棄却条件は、実装が正しく Shubert では本当に 1 点しか出せないと確認できること。
5. **Shubert の高精度側の崩れ** — 盆地は見つけているのに掘り切れていない
   （PR@1e-1 0.80 → PR@1e-5 0.15）。**その19 で「記録の問題ではない」ことが確定した**
   （visited = reported が N06 / N08 の全 seed・全 eps で厳密に一致。N06 は eps を
   1e-3 → 1e-5 にすると visited 自体が 15.3 → 3.0 に落ちる）ので、
   ここは純粋に**掘り切る側**の問題。**その21 が hunt 側から独立に裏づけた**:
   N08-Shubert3D は 54.3 hunt 中 **44.7 が相異なる盆地**（被覆は良い）なのに、
   **1e-5 まで降りるのは 1.0 hunt だけ**、1e-3 でも 9.7。N06 も 29.3 中 3.3。
   Vincent とは律速がちょうど逆で、**Shubert では distinct ではなく deep が binding**。
   hunt の解放理由は全部 `exhausted=1`（σ が底を打った or `hunt_level_tol` が発火）なので、
   **狙うべきは `hunt_level_tol = 1e-6`（`mceso.py:395`）と σ 下限**。
   Shubert は局所最適が約 760 個あるので、浅い hunt は大域最適と区別できない
   （`_basin_exhausted` のコメントどおり）— ここが深さと広さのトレードオフの現場。
   監査側で確定した「1 つのつまみでは深さと広さを
   両立できない」を踏まえ、受理規則の 2 段構え（σ が枯れるまで親比較、枯れた後だけ
   最近傍置換）で両立するかを測る。判定は PR@1e-5 が上がり、**かつ** BBOB-24 dim2 の
   SR@1e-10 が 93.5% を割らないこと。
6. **正規予算での比較を Actions に載せる** — 現状の測定は正規の 1/10 予算なので順位の細部は
   確定しない。正規予算（5 万〜40 万評価）での比較を GitHub Actions で回す。
7. **投稿先の選定** — GECCO。締切と要求形式を調べる。

**畳んだ問い**: 報告規則の転用（先行研究に占有）、監査側の全て（全滅の機序の次元不変性、
共通探索での比較、断片化ゲート、適応 τ、参照プールの解像度）。結論は
`acceptance_topology.md` に残っている。

---

## 作業ログ

新しい回ほど上に書く。1 回 1 節、日付・やったこと・分かったこと・次に繋がる問い。
**8 件を超えたら次の回は統合の回**（手順 2）。

**統合中 (claimed 2026-09-02 23:30 UTC)** — ログ 9 件 > 8 件のため、この回は新規測定をせず統合する。
並行回はこの marker を見て別の問いへ回るか、次の回まで待つこと。

### 2026-09-02 その23（撤退条件は満たさない（0.44 < 0.50）が、N07 の PR は 2 倍になった。効いているのは「1 本への commit」ではなく**再起動後の σ を盆地スケールに落とすこと**で、それは Shubert では無償）

問い 1。**事前に書いた判定（N07 の PR@1e-3 が 0.50 以上）は満たさなかった**（最良 0.44）。
だが**その22 までのどの介入より桁違いに大きく動き**（base 0.22 → 0.44、15 seed 中 15 seed で改善、
A12 = 1.00、p = 0.0006）、しかも**機序が分解できた**ので、棄却ではなく「効くつまみが 1 つ確定した」
という扱いにする。以下すべて MC-ESO の既定値は不変（診断用は別ファイル）。

**やったこと。** `core/optimizers/mceso_commit_reseed.py` に `CommitReseedMCESO` を新規作成
（`AdaptiveRepelMCESO` を継承して `_adaptive_radii` の幾何を再利用）。枯渇時の盆地切替で:

- **anchor**: その event の最初の `_diversified_reseed` 呼び出しの描画を anchor とし、
  残り 19 スロットは全部その周りに置く（**20 本の独立描画をやめる**）。
- **σ_place**: 重複潰し済みの `ir_basin_centroids` のうち anchor に最も近いものの
  最近傍距離 `d_local` に対し `commit_sigma_ratio × d_local`（追加評価ゼロ、`[0.005, 0.2]×span` で clip）。
  盆地が 3 個未満しか掘れていない間は base の経路に落ちる。
- `commit_sigma_mode`: `place`（配置だけ）/ `run`（再起動後の `st.sigma` もこの値にする）。
- **恒等チェック済み**: `commit_off` は base と数値完全一致（patch 後にも再確認）。

`scripts/diagnose_niching.py` の `_VARIANTS` に追加、`scripts/reseed_to_landing.py` に
`commit_tight` を追加。N06-Shubert2D（対照）+ N07-Vincent2D、20000 評価、**15 seed**、
eps 3 水準。`analysis/commit_2d_*.csv`（5 seed 掃引）、`analysis/commit15_*.csv`（15 seed 本測定）。

**主表（N07-Vincent2D、15 seed、対応のある Wilcoxon、reference = base）:**

| 構成 | PR@1e-1 | **PR@1e-3** | PR@1e-5 | w/t/l | p | A12 |
|---|---|---|---|---|---|---|
| base | 0.26 | **0.22** | 0.22 | — | — | — |
| その22 の adaptive（参考、5 seed） | 0.36 | 0.27 | 0.27 | | | |
| `sigma_only`（**σ だけ**、描画は base のまま） | 0.37 | **0.34** | 0.33 | 15/0/0 | 0.0006 | 1.00 |
| `commit_sigma`（commit、σ = 0.25·d_local） | 0.36 | 0.34 | 0.34 | 15/0/0 | 0.0006 | 0.99 |
| **`commit_tight`（commit、σ = 0.1·d_local）** | **0.46** | **0.44** | **0.43** | **15/0/0** | **0.0006** | **1.00** |

**N06-Shubert2D（対照、15 seed）— ここで 2 つの構成の性格が割れる:**

| 構成 | PR@1e-1 | PR@1e-3 | PR@1e-5 | 1e-5 の w/t/l | 1e-5 の p |
|---|---|---|---|---|---|
| base | 0.82 | 0.82 | 0.17 | — | — |
| **`sigma_only`** | 0.82 | 0.81 | **0.39** | **13/2/0** | **0.0014** |
| `commit_tight` | **0.76** | **0.74** | 0.26 | 10/2/3 | 0.0300 |

**機序 — 効いているのは commit ではなく σ。ただし commit は Vincent で上乗せする。**
`sigma_only` は **描画を base のまま**（20 本の独立描画も best-of-20 の競争もそのまま）にして
**再起動後の σ だけ** `0.1·d_local` に落とす対照で、これだけで N07 は 0.22 → 0.34 動く。
commit を足すと 0.34 → 0.44 でさらに上乗せされる。つまり **+0.12 が σ、+0.10 が commit**。
その22 は「描画規則も σ_init も着地を動かさない」と結論したが、そこで振ったのは
**σ_init（箱に対する固定比）**で、今回振ったのは **その時点で観測された局所盆地間隔に対する比**。
Vincent の盆地は対数間隔なので、この 2 つは同じつまみではない。

**中間指標（問いが指定したもの、`scripts/reseed_to_landing.py`、3 seed プール）:**

| 構成 | 描画が触れた相異なる最適解 | **着地した相異なる最適解** | hunt 数 |
|---|---|---|---|
| base | 36 / 36 | **11 / 36** | 102 |
| commit_tight | 33 / 36 *(注)* | **26 / 36** | 146 |

*(注)* commit では tracer が記録する「描画」は anchor ＋ その周りの雲なので、
**base の 36/36 とは別の量**（独立描画の被覆ではなく、commit した配置の被覆）。
比較できるのは着地側だけ。**11 → 26 で、その22 が指した「描画から着地までの落ち込み」は実際に埋まった。**

**報告の水増しではない。** `visited@1e-3`（履歴が eps 以内で触れた相異なる最適解、報告規則に依存しない）が
N07 で **12.0 → 17.3**（commit_tight、14/15 seed で増、p = 0.0010）。
**本当に多くの盆地を降り切っている。**

**負の結果・留保 3 つ。**
(a) **σ 比は単調ではない。** `commit_sigma_ratio` を 0.25 / 0.10 / 0.05 / 0.02 / 0.01 と振ると
N07 の PR@1e-3 は 0.37 / **0.46** / 0.37 / 0.38 / 0.38（5 seed）で **0.10 に山がある**。
締めすぎると盆地に入る確率自体が落ちる。**「commit すればよい」ではなく「盆地間隔の 1/10 で commit する」。**
(b) **commit は Shubert の粗い水準を有意に削る**（1e-1 / 1e-3 で −0.07、11/15 seed で悪化、p ≈ 0.03）。
一方 `sigma_only` は **N06 の 1e-1 / 1e-3 を一切削らずに 1e-5 を 0.17 → 0.39 に上げる**。
監査側で確定していた「1 つのつまみで深さと広さは両立しない」に対し、
**σ を局所盆地スケールにするのは（この 2 関数では）両立している側**に見える。
(c) **hunt 数が交絡している。** σ を落とすと σ が底を打つのが早く、hunt が
N07 で 35.5 → 49.1（sigma_only）/ 49.3（commit_tight）、N06 で 29.5 → 43.9 に増える。
sigma_only の base に対する利得はこの増加と分離できていない。
ただし sigma_only と commit_tight は **hunt 数がほぼ同じ**（49.1 対 49.3）なので、
**両者の差 0.34 対 0.44 は hunt 数では説明されない**（commit の寄与は交絡していない）。

**測っていないもの（次の回への引き継ぎ）。** 3D（N08 / N09）は測っていない。
問いの指示どおり N09 は天井 0.29 で識別力が無いので落とし、時間を N07/N06 の
**seed を 3 → 15 に増やす方**に使った（そのぶん検定力が上がり、15/15 の一致が取れた）。
N08-Shubert3D は測る価値があるが今回は入れていない。
**BBOB-24 dim2 の SR@1e-10 は未確認**（本体を変えていないので不要だが、`sigma_only` を
本体に取り込むなら必須）。

**次に開くこと（問い 1 として書き直した）。** 梃子は「再起動後の σ を、箱ではなく
**その時点で観測された局所盆地間隔**に合わせること」で確定した。次は
(i) hunt 数の交絡を潰す（base の hunt 数を揃えて σ の効果だけを取り出す）、
(ii) `sigma_only` を N08 と BBOB-24 dim2 に当てて、**無償に見えるのが本当か**を確かめる。

### 2026-09-02 その22（棄却 — 反発半径を盆地スケールに直しても PR は動かない。描画は既に全 36 解を覆っており、律速は「20 本の独立描画から best-of-20 を選ぶ」選抜側）

問い 1。**棄却条件が成立した**。その21 が指した「一様描画 ＋ 固定反発半径 0.02×span」を
盆地スケール適応に置き換えたが、**N07 の PR@1e-3 は 0.24 → 0.27** で、問いが先に書いた
撤退条件（**0.5 以上に動かなければ被覆仮説は棄却**）を満たさない。

**やったこと。** 本体は書き換えず `core/optimizers/mceso_adaptive_repel.py` に診断用の
`AdaptiveRepelMCESO` を置いた（`_diversified_reseed` の枯渇時経路だけを override）。
反発半径は追加評価ゼロで観測できる量から決める: (1) `ir_basin_centroids` を
0.02×span で重複潰し（この固定半径は**被覆半径としては役立たないが重複判定としては正しい** —
両地形の最小解間隔より小さい。同一盆地への再着地が自分の最近傍距離を 0 にするのを防ぐ）、
(2) 生き残った centroid ごとに最近傍距離 d_i、(3) `r_i = clip(0.5·d_i, 0.02·span, 0.25·span)`。
α=0.5 なので隣接する 2 つの掘削済み盆地の排除球はちょうど接する（自分の盆地は覆い、
隣は塞がない）。描画側は 2 通り測った: `adaptive`（棄却再描画。半径変更だけを切り出す）と
`adaptive_maxmin`（同じ描画本数で `min_i ||c_i − x|| / r_i` を最大化する候補を選ぶ）。
`repel_mode="off"` が base と厳密一致することを確認済み（恒等チェック）。
`scripts/diagnose_niching.py` の `--variant` を（クラス, kwargs）表に変え、計数用の
override を mixin（`_HuntCounters`）に切り出したので base と変種が同じ形で dump される。
3 変種 × {N06 対照, N07 決定的, N09} × 3 seed、その21 と同じ予算（2D 2 万 / 3D 4 万）。
`analysis/repel_2d_*.csv`、`analysis/repel_hunts_2d_*.csv`。

| 関数 | 指標 | base | adaptive | 判定 |
|---|---|---|---|---|
| **N07-Vincent2D** | **PR@1e-3** | **0.24** | **0.27** | **棄却**（要 0.50） |
| N07-Vincent2D | PR@1e-1 | 0.25 | 0.36 | |
| N07-Vincent2D | PR@1e-5 | 0.24 | 0.27 | |
| N07-Vincent2D | **distinct hunt** | **8.7** | **9.7** | ほぼ不動 |
| N06-Shubert2D（対照） | PR@1e-1 / 1e-3 | 0.85 / 0.85 | 0.80 / 0.80 | 微減 |
| N06-Shubert2D（対照） | PR@1e-5 | 0.17 | **0.31** | 増（3 seed、要追試） |

**機序 — 描画は既に全 36 解を覆っている。落ちているのは描画から着地までの間。**
`scripts/reseed_to_landing.py` を新規作成し、hunt ごとに全ての diversified 描画と
その hunt の終端を Vincent の真の最適解に最近傍で写像した（3 seed、102-130 hunt）:

| 構成 | 描画が触れた相異なる最適解 | **着地した相異なる最適解** | 着地先の解間隔（中央値） | 描画先の解間隔（中央値） |
|---|---|---|---|---|
| base（σ=0.2） | **36 / 36** | **11 / 36** | 1.918 | 1.023 |
| adaptive（σ=0.2） | **36 / 36** | 14 / 36 | 1.918 | 1.023 |
| adaptive（σ=0.05） | **36 / 36** | 15 / 36 | 1.918 | 1.023 |

**描画側は 3 構成とも全解を覆い、着地は 11-15 解にしか行かない。**
着地先の解間隔の中央値は 1.918、描画先は 1.023 で、**3 構成すべてで同一**。
つまり広い盆地への偏りは**描画でも σ でも生成されていない**。

**律速は選抜だった。** 盆地切替は `n_pop = max(20, 4·dim) = 20` スロット全部を
再描画する（`div_ratio = 1.0`）ので、**1 回の再起動につき互いに独立な反発描画が 20 本**あり、
そのあと μ+λ 貪欲が **best-of-20 を 1 つ選ぶ**。20 本は（実測どおり）多くの盆地に散るが、
勝つのは系統的に広い盆地の点である。**描画の分布をいくら直しても、20 本の競争と
best-of-20 の選抜が下流にある限り被覆には変換されない。**
σ_init = 0.2×span = 1.95 を 0.05×span = 0.488（解間隔の中央値 1.023 より小さい）に
落としても着地は 14 → 15 にしかならず、**σ 単独でも説明できない**ことを確認した。

**副次的な負の結果 2 つ。**
(a) `adaptive` と `adaptive_maxmin` は**全行で数値が完全一致**した。20 回の試行内に
必ず受理される候補が見つかるため max-min 経路が一度も発火しない（両者は同じ RNG 列で
同じ点を返す）。**箱が混んでいない限り描画規則の作り込みは無意味**。
(b) 報告集合側の distinct は 9.0 → 16.3 と増えたのに `blocked` が 0.3 → 6.7 に増え、
PR は動かなかった。**新しく報告された niche は eps を満たさない浅い点で埋まっている。**

**測定の欠損（次の回への引き継ぎ）。** N09-Vincent3D は 3 変種を 3 並列で回して
**OOM でワーカーごと落ち**、eps=1e-1 の行だけが stdout に残った（base 0.12 / adaptive 0.13 /
adaptive_maxmin 0.13、CSV は未生成）。base のこの行はその21 と厳密一致（216.0 / 27.0 / 38.3 /
88 / 62.0）なので同じ run を見ている。**N09 を 4 万評価 × 3 seed で回すときは並列を 2 までに
すること**（3 並列は落ちる）。ただし N09 は天井 0.29 で識別力が無く、判定は N07 で足りるので、
再測定は必須ではない。

**次に開くこと（問い 1 として書き直した）。** 梃子は描画でも σ でもなく**再起動の選抜**。
20 本の独立描画から 1 本を選ぶのをやめ、**1 本の反発描画に集団ごと commit する**
（全スロットをその 1 点の周りに、局所の盆地間隔にスケールした σ で置く）。そうすれば
描画が既に持っている被覆が選抜で潰されない。判定は同じく N07 の PR@1e-3 が 0.24 → 0.5 以上。

MC-ESO の既定値は変更していない（変種は別ファイル、`--variant base` は従来どおり）。

### 2026-09-02 その21（Vincent の棄却条件が成立 — 降下は完璧で、盆地の重複が原因。反発半径が桁で足りない）

問い 1。`diagnose_niching.py` に `--hunt-csv`（spillover 1 回 = 1 hunt につき、到達 f・終端座標・
`_basin_exhausted` の値・σ/span・その終端が他の hunt と rho 分離されているか）を足し、
`analyze_hunts.py`（3 分岐の切り分け）と `hunt_coverage.py`（Vincent の真の最適解への写像）を
新規作成した。4 関数 × 3 seed、その19 / その20 と同じ予算（2D 2 万 / 3D 4 万評価）。
`analysis/hunts_{2d,n08,n09,n07_xy}.csv`、3 並列で約 26 分。
**採点行はその19 / その20 と完全一致**（N07 36.0→9.0、N09 216→27.0 等）なので同じ run を見ている。
MC-ESO の既定値は変更していない（計数用サブクラスは集計のみ）。

**棄却条件が成立した。問いが挙げた 3 候補 (i)(ii)(iii) はいずれも主因ではない。**
問いは「hunt ごとの到達 f が既に 1e-3 を切っているのに visited が増えないなら、
降下ではなく盆地の重複が原因」と書いていた。**まさにそれが起きている。**

| 関数 | K | hunts | hunts/K | **distinct** | deep@1e-3 | deep@1e-5 | 律速 |
|---|---|---|---|---|---|---|---|
| N06-Shubert2D | 18 | 29.3 | 1.63 | 16.0 | 28.7 | **3.3** | 深さ |
| **N07-Vincent2D** | 36 | 35.0 | 0.97 | **8.7** | 35.0 | 34.7 | **被覆** |
| N08-Shubert3D | 81 | 54.3 | 0.67 | 44.7 | 9.7 | **1.0** | 深さ |
| **N09-Vincent3D** | 216 | 62.0 | **0.29** | **25.3** | 61.3 | 58.3 | **被覆** |

**Vincent では降下は完璧に働いている。** N09 の 186 hunt のうち **175 が f ≤ 1e-5** に到達し、
1e-3 を外したのは 2 つだけ。N07 は 105 hunt 中 104 が 1e-5。**hunt が中断された例はゼロ**
（`exhausted=0` は N08 の 3 件のみ、他は全部 1.00）。Vincent は sin(10 log x) の平均なので
**局所最適が存在せず全ての極大が大域最適**であり、盆地に入りさえすれば必ず降り切る。
候補 (ii)「停止条件が浅い」と (iii)「局所降下が効いていない」は Vincent について棄却。

**効いているのは distinct（相異なる盆地に着地した hunt の数）で、これが報告数をそのまま決める。**
N07: distinct 8.7 に対し reported@1e-3 8.7 / @1e-5 8.7。N09: distinct 25.3 に対し 25.7 / 24.0。
一般化すると **reported ≈ min(distinct hunts, deep hunts)** で、
**Vincent は distinct 側、Shubert は deep 側が binding**。
N08 は distinct 44.7 もあるのに reported@1e-3 は 9.0（深さで落ちる）で、対照として効いている。
**その20 の「二分法は eps=1e-1 でしか成り立たない」は、報告集合ではなく hunt を見れば
全 eps で成り立つ** — 負け方の 2 分類は正しく、見る変数が違っていた。

**機序 — 再起動の描画が一様で、反発半径が固定なのに、Vincent の盆地は対数間隔。**
`mceso.py:863` `_diversified_reseed` の枯渇時経路は **箱内の一様描画 ＋ 反発半径 0.02×span = 0.195**。
Vincent の 1 軸あたりの最適解は log x = (π/2 + 2πn)/10、n = −2..3 の 6 点で
**x = 0.333 / 0.624 / 1.170 / 2.193 / 4.113 / 7.708**。隣接間隔は **0.291 〜 3.595 の 12 倍幅**。

- **一様描画は盆地の幅に比例して当たる**ので、狭い盆地は 1 軸で 12 倍、2D で約 144 倍、
  3D で約 1700 倍当たりにくい。
- **反発半径 0.195 は最狭の間隔 0.291 より小さい。** 広い盆地（幅 3.595）では中心から 0.195
  離れた点はまだ同じ盆地の中なので、**反発は同じ盆地への再着地を全く防げない**。

N07 の実測（105 hunt を真の 36 最適解に最近傍で写像、`hunt_coverage.py`）:

| 盆地の幅（最近傍距離）帯 | 最適解数 | 総 hit 数 | 1 解あたり | 到達できた解 |
|---|---|---|---|---|
| 0.29 - 0.67（狭い） | 27 | **7** | 0.09 | **4/27** |
| 0.67 - 3.60（広い） | 9 | **98** | 3.63 | 7/9 |

**105 hunt のうち 98 が、36 個中わずか 9 個の広い盆地に落ちている**（同一解への最大再着地は
seed 0 で 14 回）。到達できた解の幅の中央値 1.023 対 未到達 0.291、
**Mann-Whitney p = 2.1e-4、A12 = 0.84**（3 seed とも 8-9 解のみ到達で一致）。
Shubert は最適解がほぼ等間隔（約 0.88）なので同じ固定半径が全域で整合し、
distinct/hunts は N08 で 0.82、N06 で 0.55 と高い（Vincent は N07 0.25 / N09 0.41）。
**「多解手法が高次元で報告集合を縮める」（その16 の引き継ぎ）の正体はこれ** — 次元が上がると
狭い盆地の相対体積が指数的に小さくなる。

**もう 1 つの天井（被覆を直しても残る）。** N09 は **hunts/K = 0.29**。重複がゼロでも
62 hunt では 216 個中 62 個までしか触れないので、**被覆を完全に直しても PR@1e-3 の上限は 0.29**
（現行 0.12）。N07 は hunts/K = 0.97 なので上限 0.97 まである。1 hunt あたりの中央値は 595 評価、
最初の hunt が 3423 評価目。その17 で予算 10 倍でも N09 が 0.03 → 0.16 で止まったのは、
被覆と hunt 数の両方が同時に効いているためと読める。

**測定上の教訓（自分のバグ）。** 最初の `hunt_coverage.py` は Vincent の最適解を
n = 0..5 で作っており、うち 2 点（x = 14.5 / 27.1）が箱の外、箱内は 4 点しかなかった。
このとき最近傍写像が壊れて **A12 = 0.45、p = 0.69（無相関）** と出て、仮説を棄却しかけた。
log(0.25) = −1.386 なので **n は負まで回す**必要がある（n = −2..3 で 6 点、K = 6^dim と一致）。
**解析解を持つ関数でも「箱内に K 個あるか」を assert してから使うこと**（現在は assert 済み）。

**次に開くこと。** 梃子は降下でも停止条件でもなく**再起動の描画と反発半径**にある。
どちらも `_diversified_reseed` の 2 行で、しかも局所的な盆地スケールに適応させられる
（既に drill 済みの最適解どうしの距離が観測できる）。これを問い 1 として書き直した。

### 2026-09-02 その20（選び直しは visited の天井に届く。ただし利得は eps=1e-1 だけ — Vincent は精度の問題でもあった）

問い 1。`scripts/diagnose_niching.py` に **履歴からの報告集合の選び直し**を足した
（`reselect_from_history`）。run の全評価点を f 昇順に並べ、**採点側と同じ rho 貪欲**で
`max(100, 2K)` 点（`core/runner.py:203` の cap）まで取る。追加評価はゼロ。本体は書き換えていない。
`_greedy_seeds_capped` は `_seed_indices` の早期打ち切り版で、貪欲は接頭辞安定なので cap は
既に採った seed を変えない（**ランダム 300 ケース × cap 6 水準で参照実装と完全一致を確認**）。
早期打ち切りが無いと Vincent3D の 4 万点履歴で seed が数千に育って回らない。
5 関数 × 3 seed × eps 3 水準、その19 と同じ予算（2D 2 万 / 3D 4 万評価）で直接比較できる。
`analysis/niching_resel_{2d,n08,n09}.csv`、3 並列で 26 分。

**選び直しは visited の天井に「ぴったり」届く。45 行すべてで `resel == visited`。**
cap は集合サイズには常に効いている（`|res|` は 100 / 162 / 432 = cap ちょうど）が、
**スコアには一度も効いていない**（visited ≤ cap が全行で成立）。eps を満たす点は f 順で
前に来るので、cap 内の先頭に全部入る。**報告枠は制約になっていない。**

**しかし利得は eps=1e-1 にほぼ集中していた**（PR = 発見数 / K、3 seed 平均）:

| 関数 | K | eps | PR 現行 | PR 選び直し | 差 | seed 一致 |
|---|---|---|---|---|---|---|
| **N07-Vincent2D** | 36 | 1e-1 | 0.25 | **1.00** | **+0.75** | **3/3** |
| N07-Vincent2D | 36 | 1e-3 | 0.24 | 0.32 | +0.08 | 3/3 |
| N07-Vincent2D | 36 | 1e-5 | 0.24 | 0.27 | +0.03 | 2/3 |
| **N09-Vincent3D** | 216 | 1e-1 | 0.12 | **1.00** | **+0.88** | **3/3** |
| N09-Vincent3D | 216 | 1e-3 | 0.12 | 0.13 | +0.01 | 3/3 |
| N09-Vincent3D | 216 | 1e-5 | 0.11 | 0.11 | +0.00 | 2/3 |
| N06-Shubert2D | 18 | 全 3 水準 | 0.62 | 0.62 | **±0.00** | 0/3 |
| N08-Shubert3D | 81 | 全 3 水準 | 0.15 | 0.15 | **±0.00** | 0/3 |
| N10-ModRastrigin2D | 12 | 全 3 水準 | 0.94 | 0.99 | +0.05 | 1/3 |

3 水準平均では N07 が 0.244 → 0.531、N09 が 0.118 → 0.414。**Shubert は全 seed・全 eps で
差が厳密に 0**（その19 の visited = reported から予測どおり。対照として機能した）。
**負に振れた seed は 1 つも無い**（利得ゼロか正のみ）。

**棄却条件は「半分だけ」成立した。** 問いは「選び直しても PR が現行と同等なら棄却」と
書いていた。1e-1 では同等どころか 4〜8 倍になるので棄却されない。**1e-3 以下では実質同等**
（+0.01〜+0.08）なので、そこでは棄却される。**記録の損失は実在するが浅い。**

**機序 — MC-ESO は Vincent の 216 個の最適解すべてに 1e-1 まで近づくが、降り切るのは約 28 個。**
履歴の visited が eps でこう動く: N09 で 216（1e-1）→ 27.7（1e-3）→ 24.7（1e-5）。
つまり**188 個の盆地は「掠めただけで降りていない」**。記録器を直すと、この掠めた点が
1e-1 の採点にだけ変換される。**その19 の分類（Vincent = (a) 記録器の容量）は強すぎた** —
数値は正しいが含意が違う。1e-1 では記録の問題、**1e-3 以下では掘り切れていないという
探索・収束の問題**で、問い 4 が Shubert について言っていたのと同じ負け方が Vincent にもある。
「Shubert は精度の問題、Vincent は記録の問題」という その15 / その19 の二分法は、
**eps=1e-1 でしか成り立たない**。

**この報告規則の公平性について（重要な留保）。** `core/runner.py:198` は
「履歴ベースの PR は多解探索ではなく密なサンプリングを報う」と警告しており、
**今回の 1e-1 の利得はまさにその警告どおりのもの**である。収束していない近傍点が
点を取っているのだから、**同じ規則を与えれば他手法も同様に伸びる**。したがって
**この +0.29 を持って NMMSO（N07 0.97 / N09 0.49、選び直し無しの測定）と比較してはいけない。**
主張にするなら全手法を同じ規則で測り直す必要がある（新しい問い 2）。

**次に開くこと。** 大きい方の梃子は記録ではなく収束側にある。N09 で「掠めた 188 盆地」を
1e-3 まで降ろせれば PR は 0.12 → 最大 1.00 で、しかもそれは密サンプリング由来ではない
本物の利得になる。これを問い 1 として書き直した。

運用: MC-ESO の既定値は変更していない（診断スクリプトのみ）。`pynmmso` は
`pip install --ignore-installed setuptools wheel` を先に流せば入る（[acceptance_topology.md]
の教訓どおり。素の `pip install -r requirements.txt` は wheel ビルドで落ちる）。
Debian の `blinker` も `--ignore-installed` が要る。

### 2026-09-02 その19（Vincent は「見つけているのに報告していない」— 確定。記録器の容量が原因）

問い 1。`scripts/diagnose_niching.py` に **複数 eps を同じ run から採点する機能**（`--eps` の
カンマ列）、**`blocked` 列**（報告集合の rho 分離点のうち eps を外して 0 点になる数）、
`--csv` を足した。5 関数 × 3 seed × 1/10 予算 × eps 3 水準
（`analysis/niching_visited_{2d,n08,n09}.csv`、約 40 分）。

**予測は当たった。棄却条件（visited ≈ reported）は Vincent では成り立たない。**

| 関数 | K | eps | visited | reported | distinct | \|rep\| | seed 一致 |
|---|---|---|---|---|---|---|---|
| **N09-Vincent3D** | 216 | 1e-1 | **216.0** | **27.0** | 38.3 | 88 | **3/3** |
| N09-Vincent3D | 216 | 1e-3 | 27.7 | 25.7 | 38.3 | 88 | 3/3 |
| **N07-Vincent2D** | 36 | 1e-1 | **36.0** | **9.0** | 9.0 | 61 | **3/3** |
| N07-Vincent2D | 36 | 1e-3 | 11.7 | 8.7 | 9.0 | 61 | 3/3 |
| N06-Shubert2D | 18 | 1e-1 | 15.3 | 15.3 | 17.7 | 55 | **0/3** |
| N06-Shubert2D | 18 | 1e-5 | 3.0 | 3.0 | 17.7 | 55 | 0/3 |
| N08-Shubert3D | 81 | 1e-1 | 27.0 | 27.0 | 63.3 | 80 | **0/3** |
| N10-ModRastrigin2D | 12 | 1e-1 | 12.0 | 11.3 | 23.3 | 57 | 1/3 |

**MC-ESO は Vincent3D の大域最適 216 個を「全部踏んでいる」のに 27 個しか報告していない。**
3 seed とも 216（探索履歴側は K で頭打ちなので実際はそれ以上踏んでいる）で、報告は 28/28/25。
Vincent2D も 3 seed とも 36 → 9 で完全一致。**追加評価ゼロで直る損失**がここにある。

**Shubert は逆で、visited = reported が全 seed・全 eps で厳密に一致する**（15→15, 3→3, 27→27）。
その15 の「Shubert は精度の問題、Vincent は精度の問題ではない」という切り分けは、
visited / reported の言葉でも独立に再現された。問い 4（Shubert の高精度側）は記録の話ではない。

**機序 — 報告集合は「再起動 1 回につき 1 点」しか書かない。**
報告集合は `pop_x + ir_archive_x + sol_archive_x`（`mceso.py:754-757`）で、
`_on_spillover_start`（`mceso.py:818-821`）が **spillover 1 回につき 1 点だけ** append する。
実測でも `|rep| − spillover` が 5 関数すべてで **26.0 ちょうど**（pop + ir_archive 分）:

| 関数 | K | spillover | \|rep\| | pop+ir | distinct | PR 上限 (distinct/K) | 実測 PR@1e-1 |
|---|---|---|---|---|---|---|---|
| N06-Shubert2D | 18 | 29.3 | 55.3 | 26.0 | 17.7 | 0.98 | 0.85 |
| N07-Vincent2D | 36 | 35.0 | 61.0 | 26.0 | 9.0 | **0.25** | **0.25** |
| N08-Shubert3D | 81 | 54.3 | 80.3 | 26.0 | 63.3 | 0.78 | 0.33 |
| N09-Vincent3D | 216 | 62.0 | 88.0 | 26.0 | 38.3 | **0.18** | 0.13 |
| N10-ModRastrigin2D | 12 | 31.3 | 57.3 | 26.0 | 23.3 | 1.94 | 0.94 |

つまり**報告集合の大きさを決めているのは K ではなく再起動の頻度**で、K > spillover 回数に
なると天井が効き始める。**N07 は実測 PR がこの天井にぴったり張り付いている**（0.25 = 0.25）。
Shubert では天井が効いていない（0.98 / 0.78 に対し実測 0.85 / 0.33）ので、
**天井が binding なのは Vincent だけ**。その15 の「差はほぼ Vincent だけで作られている」と
同じ場所を指している。

**その15 の天井の見積もりを訂正する。** 「216 個に対し 88 点報告なので上限 0.41」は誤り。
88 点は rho 分離すると **38.3 点**にしかならない（重複報告）ので、真の上限は
**0.177** で、従来の見積もりより 2.3 倍きつい。CEC2013 の採点上限は max(100, 2K) = 432 点
なので、**報告枠は 5 倍近く余っている**（88 / 432）。

**なぜ今まで見えなかったか（測定上の教訓）。** 2026-08-31 の同じ切り分け
（[history.md](history.md#飽和の機序を特定--解を見つけて捨てていた2026-08-31-採用)）は
**ε=1e-4 の 1 水準だけ**で取られており、そこでは N07 の visited は 8.6 にしか見えず
「探索側も不足、記録を直しても上限 0.24」と結論していた。ε=1e-1 で数え直すと 36.0（全解）。
**visited は eps に強く依存するので、1 水準だけで「探索が届いていない」と判断してはいけない。**
今回 `--eps` を複数取れるようにしたのはこのため（同じ run を採点し直すだけなので追加コストなし）。

**内部 niche 半径だけでは説明できない**（棄却した対抗仮説）。MC-ESO の
`niche_radius_ratio = 0.1 × span` は CEC の rho に対し N06/N08 で 4.0 倍、N07/N09 で 4.9 倍、
**N10 で 10.0 倍**。一番粗い N10 で PR 0.94 を出しているので、粗さ単独では負けを説明しない。
効いているのは半径ではなく**書き込み回数**。

**次に開くこと。** 履歴には全 216 個が入っていて、報告枠は 432 点まで使える。
**履歴を fitness 順に rho 貪欲で max(100, 2K) 点まで選び直す**だけで、追加評価ゼロで
PR@1e-1 が 0.13 → 最大 1.00 まで動きうる。ただし「履歴全部を報告する」のは正当な報告では
ない（`core/runner.py:198` の注意書き）ので、**採点上限を守った選び直し**であることが条件。
これを問い 1' として次の回に回す（下記）。

運用: `--eps` を複数取れるようにしたので、精度水準を増やしても再最適化は起きない
（同じ run を採点し直すだけ）。N09 は 1 seed 3.5 分・N08 は 2 分かかるので、
3 関数を 3 並列にして 40 分に収めた。MC-ESO の既定値は変更していない。

### 2026-09-02 その18（統合の回 — 監査ログを畳んで、ゴールに効く 3 件だけ残した）

新規測定なし（手順 2: ログ 12 件 > 8 件）。並行回との衝突を避けるため、統合そのものを
claim してから push し、それから作業した。

**やったこと 1 — 未収録だった監査側の結論 3 件を `acceptance_topology.md` に送った。**
その7 / その10 / その13 / その14 / その16 は既に該当節があったが、以下は本文が
ログにしか無く、ログを畳むと失われるところだった:

- **その8 ＋ その9（後処理）** → [後処理としては一律に効かない](acceptance_topology.md#後処理としては一律に効かない--提案の形が変わる2026-09-02-scenario_valuepy---postprocess)。
  同じ話題なので 1 節にまとめた（勝敗表 ＋ spread の直接測定による機序確認）。
- **その12（断片化の層別）** → [断片化は「幾何が効くか」を決めるが「数か距離か」は決めない](acceptance_topology.md#断片化は幾何が効くかを決めるが数か距離かは決めない2026-09-02-scriptsstratify_componentspy)。

**やったこと 2 — 監査テーマの終了節を書いた**（[該当節](acceptance_topology.md#監査テーマの終了2026-09-02--何が残り何がゴールに引き継がれたか)）。
確定した 7 点、テーマを止めた理由（射程の問題であって失敗ではない: 全滅率も期待 regret も
BBOB の未定式化基準の上の量で peak ratio には効かない、加えて報告規則の路線は先行研究に
占有）、テーマを越えて有効な測定上の教訓（`--methods none` の恒等性、CSV の環境依存、
`pynmmso` の Python 3.11 問題を含む）をそこに置いた。**新ゴールに引き継いだのは 1 点だけ** —
「MC-ESO は次元が上がると報告集合が縮む」。これは問い 1 の予測として本文に埋め込んだ。

**やったこと 3 — 問いの整理。** (i) その16 とその17 が**同じ番号 その16 を名乗っていた**
（並行回の衝突）ので、コミット順に その16 = 次元不変性、その17 = 予算掃引 と振り直した。
(ii) 旧・問い 1 の本文に、その17 で既に棄却済みの evals/K 仮説の記述が混ざっていたので削った。
(iii) 番号が 4 で重複していたのを直した。(iv) **順序を 1 つ変えた**: 旧 問い 2（Vincent の
visited / reported）を先頭に上げ、旧 問い 1（発見曲線の 3 分岐）をその次にした。理由は、
前者が後者の分岐 (b) を**追加評価ゼロ・棄却条件つき**で潰す最小コストの一手で、しかも
監査側からの予測を持って入れるから。ログを畳んだ副作用ではなく、意図的な優先度変更。

**畳んだログ**（結論は上記リンク先に全て残っている）: その7（τ の選択）、その8・その9（後処理）、
その10（参照プールの予算）、その11（push できず失われたクラウド回。その13 とその16 が同じ測定を
やり直して確定させたので、記録としての役目は終わり）、その12（断片化）、その13（次元と解像度）、
その14（適応 τ の棄却）、その1〜その6 の到達点。

運用: この回は測定をしないので依存の導入（`pip install -r requirements.txt`）はしていない。
次の回は通常どおり環境を作ってから問い 1 に入る。

次の回は問い 1 から。統合の回ではないので通常どおり claim して測る。

### 2026-09-02 その17（PR は評価回数でも K でも決まらない — 仮説棄却、分けているのは予算応答）

新ゴールの最初のサイクル。PR が「最適解 1 個あたりの評価回数（evals/K）」という 1 つの量で
決まるかを測った。`scripts/niching_baseline.py` に予算掃引を足し、6 関数（K = 4 / 12 / 18 /
36 / 81 / 216）× 5 手法 × 3 seed × 予算 4 水準（正規の 0.02 / 0.05 / 0.1 / 0.2 倍）。
`analysis/niching_budget_sweep.csv`。

**棄却。evals/K では決まらない。** 同じ evals/K の 10 倍幅の中で PR は 0.00 から 1.00 まで
散らばる（10^2〜10^3 の帯で n=70、中央値 0.27、範囲 0.00-1.00）。

**K でも決まらない。** evals/K を固定して K 別に見ると単調ですらない:

| evals/K | K=4 | K=12 | K=18 | K=36 | K=81 | K=216 |
|---|---|---|---|---|---|---|
| 300-1000 | 0.70 | 0.75 | 0.24 | 0.45 | 0.16 | 0.26 |

K=18（0.24）が K=36（0.45）より悪く、K=216（0.26）が K=81（0.16）より良い。

**分けているのは、予算を新しい最適解に変換し続けられるかどうかだった。**
予算 10 倍（evals/K で 0.02 → 0.2 倍）での PR の動き:

| 手法 | N04 (K=4) | N10 (K=12) | N06 (K=18) | N07 (K=36) | N09 (K=216) |
|---|---|---|---|---|---|
| NMMSO | 0.75 → 1.00 | 1.00 → 1.00 | 0.04 → **1.00** | 0.64 → **1.00** | 0.16 → **0.67** |
| MC-ESO | 0.25 → 1.00 | 0.35 → 1.00 | 0.15 → 0.82 | 0.11 → 0.28 | 0.03 → 0.16 |
| NCDE | 0.35 → 0.98 | 0.68 → 0.99 | 0.00 → 0.10 | 0.24 → 0.37 | 0.06 → 0.11 |
| NM-Restart | 0.75 → 1.00 | 0.83 → 1.00 | 0.00 → 0.00 | 0.31 → 0.57 | 0.09 → 0.35 |
| r3pso | 0.45 → 0.77 | 0.48 → **0.47** | 0.26 → 0.31 | 0.14 → 0.13 | 0.03 → 0.02 |

- **r3pso は完全に飽和する。** 10 倍にしても N10 で 0.48 → 0.47、N07 で 0.14 → 0.13、
  N09 で 0.03 → 0.02。予算が 1 個も新しい最適解に変わっていない。
- **MC-ESO は Vincent で伸びない。** 0.11 → 0.28、0.03 → 0.16。低 K の関数では 1.00 まで
  伸びるので、飽和は手法の一般的性質ではなく高 K 側で起きる。
- **NMMSO だけが高 K でも予算を変換し続ける。** N06 で 0.04 → 1.00 は 25 倍。

**地形族は K より効く。** Shubert（N06/N08）と Vincent（N07/N09）で手法の並びが変わる。
NM-Restart は Vincent で 0.35-0.57 取るのに Shubert では全予算で 0.00。

次に開いた問い: 止まる原因の切り分け（問い 2）。容量が埋まっているのか、同じ最適解を
再発見し続けているのか、本当に新しい盆地が尽きているのか。

**要確認**: NM-Restart は N06 で報告点数 1、N08 で 7。N10 では 100 点報告して PR 1.00 なので、
Shubert でだけ 1 点というのは**こちらの実装の不具合の可能性**が高い（問い 3）。

### 2026-09-02 その16（全滅の機序は次元不変 — 監査テーマの最後の測定）

監査側の問い（全滅の機序の次元不変性）。その11 が報告したまま失われ、その13 では予算不足で
確認できていなかった点。7 手法 ＋ 参照規則 2 つ × BBOB-24 × 5 seed × 30 シナリオ、
cut 0.10 / 0.25 を dim 2/3/5（`analysis/dim/wipe_cut{C}_d{D}.csv.gz`、4 並列 16 分）。
**測定内容と数値は
[acceptance_topology.md の該当節](acceptance_topology.md#全滅の機序は次元不変ただし全滅の代償は次元とともに膨らむ2026-09-02-scriptswipeoutpy---spread-from-rows)
に集約済み**（ρ −0.88〜−0.94 で 6 セルすべて符号一致 24/24、全滅率 30〜36% で次元にほぼ不変、
全滅の代償だけが次元とともに一桁上がる）。

この回の最中に並行回（その15）がゴールを peak ratio に切り替えたため、**監査側の問いは
畳んで正しい**。畳まずに残す価値があったのは 1 点だけで、それは問い 1 の予測になっている:

> **MC-ESO は次元が上がると報告集合が縮む。** 他の 8 系列（上位 K を含む）が全部広がる中で
> 唯一逆を向く（0.176 → 0.139 → 0.117）。peak ratio の言葉に直せば「高次元では相異なる解を
> 報告しにくくなっている」で、その15 が N09-Vincent3D で見つけた「216 個に対し 88 点しか
> 報告していない」と同じ向きの現象を、別のベンチマーク・別の指標で独立に捉えたことになる。

### 2026-09-02 その15（現状の立ち位置 — MC-ESO は 2 位、負けは Vincent に集中）

新しいゴール（peak ratio の改善）の出発点を測った。`scripts/niching_baseline.py`、
CEC2013 の N04-N10 × 7 手法 × 3 seed、`analysis/niching_baseline.csv`。
**予算は正規の 1/10**（正規は 5 万〜40 万評価でローカルでは回せない）。狙いを定めるための
測定であって、報告する比較ではない。正規予算は GitHub Actions に回すこと（問い 5）。

PR（5 つの精度水準の平均）:

| 手法 | N04 | N05 | N06 | N07 | N08 | N09 | N10 | 平均 |
|---|---|---|---|---|---|---|---|---|
| **NMMSO** | 1.00 | 1.00 | 0.77 | **0.97** | 0.11 | **0.49** | 1.00 | **0.763** |
| MC-ESO | 0.82 | 1.00 | 0.65 | 0.19 | **0.16** | 0.11 | 0.97 | 0.557 |
| NM-Restart | 1.00 | 1.00 | 0.00 | 0.56 | 0.00 | 0.25 | 1.00 | 0.543 |
| NCDE | 0.77 | 1.00 | 0.04 | 0.35 | **0.20** | 0.10 | 0.99 | 0.494 |
| r3pso | 0.77 | 1.00 | 0.31 | 0.13 | 0.06 | 0.02 | 0.48 | 0.396 |
| MC-ESO-crowd | 0.43 | 0.80 | 0.42 | 0.18 | 0.11 | 0.05 | 0.36 | 0.336 |
| DE | 0.25 | 0.50 | 0.06 | 0.03 | 0.01 | 0.00 | 0.08 | 0.133 |

**MC-ESO は 2 位で、7 関数中 6 つで負けている。** ただし負けは均等ではない:

| 関数 | 首位 | 差 |
|---|---|---|
| N07-Vincent2D | NMMSO 0.97 | **−0.78** |
| N09-Vincent3D | NMMSO 0.49 | **−0.38** |
| N04-Himmelblau | NMMSO 1.00 | −0.18 |
| N06-Shubert2D | NMMSO 0.77 | −0.11 |
| N08-Shubert3D | NCDE 0.20 | −0.04 |
| N10-ModRastrigin2D | NMMSO 1.00 | −0.03 |
| N05-SixHumpCamel | 同率 | ±0.00 |

**Vincent の 2 関数を除くと平均差は 0.06 しかない**（MC-ESO 0.72 対 NMMSO 0.78）。
入れると 0.21。つまり**全体の差はほぼ Vincent だけで作られている**。

**負け方が 2 種類に分かれる。**

| 関数 | PR@1e-1 | PR@1e-5 | 落差 | 報告点数 / 最適解数 |
|---|---|---|---|---|
| N06-Shubert2D | 0.80 | 0.15 | **0.65** | 3.1 |
| N08-Shubert3D | 0.33 | 0.01 | **0.32** | 1.0 |
| N07-Vincent2D | 0.19 | 0.19 | 0.00 | 1.7 |
| N09-Vincent3D | 0.12 | 0.10 | 0.03 | **0.4** |

- **Shubert は精度の問題**（問い 4）。盆地は見つけている（PR@1e-1 が 0.80）のに掘り切れず、
  1e-5 で 0.15 まで落ちる。受理規則の深さ／広さのトレードオフそのもの。
- **Vincent は精度の問題ではない**（問い 1）。緩めても PR が上がらない。しかも N09 では
  216 個に対し **88 点しか報告していない**ので、PR の上限が構造的に 0.41 に張り付いている
  （採点側の上限は max(100, 2K) = 432 なので、報告枠が足りないのではなく報告していない）。
  NMMSO は 110 点報告して 0.49、NM-Restart は 142 点で 0.25。

留保: 3 seed・1/10 予算なので順位の細部は動きうる。動かないのは Vincent の桁違いの差と、
Shubert の精度落差の 2 点。NMMSO はこの環境では動くが、クラウド側の Python 3.11 では
落ちる（`pynmmso` が set に `random.sample` を呼ぶ）。
