# 研究ループ（1 時間ごと 1 サイクル）

自動実行される 1 時間ごとの研究サイクルの手順書と、その状態。**このファイルが唯一の引き継ぎ**で、各回はここを読んで続きから始め、ここに結果を書いて終わる。

現在のテーマは受容集合と多様解の監査（[acceptance_topology.md](acceptance_topology.md)）。MC-ESO 側は [mceso.md](mceso.md) / [history.md](history.md)、分野調査は [related_work.md](related_work.md)。

---

## この研究のゴール（毎回、着手する問いをここに照らすこと）

**多峰度の高い問題（大域最適が多い問題）で、既存手法より多くの大域最適を見つける。**

分野の系統的レビュー（[Algorithms 17(10):448, 2024](https://doi.org/10.3390/a17100448)）が
未解決課題として「次元と多峰度が高いときのスケーラビリティ」を挙げ、今後の方向として
"space and time-scalable methods" を名指ししている。実測でも headroom はここに集中する:
K=36 の N07 では最良手法が PR 0.97 に届くのに、K=216 の N09 では 0.49、他手法はほぼ 0.25 未満。

- **主指標**: CEC2013 niching の peak ratio（精度 ε = 1e-1 〜 1e-5）。**特に K の大きい関数**
  （N06 K=18 / N07 K=36 / N08 K=81 / N09 K=216）。
- **この研究における「多様性」の定義**: 精度 ε 以内で発見された相異なる大域最適の数。
  分野の標準定義を採り、自作の定義は使わない。
- **比較対象**: NMMSO / NCDE / Crowding-DE / r3pso / NM-Restart / MC-ESO。現状の首位は NMMSO。
- **手法は自由**。MC-ESO を改善する必要はない。比較対象の 1 つとして残すだけ。
  ただし MC-ESO 自体を壊さない（BBOB-24 dim2 の SR@1e-10 93.5% は既存資産として保つ）。

**問いを選ぶときの条件**: その問いが片付くと、K の大きい関数の peak ratio が上がるか、
上がらない方向を確実に潰せるか。どちらでもない問いは、面白くても後回しにする。

**占有されている領域**（実装前に必ず確認したこと）:

- **報告規則を独立した設計対象にすること**は済み。[Peak Identification in Evolutionary
  Multimodal Optimization](https://pmc.ncbi.nlm.nih.gov/articles/PMC11505590/) が探索と選別を
  2 相に分け、niche 半径不要の HVPI と冗長解を罰する F 値を提案している。
- **報告効率を測ること**も済み。GECCO competition の Dynamic F1（報告解のうち相異なる
  大域最適だった割合の、評価回数に沿った積分）。
- **持続性による盆地分解**は [arXiv 2605.18351](https://arxiv.org/pdf/2605.18351) が占有。

**分かっている手がかり**（過去の測定から。詳細は [acceptance_topology.md](acceptance_topology.md)）:

- **受理規則が深さと広さを同時に決めている。** 親比較なら深さ、最近傍置換なら広さ。
  1 つのつまみでは両立しない（MC-ESO で SR@1e-10 100% → 0%、DE / PSO でも同傾向）。
- **効くのは返す個数ではなく、上位が互いに別物であること。**
- 現状 MC-ESO は Shubert で「盆地は見つけるが掘り切らない」（PR@1e-1 0.80 → PR@1e-5 0.15）、
  Vincent で「そもそも見つけない」（精度を緩めても 0.19 のまま）と、負け方が 2 種類ある。

---

## 1 サイクルの手順

**このループは 1 時間ごとに走る。前の回がまだ走っている可能性があるので、手順 0 と 1 を飛ばすと二重着手になる。**

0. **ブランチを合わせる** — `git fetch origin && git checkout research-loop && git pull --rebase origin research-loop`（無ければ main から作る）。**成果は全部この 1 本に積む。** 実行ごとにブランチを切らない。切ると前回の続きから進めなくなり、統合されない成果物が散る。main には触らない（人がマージする）。
1. **問いを 1 つ claim する** — 「未解決の問い」の上から、まだ claim されていないものを 1 つ選び、その行に `(claimed YYYY-MM-DD HH:MM UTC)` と書いて**即コミット・push する**。これで他の回は別の問いを選ぶ。**2 時間以上前の claim は放棄されたものとみなして奪ってよい**（奪ったら時刻を書き換える）。
2. **統合の回かを判定する** — 「作業ログ」の件数が **8 件を超えていたら、新しい実験をせずに統合作業をする**。すなわち: ログの結論を `acceptance_topology.md` の該当節に集約し、決着した問いを「未解決の問い」から消し、ログを直近 3 件＋要約に畳む。積み上がらない一番の原因は記録が増えるだけで整理されないことなので、これは実験より優先する。
3. **先行研究を潰す** — 実装の前に、隣接分野の語彙で検索する（下記「先行調査の鉄則」）。既にあるなら問いを畳んで記録し、次の問いへ。
4. **設計する** — 何を測れば白黒つくか、どういう結果なら仮説が否定されるかを先に書く。撤退条件のない実験はしない。
5. **実装して回す** — 小さく回して配線を確認してから本測定。既存スクリプトで足りるなら新規作成しない。**1 回の計算は 40 分以内**に収める。収まらないなら関数か seed を削り、削ったと書く。
6. **分析する** — 対応のある検定と効果量、seed 方向一致率。平均だけで結論を出さない。
7. **記録してコミットする** — 「作業ログ」に追記し、claim を外して結果を書く（決着したら問いを消す、続きがあれば次の問いとして書く）。**否定的結果も機序つきで残す**。`research-loop` に push する。push が競合したら `git pull --rebase` して再試行。

**1 回で全部やろうとしない。** claim した問いが 40 分で終わらないと分かったら、そこまでの結果と「次はここから」をログに書いて終える。中途半端な状態を残すより、次の回が続きから入れる形にすることが優先。

---

## 守る規則（これまでの失敗から）

**先行調査の鉄則** — 新しい定式化を思いついたら、**実装より先に 4 分野以上の語彙で検索する**。この研究では 2 回、提案の中核が先行研究と一致して撤退した。

- 進化計算（niching / QD / EDO）だけでなく、ロバスト最適化（K-adaptability, ROMS）、設計工学（solution space engineering, set-based design）、計算化学（disconnectivity graph, barrier tree, NEB）、統計（cluster tree）、TDA（merge tree, persistence）、能動学習（level set estimation）の用語で引く。
- 「barrier tree は調べたが disconnectivity graph では引かなかった」で 1 日を失った。**同じ概念の別名を列挙してから検索する。**

**測定の鉄則**

- **単一 seed で結論を出さない。** 1 seed では NM-Restart が最良と出た関数で、3 seed では最下位になった。
- **集計から落ちた対象を必ず報告する。** 比が定義できない関数を黙って除くと順位が変わる（実際に変わった）。
- **定義を変えたら結果がどう動くかを測る。** τ・重複除去半径・行動記述子・シナリオモデルはいずれも答えを桁で動かした。定義依存性は欠陥ではなく報告事項。
- **推定器の誤りの向きを述べる。** 線分による連結判定は繋がりを見落とす側にしか誤らない、など。

**MC-ESO を壊さない**

- `SR@1e-10` を下げる構成は採用しない（多解の改善でも例外なし）。
- 既定値を変えたら **BBOB-24 dim2 / n=20 で SR@1e-10 93.5% / evals_succ_mean 798** と一致することを確認してからコミットする。
- 診断用の変種は本体を書き換えず別ファイルに置く（例: `mceso_crowding.py`）。

---

## 未解決の問い（上から着手する）

claim 中のものは `(claimed ...)` が付く。2 時間以上前の claim は奪ってよい。

**方針（2026-09-02、2 度目の転換）**: MC-ESO の改善という制約を外し、「多峰度の高い問題での
peak ratio」をゴールに置き直した。報告規則の路線は先行研究に占有されていたため取り下げ
（上記「占有されている領域」）。

1. **PR が「最適解 1 個あたりの評価回数」で決まるかを測る** `(claimed 2026-09-02 16:59 UTC — ローカルで実施中)` — K=36 で 0.97 の手法が
   K=216 で 0.49 に落ちる。予算を振って PR を evals/K に対してプロットし、関数と手法を
   またいで 1 本の曲線に乗るかを見る。乗るなら手法の良し悪しは定数 1 つに落ち、
   **改善対象が「最適解 1 個を見つけて報告するのに要する評価回数」という単一量になる**。
   乗らないなら地形の何が効いているかを分離する。どちらでも次が決まる。
   `scripts/niching_baseline.py` に予算掃引を足せばよい。棄却条件は特になし（記述的測定）。
2. **Vincent で見つけているのに報告していないのかを判定する** — N09 では 216 個に対し
   88 点しか報告しておらず、PR の上限が構造的に 0.41 に張り付いている（採点上限は 432）。
   `scripts/diagnose_niching.py` が探索履歴と報告集合を分けて数える。触れているのに報告して
   いないなら記録の問題で追加評価ゼロで直る。棄却条件は visited ≈ reported。
3. **Shubert の高精度側の崩れ** — 盆地は見つけているのに掘り切れていない。受理規則の
   2 段構え（σ が枯れるまで親比較、枯れた後だけ最近傍置換）で両立するか。
   判定は PR@1e-5 が上がり、かつ BBOB-24 dim2 の SR@1e-10 が 93.5% を割らないこと。
4. **正規予算での比較を Actions に載せる** — 現状の測定は正規の 1/10 予算。順位の細部は
   これでは確定しない。正規予算（5 万〜40 万評価）での比較を GitHub Actions で回す。
5. **投稿先の選定** — GECCO。締切と要求形式を調べる。

**畳んだ問い**: 報告規則の転用（先行研究に占有）、監査側の残り（全滅の機序の次元不変性、
共通探索での比較、断片化ゲート）。結論は `acceptance_topology.md` に残っている。

---

## 作業ログ

新しい回ほど上に書く。1 回 1 節、日付・やったこと・分かったこと・次に繋がる問い。

> **現在 10 件。8 件を超えているので、次の回は新しい実験をせずに統合の回にすること**（手順 2）。
> ゴールが peak ratio に変わったので、統合では監査側のログを `acceptance_topology.md` に
> 送り、ログにはゴールに効く結論だけを残すこと。

### 2026-09-02 その16（全滅の機序は次元不変 — その11 を確定、ただし代償は次元とともに膨らむ）

問い 1（全滅の機序の次元不変性）。その11 が報告したまま失われ、その13 では予算不足で
確認できていなかった点。`--methods none` では回せない（ρ を手法をまたいで取るため）ので
手法を 11 → 7 に絞って回した。データは `analysis/dim/wipe_cut{C}_d{D}.csv.gz`（7 本、
7 手法 ＋ 参照規則 2 つ × BBOB-24 × 5 seed × 30 シナリオ。cut 0.10 と 0.25 を dim 2/3/5、
cut 0.40 は dim2 のみ。4 並列で 16 分）。実装は `wipeout.py` に `--spread-from-rows` と
gzip 読みを足しただけ（`set_properties.csv` は 2 次元専用なので、他の次元では行の `spread` 列を
使うほかない）。詳細は
[acceptance_topology.md の該当節](acceptance_topology.md#全滅の機序は次元不変ただし全滅の代償は次元とともに膨らむ2026-09-02-scriptswipeoutpy---spread-from-rows)。

**その11 の報告は確定した。** ρ(spread, 全滅率) は 6 セルすべてで **−0.88 〜 −0.94、
符号一致は毎回 24/24**（cut 0.25 で dim2 −0.92 / dim3 −0.94 / dim5 −0.89）。全滅率も
30〜36% でほぼ動かない（cut 0.25 で 32 / 34 / 36%）。**「高次元では集合を丸ごと半空間に
入れにくいはず」という事前予想は、次元を上げても全滅率が下がらないことで否定される。**
ρ を取った spread の範囲は次元とともに**広がる**（0.468 → 0.546 → 0.614）ので、高次元の ρ が
範囲の圧縮で作られた見かけでもない。なおその11 は全滅率を 35/34/32%（減少）と報告したが、
実測は 32/34/36%（微増）。水準は一致、微小な向きは逆。

**新しいこと 1 — 全滅の代償は次元とともに一桁上がる。** 正規化した損失(全滅時) は
1.13 → 1.69 → **10.69**（cut 0.25）。分母は各関数の `quality` 規則自身の p75 regret なので
分母の縮小を疑うべきところだが、生の regret でも中央値 10.2 → 9.6 → **35.9** と同じ跳ね方を
する。全滅率がほぼ一定なのだから、**跳ねているのは頻度ではなく 1 回あたりの深さ**。
生存時の損失は 0.00 → 0.04 → 0.67（生）で、比では全滅時の 1/50 のまま。「損失は全滅時にしか
出ない」は dim5 でも成り立つが、**厳密な 0 ではなくなる**。

**新しいこと 2（MC-ESO に直接効く）— 9 系列中、次元とともに spread が減るのは MC-ESO だけ。**
0.176 → 0.139 → 0.117（cut 0.25、span 比）。他の 8 系列は全部増える（NM-Restart 0.422 →
0.552、上位 K ですら 0.171 → 0.377）。結果として全滅率の悪化も最大で 34 → 42 → **46%**
（他は −3 〜 +9 ポイント）。**dim2 では上位 K と同等の spread を持っていた提案手法が、
dim5 では上位 K の 1/3 の広がりしか返していない。** 多解を狙う手法としては具合が悪い。

削ったもの: 手法を 11 → 7 に絞った（NMMSO は Python 3.11 で落ちる、DE / r3pso /
MC-ESO-crowd は予算で落とした）。関数あたりの相関点は 13 → 9。**この削減は結論を変えない**
— dim2 / cut 0.25 を旧 11 手法版（`analysis/wipe_cut025.csv`）と比べると全滅率 34% → 32%、
ρ −0.87 → −0.92。cut 0.40 も dim3/dim5 では落とした（40 分に収めるため。dim2 で領域サイズ
不変性は既に 3 水準で確認済み、今回も 0.10 と 0.25 の 2 水準で次元方向を押さえている）。
運用: 生 CSV は 7 本で 11MB なので gzip した（1.2MB）。`wipeout.py` は両方読む。

**この回とゴール転換の関係。** この測定は監査側の問い（旧 問い 1）として claim して回した
もので、その最中に並行回のその15 がゴールを peak ratio の改善に切り替えた。**監査側の問いは
畳んで正しい**（全滅率は BBOB の禁止領域シナリオ上の量で、peak ratio には効かない）。
畳まずに残す価値があるのは**新しいこと 2 だけ**で、これはゴールに直接効く:

> **MC-ESO は次元が上がると報告集合が縮む。** 他の 8 系列（上位 K を含む）が全部広がる中で
> 唯一逆を向く。peak ratio の言葉に直せば「3 次元では 2 次元より、相異なる解を報告しにくく
> なっている」で、その15 が N09-Vincent3D で見つけた「216 個に対し 88 点しか報告していない」
> と同じ向きの現象を、別のベンチマーク・別の指標で独立に捉えたことになる。
> その15 の問い 3（見つけているのに報告していないのか）は、**報告側が縮んでいる**という予測を
> 持って入れる。

したがって次の回への引き継ぎは、新しい問いを積むことではなく**その15 の問い 3 に上の予測を
添えること**（問い 3 の本文に追記済み）。残りの機序（全滅の代償が次元とともに一桁上がる）は
`acceptance_topology.md` に置いた。peak ratio に効かないので問いには昇格させない。


### 2026-09-02 その15（現状の立ち位置 — MC-ESO は 2 位、負けは Vincent に集中）

新しいゴール（peak ratio の改善）の出発点を測った。`scripts/niching_baseline.py`、
CEC2013 の N04-N10 × 7 手法 × 3 seed、`analysis/niching_baseline.csv`。
**予算は正規の 1/10**（正規は 5 万〜40 万評価でローカルでは回せない）。狙いを定めるための
測定であって、報告する比較ではない。正規予算は GitHub Actions に回すこと。

PR（5 つの精度水準の平均）:

| 手法 | N04 | N05 | N06 | N07 | N08 | N09 | N10 | 平均 |
|---|---|---|---|---|---|---|---|---|
| **NMMSO** | 1.00 | 1.00 | 0.77 | **0.97** | 0.11 | **0.49** | 1.00 | **0.763** |
| MC-ESO | 0.82 | 1.00 | 0.65 | 0.19 | **0.16** | 0.11 | 0.97 | 0.557 |
| NM-Restart | 1.00 | 1.00 | 0.00 | 0.56 | 0.00 | 0.25 | 1.00 | 0.543 |
| NCDE | 0.77 | 1.00 | 0.04 | 0.35 | **0.20** | 0.10 | 0.99 | 0.494 |
| r3pso | 0.77 | 1.00 | 0.31 | 0.13 | 0.06 | 0.02 | 0.48 | 0.396 |
| MC-ESO-crowd | 0.43 | 0.80 | 0.42 | 0.18 | 0.11 | 0.05 | 0.36 | 0.336 |
| DE | 0.25 | 0.50 | 0.06 | 0.03 | 0.01 | 0.00 | 0.08 | 0.133 |

**MC-ESO は 2 位で、7 関数中 6 つで負けている。** ただし負けは均等ではない:

| 関数 | 首位 | 差 |
|---|---|---|
| N07-Vincent2D | NMMSO 0.97 | **−0.78** |
| N09-Vincent3D | NMMSO 0.49 | **−0.38** |
| N04-Himmelblau | NMMSO 1.00 | −0.18 |
| N06-Shubert2D | NMMSO 0.77 | −0.11 |
| N08-Shubert3D | NCDE 0.20 | −0.04 |
| N10-ModRastrigin2D | NMMSO 1.00 | −0.03 |
| N05-SixHumpCamel | 同率 | ±0.00 |

**Vincent の 2 関数を除くと平均差は 0.06 しかない**（MC-ESO 0.72 対 NMMSO 0.78）。
入れると 0.21。つまり**全体の差はほぼ Vincent だけで作られている**。

**負け方が 2 種類に分かれる。**

| 関数 | PR@1e-1 | PR@1e-5 | 落差 | 報告点数 / 最適解数 |
|---|---|---|---|---|
| N06-Shubert2D | 0.80 | 0.15 | **0.65** | 3.1 |
| N08-Shubert3D | 0.33 | 0.01 | **0.32** | 1.0 |
| N07-Vincent2D | 0.19 | 0.19 | 0.00 | 1.7 |
| N09-Vincent3D | 0.12 | 0.10 | 0.03 | **0.4** |

- **Shubert は精度の問題**。盆地は見つけている（PR@1e-1 が 0.80）のに掘り切れず、
  1e-5 で 0.15 まで落ちる。受理規則の深さ／広さのトレードオフそのもの。
- **Vincent は精度の問題ではない**。緩めても PR が上がらない。しかも N09 では 216 個に対し
  **88 点しか報告していない**ので、PR の上限が構造的に 0.41 に張り付いている
  （採点側の上限は max(100, 2K) = 432 なので、報告枠が足りないのではなく報告していない）。
  NMMSO は 110 点報告して 0.49、NM-Restart は 142 点で 0.25。

留保: 3 seed・1/10 予算なので順位の細部は動きうる。動かないのは Vincent の桁違いの差と、
Shubert の精度落差の 2 点。NMMSO はこの環境では動くが、クラウド側の Python 3.11 では
落ちる（`pynmmso` が set に `random.sample` を呼ぶ）。


### 2026-09-02 その14（空振りを潰したら worst が下がった — 仮説棄却、機序つき）

問い 1（帯の中の候補数で τ を決める適応版）。その13 の「空振り＝検出力の損失」という読みを
前提に、帯の**幅**ではなく**中の候補数**を決める版を作って空振りを潰した。データは
`analysis/dim/tau_{model}_d{D}_p{P}_adapt.csv.gz`（18 本、固定 τ 8 水準 ＋ 適応 15 水準 ×
3 モデル × BBOB-24 × 5 seed、dim 2/3/5 × プール 50/150、`--methods none`、計算 9 分）。
**固定版と適応版を同じプール・同じシナリオで採点している**ので対応比較になる。実装は
`scenario_value.py` の `--tau-n` / `--tau-n-caps`（本体の規則は触っていない）。
詳細は [acceptance_topology.md の該当節](acceptance_topology.md#空振りは検出力の損失ではなくヘッジ2026-09-02-scenario_valuepy---tau-n)。

**適応は設計どおり動いた。** 空振りは構造的下限（プールが K 個以下の局所最適しか持たない
単峰系のセル。どんな規則も上位 K と一致するほかない）まで落ちる: dim2/150 で 68→46（下限 45）、
dim3/150 で 73→46（45）、dim5/150 で 68→39（39）、dim5/50 で 106→50（39）。次元ごとの
調整は一切していない。必要な幅がプールの密度で決まるという前提も実測で正しい。

**それでも worst は改善せず、棄却。** 適応が固定を上回るのは 6 セル中 2 つ（dim3 の両本数）
だけで、事前の棄却条件（dim2/150 +7 / dim3 +3 / dim5 +2 を超えること）を満たさない。
6 セル全部で使える単一設定でも固定 τ=0.05（最小 0）が最良で、適応はどれも届かない。
なお固定側の値は その13 と一致した（dim2/50 +4、dim3 +3、dim5/50 0）ので、再測定としても
妥当。

**機序（これが本題）— 空振りしていたセルは、規則が動くと負けるセルだった。**
`spreadN@15` を規則が選んだ幅で層別すると、tilt では幅 ≤ 0.1 のセルで 38 勝 2 敗（dim2）
なのに幅 > 0.1 では 9 勝 23 敗、dim5 では 16 勝 **47 敗**。逆に constraint の dim5 は同じ
広帯セルで 65 勝 6 敗。帯を広げないと K 個入らないセルは定義上**局所最適の値が離れている**
セルで、そこで広げれば名目品質を大きく捨てる。解を消さない tilt ではそれが純損、
解を消す constraint / instance では大勝ち。worst は 3 モデルの最小なので tilt の負けが
constraint の勝ちを打ち消す。

> **空振りは検出力の損失ではなく、モデルによって最適な行動が逆を向くセルで規則が
> 賭けを降りる暗黙のヘッジだった。** dim5/50 が一番はっきりしている: 固定 τ の worst が 0
> なのは 118/120 で空振りしているからで、発火させると 0 は正にならず **負になる**（−1〜−13）。

系: **安全性を担っているのは候補数ではなく幅の上限**。幅で頭打ちにした適応版は固定 τ に
退化する（dim5/50 の `spreadNc0.1@8` は全セルで幅 0.100 を選び、空振り数も固定 τ=0.1 と一致）。

**副産物で問い 2 が片付いた。** 空振り率を構造的下限に揃えた状態でも worst は次元とともに
下がり続ける（`spreadN@15` で +4 / 0 / −8、`spreadN@6` で +6 / +3 / +1、dim 2/3/5 プール 150）。
**解像度を揃えても次元の効果は残るので実在する次元の効果**で、参照プールの解像度の
見かけではない。問い 2 は消した。

削ったもの: なし（dim 2/3/5 × プール 50/150 の全 18 セルを回せた）。dim10 は今回も未測定。
運用上の注記: 生 CSV は 18 本で 80MB になるので **gzip で保存**した（5.9MB）。
`tau_choice.py` は `.csv` と `.csv.gz` の両方を読む。

次に繋がる問い: 上の機序は「規則が発火すべきかどうかはモデル依存で、しかも観測できない」と
言っている。ただし**幅を広げねばならないかどうかは観測できる**（プールから直接読める）ので、
その12 の断片化ゲート（問い 3）と同じ形の話になる。問い 3 を測るときは、ゲートの条件に
`k_big > 1` だけでなく「帯を広げずに K 個入るか」を並べて比較すること。

### 2026-09-02 その13（その11 を再現した — 2 点は一致、1 点は再現しない）

問い 1（その11 の次元測定をリポジトリに載せる）。**この回は push が通った**ので、その11 が
失った成果はここで確定する。データは `analysis/dim/tau_{model}_d{D}_p{P}.csv`（18 本、
8 τ 水準 × 3 モデル × BBOB-24 × 5 seed、dim 2/3/5 × プール 50/150 本、`--methods none`）。

実装: `scenario_value.py` に `--dim` / `--methods` / `--pool-starts`。`tau_choice.py` に
**空振りセル数**（許容帯に K 個以下しか候補が入らず規則が上位 K を返す＝比較が構成上の
引き分けになるセル）を追加した。詳細は
[acceptance_topology.md の該当節](acceptance_topology.md#次元と参照プールの解像度2026-09-02-scriptsscenario_valuepy---dim--pool-starts)。

**その11 の 3 点のうち、確認できたのは 2 点:**

- τ=0.1 は dim3 で安全（最悪 **+3**）、dim5 で崩れる（最悪 **−3**、tilt で 0-3）。数値まで一致。
- dim5 / 50 本での空振りは **105-106 / 120 セル**。報告値 105 と一致。

**再現しなかったのは「150 本にすれば dim5 の τ=0.1 は再び安全（+2）」。** 実測は **−1**
（tilt 2-3）。150 本にして戻るのは規則が**測定可能になること**（空振り 105→68、5 seed 全部
空振りで勝敗を記録しえない「死んだ関数」15→10）であって、その τ の安全性ではない。
dim5 で安全なのは τ=0.05（+2）で、しかも 150 本が要る。**次元と解像度は独立に効いており、
片方をもう片方で説明できない。**

**その11 が見ていなかった一番大きいこと: 空振りは dim5 の現象ではない。**
dim2 / 50 本でも τ=0.1 は 120 セル中 **78** で空振りし、24 関数中 **13** が死んでいる。
つまり既存の dim2 の τ 表（7-0 / 6-1 / 6-2）は、規則が 3 分の 1 のセルでしか発火していない
状態で測られていた。符号は変わらないが検出力は見た目よりずっと低い。プール本数は次元に
よらず一次の効果を持ち、dim2 でも 50→150 本で最良の最悪値が +4 → **+7** に上がる。

副産物（機序つきの注意 2 件）:

- **`--methods none` は近似ではなく恒等**。regret はどの規則も同じ per-scenario の
  `best_known` を引いた値なので、手法を落とすと regret が**共通の定数だけ**ずれ、規則どうしの
  対応差は変わらない。実測でも絶対値は最大 1.0 ずれる一方、対応差は相対 6e-5 以内・符号反転 0。
  その11 が「ずれ 0.001 = 丸め幅」と報告したのは、この恒等式が CSV の 6 桁丸めで濁ったもの。
- **保存済み CSV はライブラリ版が変わると桁まで再現しない**。dim2 / 50 本を今の環境で引くと
  3600 セル中 161（4.5%）が既存 `analysis/tau_constraint.csv` と食い違う。すべて多峰な関数
  （F03/F07/F13/F15/F17/F19/F23/F24）で、L-BFGS-B の停止点が僅かに動いてプールの 0.01·span
  重複除去の判定が反転するため。結論は動かない（constraint 7-0 → 8-0、instance 6-1 → 6-1）。
  プール生成の実装自体は 50 本では旧ループとビット単位一致を確認済みなので、差は環境由来。

削ったもの: **dim10 は時間で落とした**（40 分に収めるため。dim 2/3/5 で 27 分）。
**全滅の機序の次元不変性（ρ ≈ −0.9）は未確認** — `wipeout.py` は ρ を手法をまたいで取るので
`--methods none` では回せず、手法を回す予算がなかった。問い 3 に残した。

環境: `pynmmso` は Debian の setuptools と衝突するので `pip install --ignore-installed
setuptools wheel` を先に入れる。入れても **NMMSO は Python 3.11 で落ちる**
（pynmmso が set に `random.sample` を呼ぶ）ので、この環境では NMMSO 抜きで回すこと。

### 2026-09-02 その12（断片化では説明できなかった — 仮説棄却、ただし別の軸が見えた）

未解決の問い 4。「instance では distinct（解の数）が効き、constraint では spread（距離）が
効く」の理由を、受容集合の連結成分数で説明できるか測った。仮説は「成分が多いほど各成分に
代表を置くこと＝数が効き、単一の塊なら禁止領域は連続的に食い込むので距離が効く」。
成立するなら、成分数で層別したときにモデル間の差が縮むはず。
`scripts/stratify_components.py`（`analysis/components_bbob.csv` × `set_properties.csv` ×
3 モデルの A12）。

gap = ρ(distinct) − ρ(spread) を層ごとに:

| モデル | 単一 (k=1) | 少数 (2-4) | 多数 (5+) |
|---|---|---|---|
| instance | +0.16 | +0.13 | +0.23 |
| constraint | −0.20 | −0.10 | −0.17 |
| tilt | −0.09 | −0.06 | +0.24 |

**棄却。** gap は層ではなくモデルで決まっている。instance はどの層でも正、constraint は
どの層でも負。層別しても差は縮まない（事前に置いた棄却条件に該当）。分位を 0.01 / 0.05 / 0.1 と
振っても同じで、**定義に依存しない棄却**（このプロジェクトでは τ・重複除去半径・行動記述子と
定義依存の結果が続いていたので、これは記録に値する）。

代わりに、**独立な 2 軸**が見えた:

- **断片化は「幾何がそもそも効くか」を決める。** tilt の単一成分層では ρ(spread)=0.01、
  ρ(distinct)=−0.08 と何も効かないのに、多数成分層では 0.64 / 0.88 になる（3 分位すべてで同傾向）。
  これは以前の「単一成分の関数では A12 が全 11 関数で 0.50」と整合する独立な確認。
- **シナリオモデルは「幾何のどの側面が効くか」を決める。** 数か距離かは層に関係なくモデルで決まる。

含意が 1 つある。未知の種類は観測できないが**断片化は観測できる**（オフラインで推定できる）。
つまり「どれだけ多様性を買うか」は決められなくても、「そもそも買う価値があるか」は
問題側から判定できる。これを次の問いにした。

留保: 層の関数数が 12 / 6 / 4 と少ない。k_big=0 の 2 関数（F23、F24）はこの分位で受容集合が
取れず除外している。

### 2026-09-02 その11（次元での検証 — クラウド回の報告。**その13 で検証済み、上記も参照**）

13:30 のクラウド回が問い 1 を取って完走したが、**push が 403 で拒否されたため成果が
リポジトリに載っていない**（`remote: Claude doesn't have GitHub access to
kosei-matsuzaki/optimization for your organization`。fetch は通り push だけ不可、
GitHub MCP の `push_files` も `403 Resource not accessible by integration`）。
コミット 5 本と CSV 12 個はコンテナ内にのみ存在し、回収は破棄される。

**以下はその回の報告であって、こちらで数値を確認できていない。**
データファイルがないので再現するまで確定として扱わないこと。次の回が同じ測定を
やり直さないために記録しておく。

報告された内容:

- `scripts/scenario_value.py` に `--dim`（3/5/10）・`--methods`・`--pool-starts` を追加。
  シナリオモデルは元から次元非依存なので追加実装は不要だった。
- τ 8 水準 × 3 モデル × BBOB-24 × 5 seed を dim 2/3/5 で掃引。τ=0.1 は dim3 で余裕を保ち
  （最悪 +3）、dim5 で失う（最悪 −3、これは事前に置いた棄却条件に該当）。
- **ただしその崩れは次元ではなく参照プールの解像度**。dim5 では 50 本の multistart だと
  許容帯に K 個未満しか候補が入らず、120 セル中 105 で規則が空振りして比較が構成上の
  引き分けになる。150 本にすると空振りが 71 に減り τ=0.1 は再び安全（+2）。
  安全域自体は次元とともに狭まる（dim2 は τ=0.15 まで安全、dim5 は 0.1 まで）。
- 全滅の機序は**次元不変**。全滅率 35 / 34 / 32%、ρ(spread, 全滅率) −0.86 / −0.91 / −0.87。
  「高次元では集合を丸ごと消しにくいはず」という事前予想は否定された。

手順上の教訓: **claim は push できて初めて機能する。** この回は claim を push できなかったので
二重着手を防げていなかった。並行して走っていたその10 の回が別の問い（問い 2）を取っていたのは
偶然。push が通らない環境では、claim 方式そのものが働かない。

### 2026-09-02 その10（基準側の予算を実測した — 交絡は否定）

監査の参照規則（上位 K、complement、spread@T）はすべて 50 回の L-BFGS-B multistart で
作ったプールから選んでいる。手法の予算は 5000 評価。**プールが何評価使っているかを
これまで一度も測っていなかった**（コード上のコメントは "same budget in spirit"）。
50 × maxfun 2000 なら最大 10 万評価で、もし実際にそれに近ければ監査の見出しは
多様性の話ではなく予算の話になる。`scripts/pool_cost.py` で実測した。

**中央値 0.61 倍、最大 3.02 倍、24 関数中 11 関数（46%）が予算超過。** 致命的ではないが
無視できない。超過が大きいのは F06（3.02）、F13（2.29）、F23（2.10）、F10（1.88）、F11（1.85）。
低次元では L-BFGS-B の 1 反復あたり有限差分勾配で dim 回余分に食うのが主因。

予算内に収まる 13 関数だけで τ の表を作り直した:

| モデル | τ=0.05 | τ=0.1 | τ=0.25 | τ=0.5 | τ=1 |
|---|---|---|---|---|---|
| constraint（全 24） | 5-0 | 7-0 | 7-1 | 9-0 | 9-1 |
| constraint（予算内 13） | 4-0 | 6-0 | 6-0 | 7-0 | 7-1 |
| instance（予算内 13） | 3-1 | 5-1 | 5-1 | 7-1 | 7-2 |
| tilt（全 24） | 6-1 | 6-2 | 3-4 | 2-10 | 1-11 |
| tilt（予算内 13） | 6-0 | 5-1 | 3-3 | 2-7 | 1-8 |

**結論は変わらない。** τ=0.1 は 3 モデルとも勝ち越し（6-0 / 5-1 / 5-1）、大きい τ は tilt で
崩れる（1-8）。しかも予算内サブセットのほうが負けが少ないので、**交絡は規則に不利な向きに
効いていた**。予算超過が結果を作っていたわけではない。

残る非対称は予算ではなく**探索の種類**。参照規則はオフラインプールから、手法は自前の探索から
選んでいる。ここを揃える比較は問い 2 に残した。

### 2026-09-02 その9（機序の直接確認 ＋ ログの統合）

前の回で「品質帯が狭いと既に離れた集合は縮む」を全滅率から推論していた。spread を
`scenario_value.py` の CSV に直接記録して確かめた（`analysis/post_constraint.csv`）。

| 手法 | spread | 後処理後 | 減った関数 | 全滅率 | 後処理後 |
|---|---|---|---|---|---|
| NM-Restart | 0.422 | 0.192 | 17/24 | 24% | 36% |
| NCDE | 0.284 | 0.177 | 15/24 | 25% | 36% |
| MC-ESO | 0.179 | 0.090 | 17/24 | 33% | 41% |
| MAP-Elites | 0.219 | **0.281** | 12/24 | 30% | **27%** |
| Crowding-DE | 0.171 | **0.203** | 12/24 | 34% | **33%** |

推論どおり。既に離れた集合を返す手法では spread が減り全滅率が上がる。逆に MAP-Elites と
Crowding-DE では spread が増えて全滅率が下がる（これらは離れているが品質の揃わない点を
多く返すので、帯で絞ると良い方向に働く）。

例外として IPOP-CMA-ES は spread が減る（0.153→0.108、16/24）のに constraint で 15 勝 4 敗と
改善する。品質帯の利得が spread の損失を上回る場合があるということで、**2 つの効果は独立に
効いており、どちらが勝つかは手法による**。NMMSO は多くの関数で K 個以下しか返さないため
後処理が適用されず（4/24 でしか動かない）、この比較から実質的に外れる。

以降は手順に従いログを統合した。決着した問い（受容集合の形、シナリオモデルと性質の対応、
15 seed 再現性、禁止領域の機序、報告規則、τ の選択、後処理の可否）は
`docs/acceptance_topology.md` に集約済みで、ここには直近 3 件だけを残す。

### 2026-09-02 その8（後処理としては一律に効かない — 提案の形が変わる）

τ=0.1 の規則を、オフラインプールからではなく**各手法が返した集合そのもの**に適用する
後処理版を実装し（`--postprocess`、接尾辞 `+s`）、3 モデル × BBOB-24 × 5 seed で
各手法の素の上位 K と対応比較した（`analysis/post_{constraint,instance,tilt}.csv`）。

| 手法 | tilt | instance | constraint | 全滅率 constraint |
|---|---|---|---|---|
| DE | 19-0 | 15-0 | 13-0 | 49%→48% |
| IPOP-CMA-ES | 17-4 | 11-6 | 15-4 | 38%→41% |
| PSO | 17-2 | 12-6 | 11-5 | 47%→49% |
| r3pso | 16-3 | 15-6 | 13-5 | 39%→42% |
| MC-ESO | 17-3 | 7-11 | 5-9 | 33%→41% |
| NCDE | 16-5 | 7-14 | 8-10 | 25%→36% |
| MAP-Elites | 13-1 | 7-10 | 9-4 | 30%→27% |
| MC-ESO-crowd | 12-5 | 3-14 | 8-13 | 29%→37% |
| NM-Restart | 14-3 | **1-15** | **1-12** | 24%→36% |

**一律には効かない。効く手法と壊す手法が分かれる。** 崩れた集合を返す手法（DE、IPOP、PSO、
r3pso）では 3 モデルとも改善する。もともと良い集合を返す手法（NM-Restart、NCDE、MC-ESO、
MC-ESO-crowd）では instance と constraint で悪化し、NM-Restart は 1 勝 15 敗まで落ちる。

機序: この規則は**品質帯フィルタ + 帯の中での距離最大化**の 2 段。品質帯 τ=0.1 が狭いので、
すでに離れた良い点を持っている集合では「離れているがわずかに悪い点」が帯から落ち、
結果として集合が縮む。実際、全滅率が上がっている（NM-Restart 24%→36%、NCDE 25%→36%、
MC-ESO 33%→41%）。全滅率は spread と ρ=−0.85 で結びついているので、これは spread が
下がったことを示す。ただし**間接的な推論**なので、spread を直接測るのを次に回す。

tilt で全手法が改善するのも同じ機序で説明できる。線形バイアスの下では品質が効くので、
品質帯フィルタとして働くぶんが素直に得になる（帯の中の距離最大化は tilt では効かない）。

**提案の形が変わる。** 「どの最適化手法にも後付けできる wrapper」ではない。参照規則としての
結果（その7）は生きているが、それは共通のプールから選ぶ**報告規則の設計選択**としての主張で
あって、既存手法の出力を後処理する主張ではない。既に良い報告集合を作っている手法に対しては、
狭い品質帯で絞り直すことは害になる。

### 2026-09-02 その7（許容幅は未知の種類を知らなくても選べる — 決着）

前の回で「τ を選ぶにはどのモデルに直面するか知っている必要がある」を弱点として残した。
ただしどのモデルかは**未知の側の性質**であって地形から観測できる量ではないので、地形特徴で
τ を選ぶ筋は原理的に成り立たない。使える問いは「全モデルで害にならない τ があるか」だけ。

τ を 8 水準（0.02〜2.0）に振り、3 モデルで BBOB-24 × 5 seed を測った
（`analysis/tau_{constraint,instance,tilt}.csv`）。対応のある Wilcoxon（α=0.05）で
関数ごとの勝ち負けを数え、`scripts/tau_choice.py` にまとめた。

| τ | constraint | instance | tilt | 最悪 |
|---|---|---|---|---|
| 0.02 | 3-0 | 2-0 | 4-1 | +2 |
| 0.05 | 5-0 | 4-1 | 6-1 | +3 |
| **0.1** | **7-0** | **6-1** | **6-2** | **+4** |
| 0.15 | 6-1 | 6-1 | 5-3 | +2 |
| 0.25 | 7-1 | 6-1 | 3-4 | −1 |
| 0.5 | 9-0 | 9-1 | 2-10 | −8 |
| 1.0 | 9-1 | 9-2 | 1-11 | −10 |
| 2.0 | 9-1 | 8-1 | 1-11 | −10 |

**弱点は消えた。** τ = 0.1 は 3 モデルすべてで勝ち越し、最悪でも 6 勝 2 敗。未知の種類を
知らずに固定値で使える。モデル依存が問題になるのは τ ≥ 0.25 の広い許容幅だけで、
そこから先は tilt での損失が急に効いてくる（0.25 で −1、0.5 で −8）。

制約と留保:

- 最適点の**位置**は 5 seed では確定しない。0.05 / 0.1 / 0.15 の最悪値は 3 / 4 / 2 で
  差が小さい。頑健なのは「内部に安全域が存在すること」と「0.25 を超えると崩れること」の 2 点。
- 効果の大きさは τ = 0.1 では小さい。禁止領域下の全滅率は 35% → 33% しか下がらない。
  大きく下げる（24%）には τ = 1 が要るが、それは tilt で 1 勝 11 敗になる設定。
  **安全な設定と効果の大きい設定は別**というのがこの表の一番の含意。
- 深さは構成上不変（最良点を必ず残す）。追加評価もゼロ。

次: ここまでの τ 評価はオフラインプールから選ぶ参照規則としてのもの。実手法の出力に
**後処理**として掛けて同じ効果が出るかを確かめる（未解決の問い 1 に格上げ）。

### これまでの到達点（その1〜その6 を集約、詳細は acceptance_topology.md）

- 多解手法の報告集合は、未定式化基準のもとで「上位 K を報告する」に勝ち越さない。
- ただし結論はシナリオモデルで反転する。モデルごとに**別の性質が採点されている**ため:
  instance では distinct な解の数（ρ=0.83）、constraint では空間的な広がり（0.33）、
  tilt では何も効かない。「たくさん返す」ことはどのモデルでも効かない。
- 受容集合が単一の塊の関数では、どんな選択規則も上位 K 取りに勝てない（11 関数すべて A12=0.50）。
- 脆さの原因は受理規則。親比較を最近傍置換にすると劣化 −13 → −2、ただし深さが 100% → 0%。
- 勝率と期待 regret は別の判定で、禁止領域では 53% のセルで食い違う（tilt 15% / instance 17%）。
  MC-ESO は 24/24 で順位勝ちしながら平均勝ちは 10/24。両方で勝つのは NM-Restart だけ。
- その原因は全滅。禁止領域は報告集合を丸ごと消しうるので損失に上限がない。損失は全滅時にしか
  出ず（生存時の超過 regret は中央値 0.00）、全滅率は spread が決める（ρ≈−0.85、24/24、
  禁止領域を 4 倍にしても不変）。
- 15 seed でも結論は不変（中央 A12 は 5 seed 版の ±0.05 以内）。seed 内で A12 を取り直しても
  符号反転は 288 セルで 0 件なので、プーリングは原因ではない。
- 測定上の教訓: 関数をまたいで相関をプールしてはいけない（スケールが桁で違い、符号が反転する）。
