# 事前登録 — その162（統合の回。研究の測定は「その93 のクラス上限の机上切り分け」1 本のみ）

**日付**: 2026-09-24。**claim**: キュー 1（統合の回）。**追加評価ゼロ・最適化 run ゼロ。**

## 1. この回がやること（キュー 1 の (a)-(d) をそのまま）

- **(a)** ログ 9 件を直近 3 件＋要約に畳む。畳む前に `check_cost_rows.py` → `check_doc_links.py` の順で通す。
- **(b)** 畳める路線のデータを消す。**消す前に `grep -rn` で被参照を確認し、
  docs に無い数値は移してから消す。**
- **(c)** `e161/scan_silent_null.py` を `scripts/` へ上げる。
- **(d)** **その93 の D=20 クラス上限 4 通りが、その151 の実測 MPR 0.3831 にどこで破れたかを机上で切り分ける。**

## 2. (d) の設計（測るものと、閉じる条件）

**読むのは保存物 2 本だけ**: `e93/ceiling_mpr_d20.csv`（4 通りの上限、40 抽選 null の集計 16 行。
**null の降下ダンプ本体は削除済み**）と `e151/descents.csv.gz`（実測 1,883 降下）。

**4 通りが何を仮定しているかを `e92/analyze.py` の定義から書き出し、仮定を 2 つに分ける**:
**(A) 40 抽選の着地分布の台が、この一族の到達できる集合を代表している**、
**(B) 降下 1 本の値段が「収束まで走った降下」の平均で固定できる。**
**無限再起動（`mpr_sup`）と ＋Chao1 は n に依らないので (A) だけに乗る** ＝
**実測がこの 2 つを超えたなら、破れたのは (B) ではなく (A) である。**

**関門**: 降下から数え直した台/K が その151 の記録 MPR と **16/16 で一致**すること
（一致しなければ数え方が違うので、以下の切り分けは出さない）。

**閉じる条件（キュー 1 (d) の文言そのまま）**: **破れたものを名指しでき、
D=10 で 97.2% 当たっていた理由も同じ枠で説明できること。**

**事前に置いた容疑 2 つ**（俯瞰がキューに書いたもの）:
**(i) 降下 1 本あたりの評価回数が次元で乖離している**（＝ (B) 側）、
**(ii) Chao1 の被覆外挿が低い被覆水準では過小になる**（＝ (A) 側の緩め方が足りない）。

**反証条件（これが起きたらこの切り分けは失敗）**:
- **(a) 実測の台が 16 問すべてで `mpr_sup` 以下**なら、破れたのは台ではなく採点／報告の側で、
  この枠では説明できない。**その場合は「切り分けられなかった」と書いて閉じない。**
- **(b) 実測ダンプを 40 本に間引いて Chao1 を当てたとき、全降下の台をほぼ埋める**（埋め残し ≤ 0.01／K）なら、
  容疑 (ii) は死ぬ。
- **(c) D=10 で同じ枠を当てて実測の台が ＋Chao1 の上限を<u>超える</u>**なら、
  「D=10 では screen が当たっていた」という前提自体が誤りなので、切り分けの枠ごと作り直す。

**`hunt_coverage.py --null` は回さない。`core/` は 1 行も触らない。MC-ESO の既定は変えない。**
