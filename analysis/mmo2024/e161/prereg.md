# その161 事前登録 — キュー 1（記録が黙って帰無を作れる経路 ／ その113 の引き直し）

**2026-09-23 18:30 UTC claim。新規 run ゼロ・追加評価ゼロ。**

事前登録の中身は俯瞰が 2026-09-21 にキュー 1 として書いた本文そのもの
（[research_loop.md](../../../docs/research_loop.md) の「未解決の問い」1 番）。
実行役は問いを差し替えていない。以下は着手前にその本文から写したもの。

## 測るもの

- **(i)** その150 §5 が挙げた 4 本のうち残り 3 本（`e113/analyze.py` / `e118/power_and_landing.py` /
  `e110/identity_check.py`）を 1 本ずつ叩き、入力が欠けているものは**理由を印字して exit 1** に変える。
  **保存ダンプが唯一の控えである本体は消さない。**
- **(ii)** `e113` が出した結論（MC-ESO は null の 130 倍の点を撒いて盆地は半分しか触れない、16/16）を、
  **いま生き残っている入力だけで引き直し**、acceptance_topology.md の その113 の節の数値と照合する。
- **(iii)** 同じ型の穴が他に無いか、`analysis/` 全体でもう一度機械で数える（その150 の数え方をそのまま使う）。

## 判定

**(ii) が記録値を再現すれば、頭の主張の機序側は無傷 ＝ 1 行書いて閉じる。**

## 反証条件（発火したら俯瞰に上げる）

- **(a)** (ii) が再現しない、または入力が足りなくて引き直せない
  → status.md の「書ける主張」の機序側（広さと深さの分解）に但し書きが要る。
- **(b)** (iii) で新たに 1 本でも出たら、「過去の帰無のうちどれがこの経路で作られたか」を数える問いを立て直す。

## 実行役が着手前に足した測り方（判定条件は変えていない）

- **(ii) の照合は目視ではなく機械で行う。** 基準は `e113/scored.txt`（その113 の記録出力そのもの）を
  正規表現で読み、**問題ごとに 4 量**（K ／ hunt 本数 MC ／ hunt 本数 null ／ null の 1 本あたり評価
  ／ @1e-1 被覆 MC・null）を突き合わせる。**16 問すべて一致して初めて「再現した」と書く。**
- **(iii) の走査は 3 本かける。** その150 の 2 本（A: 文字列で組むパスの実在、
  B: 実在チェック後に黙って抜ける loader の**形**）に加え、
  **C: `os.path.join(BASE, "eNNN", …)` と分割して組む形**（走査 A はリテラル 1 個の中に `/` が
  無いと拾えないので、その150 の一覧の `e113` / `e110` はこの形で漏れる）。
- **(i) は「直す」前に 1 本ずつ実際に叩き、`exit code` と `stdout` を記録する。**
  静的走査は「壊れている」しか言えず、**「黙って帰無を刷るか、落ちるか」は走らせないと分からない。**

## 枠を超えたときに落とすもの（先に決めておく）

**(i) を最優先、(ii) を次、(iii) を最後に落とす。** (ii) が主張の機序側に直結しており、
(iii) は数え直しなので次サイクルに持ち越しても記録は壊れない。
