# その169 事前登録 — キュー 1: NMMSO 側 9 run を出し、10 問の対検定を引く

**claim: 2026-09-25 18:30 UTC。run を出す前に書いた。** 方針欄は 2026-09-11 が最新で
(1)(2)(3)(4) とも履行済み ＝ 未処理の指示は無いので、キューの上から取った。

## 1. 問い

その167 が RL 側 9 run（`e167/baseline_all.csv`）を確定させ、**対照の NMMSO 側 9 run だけが残っていた**
（`pynmmso` の導入手順が前回の自動化モードで拒否されたため）。**この回でそれを出す。**
その167 §4 が**追加評価ゼロで 441 通りを全数走査し、決定境界を確定させてある**:
**10 問で α=0.05 に届くのは「追加 3 問を全勝」の場合だけ**（勝ち 3/3 で最小 p 0.03125、
**勝ち 2/3 では最小 p 0.0625 で届かない**）。**＝ 見るのは 3 つの符号だけでよい。**

## 2. 測るもの（キュー 1 の指定どおり。腕は作らない・既定は変えない）

* 手法 1 本: **NMMSO**（`swarm_size = 10·D` ＝ その136・その143 で直した公表設定。`core/optimizers/nmmso.py:55`）。
* 問題 3 問: **`N11-CF1-2D` / `N12-CF2-2D` / `N13-CF3-2D`**（2D、K=6/8/6）。**登録名で渡す**（短縮名は `KeyError`）。
* **seed 0/1/2**、**予算は suite 既定**（`--evals-frac 1.0` ＝ 3 問とも 200,000）。
* 採点は CEC2013 公式の rho ベース（`core/runner._niching_counts`、rho=0.01）。報告規則は `current`。
* **対照は保存物をそのまま使う** ＝ 追加評価は NMMSO の 9 run だけ。
  * 追加 3 問の RL 側: `e167/baseline_all.csv`（9 run）。
  * 既存 7 問（F14-F20）の両手法: `e166/baseline_all.csv`（42 run）。

## 3. 判定指標

**主指標は CEC2013 公式 peak ratio（MPR、5 水準平均）**。その166 §5 の実測（この suite では Score ≒ MPR/2、
相関 r=0.9914）により **Score は併記するが主指標にしない**（方針欄 2026-09-08 (2)）。
**関数別を必ず列挙する**（その162 の教訓）。対検定は**両側 Wilcoxon signed-rank exact**（n=10、α=0.05）、
効果量は rank-biserial を併記。**降下本数 ÷ K は NMMSO 側には無い概念なので、代わりに報告点数を併記する。**

## 4. 反証条件（事前に固定する。run の前に書いた）

* **(a) 追加 3 問のどれか 1 つでも RL − NMMSO が同点または負けなら、その時点で 10 問の最小 p は 0.0625 > α**
  ＝ **CEC2013 では有意にならないことが確定し、この軸は閉じる。** status.md の主張 1 の限定
  （「GECCO'2024/'2025 suite 上で」）は恒久的になる。
* **(b) 追加 3 問を全勝なら p=0.03125 < α** ＝ **主張 1 の限定は外れ、2 つの suite で支えられる。**
* **(c) NMMSO が 3 問とも MPR 1.00 に張り付いたら (a) の一種**（RL は 0.58〜0.67 なので全敗になる）。
  **その167 が (d)「2D に headroom 無し」を誤りと確定させたのは RL 側についてで、NMMSO 側は未測定。**
* **(d) 煙試験の関門**: NMMSO が 1 run 走らなければこの問いは環境で止まっているので、2 番へ移る。
  **【2026-09-25 18:35 UTC 通過】** `N11-CF1-2D --evals-frac 0.01 --seeds 1` が数秒で完走し
  報告 7 点・MPR 0.467。**＝ 前回の障害は解消した**（手順は §5）。

## 5. 環境（前回この問いを止めた箇所）

`acceptance_topology.md` の「環境の再構築」手順 1-3 がそのまま通った。
手順 2・3 は **site-packages ではなく scratchpad 上のディレクトリに展開し `PYTHONPATH` で渡す**形にした
（その106 と同じ。リポジトリには何も入らない）:
`pip download --no-deps --no-binary :all: pynmmso` → 展開 → `pynmmso/pynmmso.py` の
`random.sample(<set>, k)` を **4 箇所**（238 / 414 / 440 / 446 行）`random.sample(list(...), k)` に置換
（Python 3.11 は set を拒否する）。`re.subn` で 4 箇所とも当たったことを確認した。
