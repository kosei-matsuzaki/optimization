# `e115/s1/` は その168 が畳んで削除した（2026-09-25、統合の回）

**消したもの**（32 ファイル / 103,320 バイト）:
- `e115/s1/descents/M??-D10-PIN01_seed100.csv.gz` —— **16 本 / 100,414 バイト**（seed 1 の行単位の降下ダンプ）。
- `e115/s1/by_problem/M??-D10-PIN01.csv` —— **16 本 / 2,906 バイト**（1 ファイル 1 行の問題別集計）。
  **削除前に 1 本の `s1/by_problem_s1.csv`（16 行、ヘッダ同一）に畳んだ**ので、**この 16 行の数値は 1 つも失っていない**
  （畳んだ後の CSV は同じディレクトリに置いてある）。

**理由（「路線を畳んだ」ではなく「読み手がもう居ない」）**:
**`s1` を読む経路は `e115/analyze.py` の `SRC_COORD`（`load_runs(with_coords=True)`）だけ**で、
**その関数の呼び手は同じ script の `main()` 1 つ。** その `main()` は既定の `SRC_PLAIN` が
`e110/descents` / `e111/descents` を要求し、**その2 本は その118 が削除している**ので、
**その118 以降この script は exit 1 でしか終わらない**（実測済み）。
**後続サイクルは全部、自前の `load_runs` を書いて `e115/descents`（seed 0）を直接読んでいる**
（`e119` / `e139` / `e142` / `e149` / `e151` / `e153` / `e154` / `e158` / `e160` —— どれも `s1` を見ない）。
**＝ seed 1 の座標つきダンプは その118 から 50 サイクル、読み手ゼロで置かれていた。**

**[acceptance_topology.md](../../../docs/acceptance_topology.md) の冒頭の保持メモが
「`e115/s1/descents` は キュー 1 の解析スクリプト（`e119/analyze.py` / `headroom.py` / `robustness.py`）が
直接読むので消していない」と書いていたが、これは誤りだった** ——
3 本とも読むのは `e115/descents/{pid}-D10-PIN01_seed0.csv.gz`（seed 0）である。
**その168 がこの記述を直した。**

**数値の行き先**:
- 16 問平均（seed 1 単独の MPR 0.5350 ほか）は acceptance_topology.md の **その111 / その115 の節**。
- 問題別は `s1/by_problem_s1.csv`（16 行）と `e115/scored.txt`。**その168 の節に PR@1e-5 の 16 個も本文で書いた。**

**残してあるもの**: `e115/descents/`（seed 0、16 本）—— **生きた読み手が 9 サイクルぶんある D=10 の標準対照。畳まない。**
`e115/by_problem/`（seed 0、16 本）—— `problem_K()` が glob で読むので残す。

**`e115/analyze.py` の guard の文言に、s1 が消えた理由と行き先を足した**（`SRC_COORD` の定義自体は残してある ——
guard が「どのディレクトリが無いか」を名前で報告するのに使うため）。
