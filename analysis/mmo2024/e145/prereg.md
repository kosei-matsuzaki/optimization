# その145 事前登録 —— キュー 1: 勝っている null 自身のつまみを振る

**書いた時刻: 2026-09-19 18:35 UTC（run の投入は 18:32、集計コードは 1 行も書いていない時点）。**

## 何を測るか

`Restart-Lander` の `descent_budget` を **12500 → 1540** に下げた腕 `Restart-Lander-1540` を
**新 suite M01-M16（D=10、PIN01、正規予算 50 万評価、seed 0）で 1 本ずつ**回す（**新規 run は 16 本**）。
1540 は **MC-ESO の 1 segment の実測値**（その113）。これで **1 hunt の長さが MC-ESO と揃い、
本数も約 325 本 ＝ MC-ESO の 317.9 本とほぼ揃う**。

**変えたのは driver の `_METHODS` の 1 行だけ**（`scripts/niching_baseline.py`）。
`core/` は 1 ビットも触っていない。既存の `Restart-Lander` の行も不変 ＝ **記録済みの値は全部そのまま。**
MC-ESO は回さない（その113 の保存ダンプと対にする）。

## 読み A（頑健性。先に書く側）

**量**: 16 問平均 Score（`eps_loose+dedup`、`r = 0.05 × span`、5 水準等重み ＝ その115 の合法な最良腕）。
**採点は `e115/analyze.py` の `rule_indices` / `score` / `aggregate` / `paired` をそのまま import する。新しい統計量は定義しない。**

**関門（判定の前に通す）**: `e115/descents`（base の `Restart-Lander` seed 0）を同じ経路で再採点し、
**`e116/ranking_d10.csv` の `arm=legal` 行と 4 桁一致すること**（許容 5e-5）。外れたら判定に進まない。

**比較先**: MC-ESO **0.1385**（その116）／ 直した NMMSO **0.4144**（その139）／ 現行 null **0.6284**（その116、seed 0）。

**事前登録した反証条件**:

- **(A1) この腕の 16 問平均 Score が NMMSO の 0.4144 を下回ったら、頭の結果は `descent_budget` の選び方に依存する。**
  その場合**主張を書き換えず**、どの水準で順位が入れ替わるかを俯瞰に上げる（`sigma_ratio` を振る回が別に要る）。
- **(A2) 0.4144 以上なら、8.1 倍振っても null は MC-ESO と NMMSO の両方を上回る ＝ 弱い環 2（監査の非対称）は
  `descent_budget` については閉じる。**
- 副次: **腕 − 現行 null の対検定**（16 問、両側 Wilcoxon exact）。符号と大きさを記録する。
  **これは「つまみが効かない」ことの主張ではない**（効いてよい。問うているのは順位が動くか）。

## 読み B（機序。旧来の側）

**量**: 降下ダンプからの被覆 `detected(ε) = |{land_opt : best_f ≤ ε}| / K` の 5 水準平均（**その113 と同じ定義**）。
**比較先は 3 本**: 現行 null（95.2 本 × 6327 評価）**0.5644**、MC-ESO（317.9 本 × 1540 評価）**0.1094**、および今回の腕。

**事前登録した棄却条件**:

- **腕の 5 水準平均被覆が 0.40 以上** → **hunt 長は原因ではない ＝ 選抜が原因**と書き、以後 hunt 長の調律に 1 サイクルも使わない。
- **0.20 未満** → **hunt 長だけでほぼ説明がつく**と書き、「選抜が広い盆地に偏る」（その22）は新 suite では未証明のまま閉じる。
- **中間（0.20-0.40）** → 両方が効いている。**寄与を比で書いて記録**し、以後は寄与の大きい側だけを見る。

**併せて記録する**: 問題ごとの降下本数（**約 325 本 ＝ MC-ESO の 317.9 本と揃っているか**の確認。
揃っていなければ「hunt 長を揃えた」という対の前提そのものが成立しないので、その場合は本数も併記して読み B を条件つきにする）。

## 予算の逃げ道（事前登録）

**19:10 UTC で打ち切る。** 完走した問題だけで対を作り、**問題は落とさず、揃わなければ「部分結果」と明記する**
（その129 の教訓: 片側だけ完走した run は判定に入れない）。16 問が揃わない場合、読み A の (A1)(A2) は
**揃った問題の部分集合での値**として書き、**その131 の「部分集合は中立ではない」を明記する。**

## 片付け（この回の約束）

**この回が閉じたら `analysis/mmo2024/e113/hunts/`（3.0 MB、`analysis/` 最大の塊）を同じサイクルで削除する**
（この問いが e113 の hunt ダンプを使う最後の路線。CLAUDE.md の保持規則）。
**消す前に、この回が依拠する e113 由来の数値（MC-ESO の被覆 0.1094、降下本数 317.9、1 segment 1540）が
本文に書かれていることを確認する。**
この回が出す 16 本の降下ダンプは、**同じサイクルで 1 本の `.csv.gz` に畳む**（畳む前後で `analyze.py` の出力が
1 文字も変わらないことを確認してから消す。**畳む script と `rm` を 1 つのコマンド行に並べない** ＝ その131 の事故）。
