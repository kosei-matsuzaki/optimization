# `e113/hunts/` は その145 で削除した（3.0 MB、32 ファイル）

**約束どおりの削除。** キュー 1（その145）が **この hunt ダンプを読む最後の路線**で、
[research_loop.md](../../../docs/research_loop.md) のキュー 1 本文と その144 の申し送りが
「1 番が閉じたら同じサイクルで消す」と書いていた（CLAUDE.md の保持規則）。

**消す前に本文へ移した数値**:

- **MC-ESO の問題別 5 水準被覆と hunt 本数（16 問）** -> **`../e148/by_problem_e145_arms.csv` の `series=MC-ESO` の 16 行**
  （**その145 がこのダンプから計算して書き出した控え**）と
  [acceptance_topology.md](../../../docs/acceptance_topology.md) の その145 の節の表。
  **【2026-09-23 その161 が訂正】ここは `../e145/by_problem.csv` を指していたが、
  `e145/` は その150 が路線ごと削除しており、その時点でこの行は宙に浮いていた。**
  **実際の移設先は `e148/by_problem_e145_arms.csv`**（48 行のうち `series=MC-ESO` が 16 行）。
  **＝ この 16 行が MC-ESO の問題別被覆の唯一の控えである。**
- **16 問平均の被覆 0.1094 / 本数 317.9 / 1 hunt 1540 評価 / 深さ保持率 0.275 / 選抜一致率 72.8%** ->
  `scored.txt`（このディレクトリに残してある）と acceptance_topology.md の その113 の節。

**影響を受ける script は 2 本**（どちらも閉じた路線のもの）:

- `analyze.py`（このディレクトリ）—— 入力が無いので再実行できない。**出力は `scored.txt` に全部残っている。**
  **【その161】理由を印字して exit 1 するように直した**（null 側の読み先も、削除済みの
  `e110/descents` から生きている `e115/descents` に付け替えた）。
  **生き残った入力からの引き直しは `../e161/rederive_e113.py`。**
- `../e118/power_and_landing.py:92` —— その118 は「最適の配置の構造では説明できない」（ρ=−0.091）で閉じた路線。
  **結論は acceptance_topology.md の その118 の節にある。**
  **【その161 が実測】この 1 本は<u>黙って壊れていた</u>** —— `read_mceso` が `None` を返し、
  `part_c` が **MC_touch / MC@1e-5 を 0.00 として 16 問の表を刷ったうえで、その 0 で
  `landing_d10.csv` を上書きした**（＝ `mc_touch` / `mc_deep` 列の唯一の控えを消す）。
  **その161 が理由を印字して exit 1 に直し、上書きされた CSV を git から復元した。**

**その145 の `analyze.py` はこの削除に備えてある** —— `hunts/` があればそこから計算し、
無ければ `e145/by_problem.csv` から読む。**どちらでも出力は 1 文字も変わらないことを削除前に確認した。**
